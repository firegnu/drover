# 交接

新开主控先读这份，再读 `AGENTS.md`。**只写「现在在哪、下一步干什么、有什么悬着」**，设计和理由在 `docs/ROADMAP.md`。

---

## 2026-09-23 —— 看板标题配色已按用户直接授权微调

**这一节最新。** 用户在 T17 研究后直接授权实施最小视觉改动，明确不要另走任务。`b62c8d2` 已在 main 修改 `bin/drover-board`、定向显示测试与 ROADMAP：进行中/等你放行的标题状态词分别绿/黄粗体，空闲标题、任务名和栏目标题用默认前景粗体；文字、行位置、判据颜色及交互保持原样。未新建任务、worktree 或 agent。

- `/opt/anaconda3/bin/python3 -B tests/board-layout.py` 通过；`board-demo.py --record` 的 36 组合成 PTY 场景全过，改前后 375 帧屏幕文字一致，ANSI 核对了标题和栏目色码。当前真实终端的视觉效果仍待用户目视。
- T17 在 drover 中仍为进行中，须用户按 `g` 放行；主控未代操作真实 `next/done/go/loop`，未修改服务或推送。若颜色层级仍不满意，下一步由用户在常用终端反馈画面；窄屏密度和判据语义未实施。

---

## 2026-09-23 —— T17 看板 TUI 视觉探索已交付，等待用户审阅与放行

**这一节最新。** T17 研究记录和主控审查在 `docs/任务/m26-kanban-visual-explore 看板 TUI 视觉探索.md`。Claude Code `drover/dev-kanban-visual-1`（常规档 `opus[1m]` / high）提交最终修订 `022728b`；本地合并 `8177352`，主控审查 `6f7a377`，清理后收尾空提交 `b434d07`。交叉审查按路由结论未安排。

- 研究建议先讨论「标题状态色 + 节标题去青」这个最小实现草案，再逐项考虑窄屏密度和断行；未批准实施。当前生产合成 PTY 演示由 Claude 和主控各跑 36 组，全过；缺当前真实终端截图，用户主题里的颜色与字体效果仍待目视。D1(b) 的「未达到 / 失败」颜色区分不能靠现有字段可靠判断，若要做须另定数据来源并审议已定设计。
- 分支和 worktree 已清理，自开的 Claude 随工作目录删除一并关闭；`corral ls` 只剩 `corral/main` 和 `drover/main`。清理前它已回复 DONE、提交落盘、worktree 干净且无人接入，但 `corral status` 后来因 `ScheduleWakeup` 持续显示 `working`；用户明确确认它实际已 idle、状态有问题，主控据此完成清理。下一步由用户审阅报告、决定是否另排 UI 实施任务并按 `g` 放行 T17；主控未操作真实 `next/done/go/loop`，未改生产代码、ROADMAP、服务或配置，未推送。

---

## 2026-09-23 —— Claude 临时派发限制已解除

**（历史记录，当前队列见上。）** 用户明确通知：此前「暂时只派 Codex、不派 Claude，等用户通知后恢复」的限制已经解除。后续按 corral-dispatch 技能正常选择开发和审查 agent；旧交接中 2026-09-22 的临时限制仅作历史记录。项目 `AGENTS.md` 和 corral 模板都没有 Codex-only 限制，本次不改它们，也未派发新任务。

---

## 2026-09-23 —— jb-finetune 已切换到新协作流程

**（历史记录，当前派发约束见上。）** 用户明确废弃 jb-finetune 的旧 `request-review` / 规划者 / 旧看板，要求采用 corral 主控、corral-dispatch 分派审查和 drover 顶层队列。项目 `AGENTS.md` 已替换旧协议，旧项目配置已移出仓库并归档，专用旧规则文件已从当前树删除，旧评审记录保留为历史。JB 仓库本地合并 `db68db1`，收尾记号 `8aee6fe`，未推送。

- `drover init jb-finetune` 已登记项目；`.drover.conf` 指向现有 `jbfinetune/main`，`TASK_FILE_DIR=docs/任务`，`TASK_GATE=1`，项目 loop 关闭。`drover list` 核对为空队列、无进行中任务。旧 T1–T8 均已完成或放弃，不迁移历史任务。
- 这次是配置和协作规则迁移，尚未用真实 JB 开发任务端到端验证。下一件业务任务由用户决定；保留手动放行，不自行发 `next`、开 loop、推送或操作旧 herdsman 全局服务。
- drover ROADMAP 第 4 层和本项目 `AGENTS.md` 的旧隔离边界已按用户决定更新。未改 corral、corral-dispatch、drover 业务代码、服务或其他项目。

---

## 2026-09-23 —— T16 研究已交付，等待用户审阅与放行

**这一节最新。** T16 研究记录在 `docs/任务/m25-dispatch-planning 主控自主拆分与派员数量研究.md`。只读核对现有项目规则、corral-dispatch 技能和 corral 契约；用历史/合成样例演练单路、分阶段并行、立即并行及开发/审查 agent 数量，并用合成工作单位核对依赖和并发算术。研究提交 `f94f460`，本地合并 `62abf79`，收尾记号 `92f9eaf`。研究 worktree 与分支已清理，没有创建 agent。

- 结论：现有技能能执行已拆好的并行工作，但尚无明确的派发前拆/不拆及人数判断步骤，也未证明主控面对任意顶层任务能稳定判断。建议先由用户决定是否在本项目 `AGENTS.md` 试行判断规则；若要跨项目默认生效，须另行授权 corral 仓库的技能/模板任务。corral 命令和 drover 业务代码当前都无需改。
- 下一步由用户审阅 T16 报告、决定试行范围并按 `g` 核对放行；本轮未代用户操作真实 `done/go/next/loop`。未改 `AGENTS.md`、`ROADMAP.md`、corral/corral-dispatch、loop/TASK_GATE 或后台服务，不推送。T8 stash 原样保留。

---

## 2026-09-23 —— T15 已放行，T16 已入队

**（历史记录，T16 已由用户发出并完成研究，见上。）** 用户已放行 T15；`drover list` 显示 T15 已完成、当前无进行中任务。T16「研究主控自主拆分任务及并行 agent 数量决策」已通过 `drover add` 加入待办，等用户按 `n` 发出。

- T16 聚焦主控只收到一件顶层任务时，如何判断单人、先地基后并行、立即并行，以及每阶段开发和独立审查 agent 的数量与最大并发数。先用合成或历史样例做规划验证，交付现有能力、缺口、方案取舍和具体修改范围；正式实现另行决定。
- T16 未授权修改 corral/corral-dispatch、drover 业务代码或已定设计，也不启动真实开发/审查 agent。若方案涉及这些边界，先列明供用户裁定。顶层队列仍串行，保留手动 `n → g → n`。
- 本轮只追加队列任务并更新交接；已核对旧队列原样保留、T16 正文一致、`tasks.state` 未变。未操作真实 `next/done/go/loop`，未改 loop/TASK_GATE 或后台服务，不推送。T8 stash 原样保留。

---

## 2026-09-23 —— T15 研究已交付，实施与试行待用户决定

**（历史记录，后续 T15 放行与 T16 入队见上。）** 用户已发出 TASK T15；任务性质是研究探索，允许临时、合成环境的小规模验证，正式功能实现不在本任务内。研究记录在 `docs/任务/m24-parallel-research 主控多 agent 并行协作研究.md`，本地合并 `3dc771e`，只新增文档，未改业务代码或 ROADMAP。

- 结论已按用户追问纠正：项目 AGENTS 能触发主控分派，corral-dispatch 有并行时的操作规则，**但 corral 不判断任务，技能也不保证主控会从一件顶层任务中发现正确的子任务或判定该不该并行**。用户要的是只给一件顶层任务、主控主动判断；报告补了“单任务 / 先串行地基再并行 / 立即并行”的项目级判断契约草案，尚未采纳或写入 AGENTS。此前实验只验证派出后的 Git 判据，未验证主控拆分判断；下一步应先做只读拆分演练，再由用户决定是否在真实任务上受控试行。corral 没有顶层/子任务结构字段，drover 当前不能可靠显示子任务阶段。
- 合成状态还确认：主控 idle 超过 120 秒而开发 agent 正在 working 时，看板仍可能显示 `STOP/needs_me`。这是显示提示的宽松启发式，不影响完成判据；是否调整由用户决定。报告列出三项待裁定：要不要真实试行、现有看板是否够用、是否接受该提示。
- 下一步请用户审阅研究记录，再决定是否另排试行或实施任务。T15 在 drover 状态中仍是进行中；本轮未代用户操作真实 done/go/next/loop，也未改服务、loop/TASK_GATE 或推送。研究 worktree/分支已清理，没有创建 agent；T8 stash 原样保留。

---

## 2026-09-23 —— T15 范围纠正为研究探索

**这一节最新。** 用户明确纠正：T15 是研究探索，不是实现任务；允许在临时、合成环境做小规模验证实验。队列中 T15 的标题和正文已按此修正，下方 2026-09-22「规划并实现」的记录仅保留为历史。

- 下一步围绕一件顶层任务内部的多 agent 协作，核对现有 corral-dispatch 能力、实际缺口、候选方案和取舍，形成研究结论；正式实现由用户另行决定和排期。
- 本次未派发 T15、未创建 agent/worktree、未操作真实推进或服务。项目仍保持手动 n → g → n；不改 loop/TASK_GATE，不推送。T8 stash 保留。

---

## 2026-09-22 —— T15 已入队，明天讨论并行子任务协作

**这一节最新。** 用户要求加入明天的下一件任务：T15「规划并实现主控拆分任务后的多 agent 并行协作」。已通过 drover add 入队并回读核对完整正文，原队列和 tasks.state 未改，项目 loop 仍关闭。T14 已只读核实存在 start/done/go，已由用户放行，不再待放行。

- 用户意图：一件顶层任务交给主控，主控识别可并行部分、拆分并同时派多个开发 agent，子任务按需安排 review/返工，最后主控统一集成、审查与收尾。Claude Code managed agents 是体验类比，不是预定技术方案。
- 明天先商量计划，确认后再实现；今天不派发、不调研、不实现、不创建 agent/worktree。讨论现有 corral-dispatch 的可复用能力、并行依赖和文件边界、开发/审查组织、主控汇总及看板是否展示子任务。未预先批准顶层队列多任务并行、修改 corral/corral-dispatch 或突破现有依赖边界。
- 后台引擎已运行、当前仍手动 n → g → n、TASK_GATE=1（默认）；看板用 drover-board 快捷入口。此次未操作真实 next/done/go/loop、未改服务或配置、未推送。T8 stash 保留。

---

## 2026-09-22 —— 已授权真实安装及后台启动，项目仍手动推进

**（历史记录，下一件已排为上方 T15。）** 用户明确要求安装并启动后台服务。已执行本仓库 install.sh，创建 `~/.local/bin/drover`、`drover-board` 到 main 仓库脚本的软链，生成 `~/.drover/dev.drover.loop.plist`；登记 `~/Library/LaunchAgents/dev.drover.loop.plist` 并 bootstrap `gui/501/dev.drover.loop`。本次授权仅操作 dev.drover.loop，未碰旧 herdsman 服务。

- 安装前核对目标均不存在、服务未登记、无已有 drover loop 引擎，项目登记表只有本仓库且 loop 关闭。启动后复核服务 running、runs=1、PID 44037、尚无退出记录；stdout 显示每 5 秒一跳，stderr 为空。以后登录自动启动、退出自动重启，不要另开第二个引擎。
- 项目仍 `loop off`，TASK_GATE 未显式配置、有效值为 1。启动前后 `.drover.conf`、queue.md、tasks.state 的 SHA-256 完全一致；未发送、完成或放行任务。保持 n → g → n；以后 loop on + TASK_GATE=1 为逐件放行，loop on + TASK_GATE=0 为自动连续推进。
- 用户此前指定的简短看板入口已写进 `~/.zshrc`：`drover-board` alias 调用 `/opt/anaconda3/bin/python3 /Users/firegnu/Developer/personal_projs/drover/bin/drover board`，新终端中 --help 验证通过，安装后该 alias 保留。看板仍由人打开，q 只关闭看板。
- 下一步照常使用；本次没有打开项目 loop 或修改 TASK_GATE。T14 代码已完成，未代用户操作 g，不据此推断已放行。未推送；T8 stash 保留。运行前状态快照在 `/var/folders/vs/3tm61ygs569g764_td0zxtym0000gn/T/drover-service-enable-hz7m_lud/before.json`。

---

## 2026-09-22 —— T14 已合并收尾，等待用户放行

**（历史记录，后续真实安装及启动已在上方记录。）** T14 按用户批准的最小范围完成：服务 PATH 使用固定常用目录，不再复制终端 PATH；两项新回归及原安装保护保留。被审 `a1b64fc`，合并 `b3b2485`，收尾空提交 `a8326cb`，未推送。

- 主控定向审查通过：核对有效 RED→GREEN，重跑隔离安装套件 11 项及静态检查全过；合并后生产、测试与设计文档和被审提交完全一致。未跑全仓库，无独立交叉审查。详细证据和取舍见 `docs/任务/m23-install-path 安装环境 PATH 调研与修复.md`。
- 开发 worktree/分支已按干净且已合并条件清理；自开 `drover/dev-install-path-1` 在 idle、无人接入时随工作目录删除关闭。现场只剩 `drover/main`。T8 实验 stash 原样保留。
- 下一步用户按 g 核对并放行 T14；主控未操作真实 done/go/next/loop。没有实际安装、运行 launchctl、启用引擎或改变 loop/TASK_GATE，手动 n → g → n 照旧。
- 旧 plist 内容不同仍拒绝覆盖，未自动迁移。固定目录以外的自定义环境不自动支持；本任务不再扩展环境配置机制。原 T4 暂缓所指欠账已由 T14 解决，不再另排同一修复。

---

## 2026-09-22 —— T14 最小修法已获批准，继续实施

**（历史记录，现已合并收尾。）** 用户明确同意固定常用目录，不再复制终端 PATH；要求收敛，不再围绕通用 Python 环境设计。原 `drover/dev-install-path-1` 在 `m23-install-path` 继续实施，Codex gpt-6-astra / xhigh。具体授权已写入任务文件并同步到开发 worktree。

- 服务 PATH 固定为 HOME/.local/bin、/opt/homebrew/bin、/usr/local/bin 和系统目录；不新增覆盖变量、依赖预检/警告或安装拆分。旧配置不一致仍拒绝覆盖。两项回归覆盖无关 PATH 重装与临时 Python 不被保存。
- 主控已实测默认 Python 可运行 drover list，且固定 PATH 下 python3/git/corral 版本命令成功。后续只做隔离安装定向 RED→GREEN 与原安装套件，不重复环境矩阵，不跑全仓库。
- 初始路由覆盖面已被用户收窄，由主控定向审查，不再独立交叉审查，理由记录在任务文件。通过后按项目规矩本地合并收尾；不推送、不真实安装、不运行 launchctl、不启用引擎、不改 loop/TASK_GATE。T8 stash 保留。

---

## 2026-09-22 —— T14 调研复核完成，待 PATH 政策裁定

**（历史记录，原提案已被上方最小修法授权取代。）** 第一阶段调研提交 `8c2e7b4`，`drover/dev-install-path-1` idle，开发 worktree 干净。主控已将完整报告同步到主仓库任务文件并追加审查；T14 尚未完成，未启动实现。

- 主控复跑：两个目标缺陷均有效 RED，55 次矩阵观察退出 0，现有隔离安装测试 9 项通过。证据 `/private/tmp/drover-m23-ua0xpt1r/matrix-4y8j22pq/`，详情见任务文件。
- 待用户决定：固定默认服务 PATH + `DROVER_LAUNCHD_PATH` 显式完整覆盖；保留旧 plist 字节不一致就拒绝的规则。主控建议缺少服务依赖仅明确警告，不新增整个安装失败条件，以保留安装命令与启用后台服务的独立性。具体目录、输入校验、兼容代价与取舍在任务文件，ROADMAP 尚未改。
- 下一步获批后续派同一 agent 实施并安排独立审查。分支/worktree/agent 保留，不合并、不清理、不打收尾；主仓库任务文件和 HANDOFF 的审查/排期改动尚未提交。不操作真实推进、安装或服务，不推送；T8 stash 保留。

---

## 2026-09-22 —— T14 已正式委派，先调研安装 PATH

**（历史记录，调研已由上方审查交接接替。）** 已收到正式 TASK T14，原 T4 的暂缓已由本次排期取代。第一阶段交给自开 `drover/dev-install-path-1`，Codex 重档 `gpt-6-astra / xhigh`；路由重 0.99、交叉审查要，实施后安排独立审查。

- 分支/worktree `m23-install-path`，任务文件 `docs/任务/m23-install-path 安装环境 PATH 调研与修复.md`（主仓库任务提交 `2d4b2f2`）。本轮仅追加调研报告，探针放临时目录；不改生产代码、测试或 ROADMAP。
- 调查安装环境矩阵，隔离复现 PATH 重装拒绝、临时解释器残留、白名单遗漏自定义命令位置，提出最小方案与具体待裁定点。下一步读取状态、回复和报告，主控复核后确定实施范围；改变已定设计先交用户决定。调研 DONE 不代表 T14 完成。
- 不做真实安装，不执行 launchctl，不改真实 PATH/全局命令，不操作真实 done/go/next/loop、不启用引擎、不改 TASK_GATE。保留手动 n → g → n。T8 stash 原样保留，不推送。

---

## 2026-09-22 —— 原 T4 已重新排期为 T14，等待用户发出

**（历史记录，现已收到正式 T14 派发。）** 用户理解安装修复、启用引擎与项目 loop 是独立事项后，明确要求把 T4 放出来。已通过 `drover add` 以新编号 T14 重新加入待办，旧 T4 的放弃记录保留。

- 原任务正文完整保留，仍先调研安装环境与 PATH 风险，再定最小方案；涉及已定设计变更先交用户决定。实现与验证只用临时 HOME 和合成环境，不做真实安装，不运行 launchctl，不修改真实 PATH 或 `~/.local/bin`。
- 已回读验证 T14 正文一致、原队列前缀及 tasks.state 未改；项目 loop 仍关闭。保留手动 `n → g → n`，不启用引擎、不改变 TASK_GATE。
- 下一步由用户按 n 发出 T14 后再开始，后续开发仍只派 Codex。本轮未派发、未开 agent、未实现或运行测试、未提交推送；T8 实验 stash 原样保留。

---

## 2026-09-22 —— T13 用户实测认可并已放行，今日收工

**（历史记录，后续排期由上方 T14 交接接替。）** 用户反馈“已经放行了，我觉得还可以”，据此记录T13实际使用认可、已由用户放行，不再列为待验收或待放行；不扩大为所有设备/尺寸逐项验过。用户明确授权本次提交并推送，并要求今天到此为止。

- T13被审 `b0a586e`，合并 `ed93868`，收尾 `d8dad55`；定向检查、性能及PTY证据见下方历史交接和任务文件主控审查。代码此前已提交，本轮仅记录用户验收与收工，不重跑测试、不再开任务。
- 本次推送范围是main相对origin/main的全部本地已提交成果；推前刷新远端并执行gitleaks/trufflehog扫描，推后以远端SHA与本地HEAD一致为准。后续会话先只读核对同步状态，不因本条授权自动推送未来改动。
- 自开开发worktree/分支/agent已清理；T8实验stash `e039c0163f1582e89cb162f9dab45f71ba3b3bff` 原样保留。T4继续暂缓，没有下一件已授权启动的任务。今天收工，等用户下次指示。
- 未操作真实done/go/next/loop，未启动真实循环；放行事实来自用户反馈，不替用户重复操作。设计与实现取舍仍查ROADMAP及 `docs/任务/m22-history-scroll 完整历史与局部滚动.md`。

---

## 2026-09-22 —— T13 已合并收尾，真实设备体验待用户确认

**（历史记录，由上方用户验收交接接替。）** 完整历史与历史区原生鼠标局部滚动已落地，取消数据入口五条上限；正文与历史分别滚动，刷新按记录锚点保持位置。被审 `b0a586e`，本地合并 `ed93868`，收尾空提交 `d8dad55`，未推送。

- 主控定向审查通过：history 13项、body-scroll 10项、layout（含16组Tab/Unicode及消息边界实际curses）通过；另造65个分叉/合并区间对照旧统计、标签对象降级均一致。300条真实合成历史含路由复测约0.0958秒/1次Git，前轮约2.02秒/300次；批量只在当次刷新内计算，不跨刷新缓存。
- 开发16组/2300帧历史PTY已核对，主控另跑四组历史（120×32单空闲、80×24多进行中、40×10单进行中、160×32多进行中，含缩放）和两组正文PTY，抽读最终首尾画面，最早记录及长原因末尾可达。未跑全仓库、无独立交叉审查。证据 `/tmp/drover-m22-maAzfb/`、`/tmp/drover-m22-main-review-pty/`、`/tmp/drover-m22-main-review-extra/`。
- 合并后bin/tests/ROADMAP与被审提交相同，主仓库既有调查审查、用户授权及最新交接保留。开发worktree/分支已清理，自开 `drover/dev-history-scroll-1` 随工作目录删除关闭；本任务没有审查worktree/agent，现场仅剩 drover/main。
- 下一步由用户重开看板，在历史区试实际滚轮/触控板，确认旧任务能看到、正文与历史互不干扰，再自行按g核对放行。窄屏必要时先PgUp/PgDn到历史区；PTY证据不能替代真实设备验收，不绕过Computer Use限制。大祖先图或异常端点回退逐条查询仍可能更慢，性能数字仅代表本次合成样本。
- 重要文件：`docs/任务/m22-history-scroll 完整历史与局部滚动.md` 的主控最终审查/收尾；设计取舍在ROADMAP的T13节。不操作真实done/go/next/loop，不启动真实循环、不推送。T4暂缓；T8实验stash `e039c0163f1582e89cb162f9dab45f71ba3b3bff` 原样保留。

---

## 2026-09-22 —— T13 最小批量查询已获批准，继续原任务

**（历史记录，由上方T13收尾交接接替。）** 开发调查提交 `6efe51b`，仅 ROADMAP/任务记录，生产与测试未改。主控已读状态/回复并复核有效RED及真实合成Git性能：300条历史取消截取后，task_view 从约0.036秒/5次Git增至2.021秒/300次Git。调查通过，T13功能未完成，未合并、未补收尾记号。

- 建议仅在当次刷新历史统计中批量读取Git信息，保持原提交数语义；不加跨刷新缓存、后台线程或通用设施。用户已明确回复“批准”，允许按此最小方案继续。详见 `docs/任务/m22-history-scroll 完整历史与局部滚动.md` 主控前段调查裁定及用户批准继续一节；开发完整调查在该分支文件末尾。
- 保留 `m22-history-scroll` 分支/worktree 和自开 `drover/dev-history-scroll-1`（常规Codex），不要关闭或清理。本轮交原agent继续最小查询调整及原定历史滚动，按corral挂完成提醒；先验统计等价、5/300条性能，再验局部滚动/锚点/PTY；不加独立交叉审查。
- 证据 `/tmp/drover-m22-maAzfb/`，主控重测 `main-review-performance.json` 及两份 `main-review-*.log`。当前尚无T13滚轮/最终PTY/真实设备验收，设备体验交用户；无全仓库测试。
- 不操作真实done/go/next/loop、不推送；T4暂缓。T8实验stash `e039c0163f1582e89cb162f9dab45f71ba3b3bff` 已核对仍原样存在，不恢复、不删除。

---

## 2026-09-22 —— T13 完整历史与局部滚动已正式委派

**（历史记录，由上方性能调查交接接替。）** 用户发现历史统计11件完成但只能看到最近5条。代码确认task_view生成finished时有`[-5:]`限制，旧记录没有因此删除；已批准“完整历史＋历史区域局部滚动”设计并要求开新任务。

- 已收到正式TASK T13并委派自开 `drover/dev-history-scroll-1`，Codex常规档 `gpt-6-astra / high`；分支/worktree `m22-history-scroll`，任务文件 `docs/任务/m22-history-scroll 完整历史与局部滚动.md`（提交 `274eda7`）。路由tier拿不准（常规0.86），主控选常规；cross_review不要。完成提醒已挂，下一步查状态/回复及提交，按任务做定向主控审查。
- 范围：取消五条截取、沿用既有历史顺序/统计/排除规则；历史区按可用空间展开或限高，悬停滚轮只滚历史，保留正文局部滚动与PgUp/PgDn。位置提示按显示行；切项目归零，同项目刷新/新增记录尽量保持阅读位置，缩放夹限；无新模式/快捷键/持久化状态。
- 必须从合成tasks.state经真实task_view到最终画面验证12条以上历史及最早记录，不只绕过数据层造VM。性能放在本轮前段：5条对比数百条（建议300条）真实有效Git提交区间及任务文件查询，不能用空SHA/相同端点或全stub查询掩盖成本；若明显拖慢先提交调查报告最小方案，待主控/用户裁定，不自行引入缓存/异步架构或重新截断。保住T6/T7/T8/T12，实际PTY与真实设备证据分开，设备体验由用户确认。
- T12已由用户认可并放行。T4仍暂缓；T8实验stash `e039c0163f1582e89cb162f9dab45f71ba3b3bff` 原样保留。未推送、未操作真实done/go/next/loop，未启动真实循环。

---

## 2026-09-22 —— T12 用户认可实际观感并已放行，先保持现状

**（历史记录，由上方T13交接接替。）** T12四项显示整理完成：历史/指标降权与续行对齐、判据标签着色而理由普通色分行、普通短消息按实际末行列宽去重、顶底栏默认背景并仅突出按键。被审 `05aa875`，合并 `c5f59d2`，收尾空提交 `6d53d40`；未推送。

- 主控定向审查通过，无独立交叉审查。重跑board-layout（含原8组Tab/Unicode与新增8组消息边界PTY）、board-body-scroll（10项）、board-body-pty（8组）均过；旧生产配新检查准确复现短消息重复RED。未跑全仓库、未重复全部视觉矩阵。
- 开发前后各36组实际PTY（291/299帧）已核对统计并抽读关键最终屏幕；主控另验正文滚轮8组。开发证据 `/tmp/drover-m21-4KVqoV/`，主控滚轮证据 `/tmp/drover-m21-main-review-body/`。临时目录保留用于复核；不是实际桌面或设备验收。
- 接受判据分行多占少量行的取舍，80×24长正文下判据入口仍可见、余下原因和内容按原PgUp/PgDn完整可达。短消息只去重已知普通回执；T6警告/拒绝/放行报告及未知、多行、过长消息保留完整滚动版本。输入、采集、判据、命令/超时和刷新均未改。
- worktree/开发分支已清理，自开 `drover/dev-board-polish-1` 已随工作目录删除关闭。合并后bin/tests/ROADMAP与被审提交相同，主仓库既有HANDOFF保留。
- 用户明确反馈：“看起来还不错。先这么用着。我已经放行任务了。”据此记录实际使用观感认可、T12已由用户放行，不再列作待验收/待放行；不扩写成各种尺寸、多项目和设备均逐项通过。先保持这版，不追加视觉调整、不自动派新任务。本轮仅依据用户反馈更新交接，未重新查询或操作真实队列、未重跑测试。
- T4继续暂缓，只派Codex；T8实验stash `e039c0163f1582e89cb162f9dab45f71ba3b3bff` 原样保留、不恢复/删除。详见 `docs/任务/m21-board-polish 看板视觉层级整理.md` 末尾主控审查及收尾。

---

## 2026-09-22 —— T12 视觉整理已正式委派

**（历史记录，由上方T12收尾交接接替。）** 用户同意实施T11的四项视觉建议，继续curses、保留现有交互，不迁移框架。已通过drover add加入T12“整理看板视觉层级与排版（保留现有交互）”，完整正文回读一致，tasks.state未改。

- T11已由用户放行；现已收到正式TASK T12并委派自开 `drover/dev-board-polish-1`，Codex常规档 `gpt-6-astra / high`。分支/worktree `m21-board-polish`，任务文件 `docs/任务/m21-board-polish 看板视觉层级整理.md`（提交 `65d7a20`）。路由档位拿不准（常规0.78），主控选常规；交叉审查不要。完成提醒已挂，下一步查状态/回复及提交，主控只做定向审查。
- T12范围：历史/指标降权与续行对齐、判据符号/名称突出且理由普通色、底部能完整容纳的普通短消息去重而长消息/警告保留、顶底栏低对比统一。保持中文/Unicode/Tab、T6消息、T8局部滚动与全部内容可达性；只跑相关定向检查及合成实际PTY，不跑全仓库四套。用户做最终终端观感确认，受限桌面访问不绕过。
- T11评估已本地合并并清理，文档与裁定见下方。T12只允许显示层、直接相关测试及ROADMAP/任务记录；允许按新视觉目标精确更新反色/坐标等纯视觉期望，必须逐条解释、保留行为断言。通过后按规矩本地合并清理补收尾；用户负责常用终端最终观感确认，不自行补签。未批准其它功能或框架迁移。T4继续暂缓，只派Codex；T8实验stash原样保留。不推送，不操作真实done/go/next/loop。

---

## 2026-09-22 —— T11 排版评估已合并收尾，未授权UI实现

**（历史记录，后续实施授权由上方T12交接接替。）** 仅文档评估完成：初稿 `bd704db`、截图补评 `d909d64`，主控逐条裁定后本地合并 `7b65ed9`，收尾空提交 `f752c60`；未推送。

- 用户三张附件覆盖宽屏单项目的空闲、刚派发、刷新后状态，主控和评估agent均已看图。四项建议：弱化历史及指标、突出判据符号/名称而收敛长理由着色、短操作消息有条件去重、统一顶底栏且优先降低对比。满宽高亮/外框仅为草案，不是既定设计。详情见 `docs/任务/m20-board-layout 看板排版评估.md` 末尾主控最终裁定。
- 只有任务文档变化，通读及diff检查通过；没跑测试、没有交叉审查、没有修改功能、配置或ROADMAP。保留原主仓库bd704db初稿审查和最新交接。窄屏/多项目及真实设备交互未验，不扩大三图的证据范围。
- worktree/开发分支已清理，自开 `drover/dev-layout-review-1` 已随工作目录删除关闭；其它项目agent未动。T11仅交付评估，下一步用户可按g核对放行，并选择是否另排视觉实现任务；不能自动实施、入队或迁移框架。
- 未操作真实done/go/next/loop。T4继续暂缓，只派Codex。T8实验stash `e039c0163f1582e89cb162f9dab45f71ba3b3bff` 原样保留、不恢复或删除。

---

## 2026-09-22 —— T11 看板排版轻量评估进行中，三张实际截图已收到

**（历史记录，由上方T11收尾交接接替。）** 用户明确当前功能基本满意，只觉得排版不够优雅；本次仅评估，不实现UI、不迁移Textual或其它框架。

- T10已由用户放行：本轮入队T11前只读看到tasks.state中T10的done/go事件。T11按用户要求通过drover add加入，完整正文回读一致、原队列及tasks.state未改写；随后收到正式TASK T11。
- 已委派自开 `drover/dev-layout-review-1`，Codex轻档 `gpt-5.6-luna / medium`；分支/worktree `m20-board-layout`，任务文件 `docs/任务/m20-board-layout 看板排版评估.md`（任务提交 `34634cf`）。只在该任务文件追加三到五条问题及最小建议、字符布局草案，未授权功能实现；无交叉审查，完成提醒已挂。
- 用户已提供三张实际截图：桌面 `SCR-20260922-qteh.png`（空闲待办）、`SCR-20260922-qtkf.png`（刚派发、有操作消息）、`SCR-20260922-qtpm.png`（进行中、刷新后）。主控已看图；截图补评通过corral send --after排给同一agent（当时仍working），下一轮读取用户附件并修正推断。Computer Use访问Ghostty被安全限制拒绝，未绕过；不能把附件目视扩大为实际设备交互验收，窄屏/多项目仍无截图。
- 下一步查agent状态/回复和文档，注意初稿完成提醒可能早于截图补评：working时重新挂提醒，idle时核实完成记录是否实际包含三图。主控轻量审查后反馈三到五条建议和布局草案，保持原有功能和交互，不跑测试套件、不加安装或依赖。
- 主控未操作真实done/go/next/loop、未推送。T4继续暂缓，只派Codex；T8实验stash `e039c0163f1582e89cb162f9dab45f71ba3b3bff` 原样保留，不恢复/删除。其它项目agent不动。

---

## 2026-09-22 —— T10 list安全输出已合并收尾，等用户放行

**（历史记录，当前由上方T11交接接替。）** T10只将cmd_list四处任务文字输出改用现有print_text，避免异常标题/放弃原因中的孤立代理码导致整份列表崩溃；正常文本和原始数据不改。被审 `491d7f6`，本地合并 `5df101c`，收尾空提交 `d55230c`，未推送。

- 主控轻量审查通过，独立定向入口 `python3 -B tests/list-output.py`：1项/6子场景通过；旧生产临时副本同测恰好5类UnicodeEncodeError失败、正常对照通过。严格UTF-8、完整输出/后续条目、合法Unicode及内存/文件不变均有检查。只跑定向，未跑全仓库、未加交叉审查。
- 开发worktree/分支已清理，自开 `drover/dev-list-surrogate-1` 随工作目录删除一并关闭。合并后bin/tests与被审提交相同，主仓库无未提交修改。
- 下一步用户按 `g` 核对并放行T10；主控未操作真实done/go/next/loop，不自动派新任务。T4继续暂缓，只派Codex。
- T8兼容实验stash `e039c0163f1582e89cb162f9dab45f71ba3b3bff` 原样保留、不恢复/删除。T9已由用户放行。欠账14到此解决，其余欠账未扩大范围。
- 详情读 `docs/任务/m19-list-surrogate 列表异常字符安全输出.md` 末尾完成记录/主控审查/收尾。

---

## 2026-09-22 —— T9 已由用户放行，流程反馈顺畅

**（历史记录，由上方T10交接接替。）** T9 只修改 QUICKSTART/手册：验收命令仅来自项目 CHECK_CMD，正文不执行命令；空值不运行、第3条不适用，其它判据照旧；两个任务示例改为自然语言。被审 `37c6ba6`，合并 `621027a`，收尾空提交 `d2b3978`，未推送。

- 主控轻量审查通过：通读/搜索/Markdown与diff检查，两份文档合并后与被审提交相同。按用户范围未跑测试、未执行示例、无独立交叉审查。
- 开发worktree和分支已清理，自开 `drover/dev-check-docs-1` 随工作目录删除一并关闭。未动其它agent、真实队列或服务。
- 用户确认「我放行了，感觉还挺顺」：T9已由用户放行，流程反馈顺畅。已按用户选择加入待办 T10「修复 drover list 遇到异常字符标题时崩溃」，仅输出边界和定向回归；通过drover add入队并回读核对，tasks.state未变。尚未派发/开agent/实现，由用户按n发出。主控未操作真实done/go/next/loop，也未另行查询队列；T4继续暂缓，只派Codex。
- T8已由用户实测通过并放行；兼容实验stash `e039c0163f1582e89cb162f9dab45f71ba3b3bff` 原样保留，不恢复/删除。此轮解决了历史交接中“两份文档残留正文覆盖CHECK_CMD旧说明”的欠账，其余欠账未动。
- 细节读 `docs/任务/m18-check-docs 验收命令说明纠偏.md` 末尾审查和收尾记录。

---

## 2026-09-22 —— T8 用户实测通过并已放行

**（历史记录，由上方 T9 交接接替。）** T8 正文限高/鼠标局部滚动已本地合并：被审 `885d49f`（生产 `6b914a7`），合并 `c3aabe9`，收尾空提交 `1ca190d`。采用用户确认的原生 curses 简单方案，使用现有 `/opt/anaconda3/bin/python3`，不再做旧curses鼠标协议兼容。不安装、不改 PATH/启动器；未推送。

### 当前状态与下一步

用户明确反馈：「我测试了一下，可以了，已经放行了」。据此记录 T8 实际使用验收通过、已由用户放行；不再列作待验收或待放行。用户未细分设备/场景，本记录不扩写成每种鼠标、触控板或尺寸均逐项验过。

- T9「修正文档中已过时的验收命令说明」已收到正式TASK，已委派 `drover/dev-check-docs-1`，轻档Codex（gpt-5.6-luna/medium），完成提醒已挂，分支 `m18-check-docs`，worktree `../drover-worktrees/m18-check-docs`。任务文件 `docs/任务/m18-check-docs 验收命令说明纠偏.md`；仅改两份文档及完成记录。路由轻/不要交叉审查，主控轻量审查后本地合并收尾，不跑测试套件。T4继续暂缓，只派Codex。
- 实际入口为 `/opt/anaconda3/bin/python3 ./bin/drover board`；安全合成演示为 `/opt/anaconda3/bin/python3 tests/board-demo.py --scene body --multi`。旧curses退化行为未改变。
- 主控未操作真实done/go/next。本次仅依据用户确认更新文档，未重新核对真实队列、未推送。自动化证据仍是合成输入/实际PTY，实际使用通过的依据是用户反馈。

### 已完成与留存

主控两套回归、定向/PTY/Tab与可达性已过；独立Codex结论可以合并，必须改0、建议改0，主控逐项接受。独立补验5项定向、内存RED/缺陷自证、3个实际PTY生命周期（编辑返回、q/SIGINT/异常退出与终端恢复）通过；本轮不重复全量。合并文档保留最新运行范围，bin/tests与被审提交完全一致。

开发和审查两个worktree及开发分支已清理；对应自开 `drover/dev-body-scroll-1`、`drover/dev-review-body-1` 随工作目录删除一并关闭。drover只剩main，其他项目agent未动。兼容实验另存Git stash `e039c0163f1582e89cb162f9dab45f71ba3b3bff`，原样保留，不属于交付、不恢复/删除；旧兼容返工提醒不得触发恢复。

优先读 `docs/任务/m17-body-scroll 任务正文限高与鼠标滚动.md` 末尾最终裁定/收尾、`docs/任务/m17-body-scroll 交叉审查.md` 末尾。设计与运行范围在ROADMAP，操作入口在QUICKSTART。其它欠账沿用下方历史交接。

---

## 2026-09-22 —— T7 已本地合并收尾，桌面视觉由用户单独验收

**（历史记录，由上方 T8 交接接替。）** T7 看板字符网格视觉优化已本地合并 main：被审代码 `3486cf5`，合并 `e2a40a7`，收尾空提交 `1cca95d`。未推送。开发/审查两个 worktree 和开发分支已清理；自开的 `drover/dev-kanban-ui-1`、`drover/dev-review-ui-1` 已随工作目录清理一并关闭。drover 侧仅剩 `drover/main`，未动别人的 `corral/main`、`globalmesh/main`。

### 下一步

用户明确确认：允许 `tests/drover.sh` 单条两行历史断言调整；**本地主控收尾后，由用户单独做桌面视觉验收**。这项尚未验收通过，不再作为本地合并前置门槛。

- 用户退出旧看板后重新运行 `python3 ./bin/drover board`，查看新布局；若有视觉问题，再按用户反馈安排窄范围修正。
- 验收满意后由用户按 `g` 核对、记完成并放行 T7。主控未操作真实 done/go/next，实时 list 仍是 T7 进行中、待办为空；T6 已完成。
- 继续手动推进，不开循环；T4 继续暂缓，不自动恢复。后续开发/交叉审查仍只派 Codex，等用户通知再恢复 Claude。

### 已完成验证

主控与独立 Codex 代码审查通过，剩余必须改/建议改均为 0。首次实现四套回归通过；主控重跑看板、drover 两套通过，并录制 28 组真实 PTY（158 帧）检查宽窄屏、单/多项目、长内容可达性、缩放及 T6 消息。审查发现 Tab 缩进在双栏覆盖命令尾部，已用最小显示层修复；八组真实 PTY 定向回归通过，旧代码副本验证能准确抓住两组双栏 Tab 缺陷。后续只复核该增量，没有重复全仓库测试。合并后生产代码/测试/ROADMAP 与被审提交逐字一致。

桌面 Terminal 访问被 Computer Use 安全限制拒绝，未绕过，故字体、配色和实际字形目视由用户接手；不得把 PTY 证据写成已做桌面目视。

优先读 `docs/任务/m16-kanban-ui 字符网格视觉优化.md`、`docs/任务/m16-kanban-ui 交叉审查.md`。设计/取舍在 ROADMAP 对应小节；合成演示为 `python3 tests/board-demo.py --scene working`（可加 `--multi`），只显示合成数据，不执行队列操作。

**其余悬着**：QUICKSTART/手册中正文「验收：」覆盖命令的旧说明仍待后续文档任务；其它欠账沿用历史清单，本次未扩大范围。

---

## 2026-09-22 —— T6 已合并，等用户按 g 验收

**（已由上方 T7 交接接替；T6 已由用户完成。）** T6（`m15-go-done`）已本地合并到 main，合并提交 `436e944`，收尾空提交 `c42db51`。**未推送**。两个 worktree 和开发分支已清理，对应自开的 Codex 开发/审查 agent 已关闭；drover 这边只留 `drover/main`，未动别人的 `corral/main`。

### 下一步

**用户在看板按 `g` 验收 T6，不需要先敲 `drover done`。** 主控未操作真实 done/go，T6 仍在进行中；自举口径暂为 **4/5 + T6 待用户验收**，按 g 成功后再确认第 5 件闭环和零误判。

- `g` 现在核对一次、记完成并放行；失败显示具体原因，已经记完成的只放行。它本身不发下一件，手动下一件仍按 `n`。
- 保留 `drover done` 兼容和引擎入口，手动缺收尾记号仍明确警告并接受人的确认；具体设计与取舍见 ROADMAP 对应小节。
- 仍为全手动：`loop` 关闭、`TASK_GATE=1`，不擅自启用无人值守。
- **T4 继续暂缓**，等用户明确决定后用新编号重排；不算完成，也不自动恢复。T6 之后暂无待执行任务。
- **开发和交叉审查暂时只派 Codex，不派 Claude**，等用户通知后再恢复。

### 验证和审查

四套回归在实现及首次交叉审查阶段全绿；后续两次返工仅重跑相关测试/定向检查，没有重复全仓库审查。最后的超时修复经主控 drover 套件和独立审查者定向慢验收 GREEN、恢复旧超时的植入验证通过，最终结论「可以合并，剩余 0 条」。合并后核对生产代码、测试和操作文档与被审分支完全一致。

本次抓住并修复两项正常使用问题：TUI 只显示首行导致警告/拒绝原因丢失；TUI 的 120 秒外层期限会提前杀掉正常慢验收。现在消息优先显示放行状态、警告和拒绝原因，go 的外层期限与引擎一致为 `CHECK_TIMEOUT + 60`，其它短命令保持 120 秒。证据在 `docs/任务/m15-go-done 把完成核对折进 go.md` 和 `docs/任务/m15-go-done 交叉审查.md`。

**悬着**：QUICKSTART / 手册仍留有正文「验收：」可覆盖 CHECK_CMD 的旧说明，与已删除的语法不一致；本次按范围未清理，后续文档任务处理。其它欠账继续见下方历史清单。

---

## 2026-09-22 —— T4 暂缓，下一件 T6

**（进度已被上面那节接替。）** 用户决定当前先用手动推进，无人值守还没到需要安排的时候；**T4 等用户认为时机合适后再排，不自动恢复**。

- T4 已用 `drover drop T4` 撤出待执行队列，原因记为暂缓；命令显示「放弃」，但原任务正文仍保留在 `queue.md`，欠账 6 尚未解决。以后重新排期用新编号。
- **T6 已收到派发：把记 done 折进 `drover go`，删掉手敲那一步。** 主控按 corral-dispatch 委派、审查、本地合并；完成后由用户按 `g` 验证，不由主控代操作真实队列。
- **临时派发约束（2026-09-22 用户指定）**：用户反馈 Claude 服务故障，后续开发和交叉审查暂时只派 Codex，不派 Claude；等用户通知后再恢复。
- 自举仍是 **4/5**；T4 暂缓不算完成。仍为全手动，`loop` 关闭、`TASK_GATE=1`，`drover done` 和放行由用户操作。

---

## 2026-09-21（夜）—— 自举 4/5，验收那条语法删了

**（排期已被上面那节接替。）** 今天又跑完两件（`T3`、`T5`），中间用户叫停问了一串「为什么要有验收命令」，**结果是把队列条目的「验收：<命令>」覆盖语法整个删掉了**。main 干净、没有 worktree、没有别的分支、没有开着的 dev agent，四套全绿，收尾记号 11 条。

### 新 session 第一件事

**按 `n` 发 `T4`。** T5 用户已经自己跑完 `drover done` + `g`，队列干净地停在 T4 前面。

队列：

- `T4 install.sh 把整条 PATH 烤进 plist`（欠账 6，**先调研再定方案**）
- `T6 把记 done 折进 drover go，删掉手敲那一步`（用户今天定的，设计见 ROADMAP「记 done 折进 drover go」）

> **`drover done` 归人跑，主控别抢。** 档位是「什么都不自动」，`HANDOFF` 老早写死「每一步都要人动手」。主控这次抢跑过一次 T3 的，被用户逮到了。

**档位仍是「什么都不自动」**：`loop` 关着，`TASK_GATE=1`。**不要擅自 `drover loop on`。**

### 今天最大的一件：删掉「验收：<命令>」覆盖语法

`T3` 做完之后 `drover done T3` 判不过，判据 3 报 `退出码 127`——队列条目里那行「验收：给 detail_lines 补一条精确文本断言…」被当成 shell 命令跑了。

用户追问「为什么要有这个验收命令」，查下来：

- ROADMAP「三个死路」里**明确否决过**「验收命令退出码 0」当完成信号（写不出验收命令的活会被挡在外面；就算写了也只证明没坏）。依据只有收尾记号。
- 这条语法是 `4ccaf51`（2026-09-11，老 herdsman）来的，`bbca61f` 改名时原样搬过来，**drover 从没重新评估**。它当初的安全边界是「只认 queue.md 里写的，网页表单拒收」——网页那层 `01c4424` 已经整个删了。
- `drover --help` 对 `queue.md` 写着「下面**随意写约束和验收**」，而那行又会被当 shell 跑——**两句话不能同时成立**。
- 在 drover 自己仓库里它**从没成功用过，只成功炸过一次**。

**用户定：删。机制保留、默认为空**（`CHECK_CMD=` 空着 = 这个项目不跑验收，判据 3 恒为「不适用」不参与判断），以后前端项目想「每个人做完跑一遍测试」再填。提交 `29ed637` + `2fe729b`。

> **别再往回加**：不要恢复正文覆盖 `CHECK_CMD` 的语法，不要给「验收：」行加校验当补丁。要 per-task 验收，先想清楚它和「queue.md 随意写」怎么共存。

### 一个卡死过的机制，记住它

改了 `queue.md` 对 T3 **没用**——**进行中的任务，正文是派发那一刻冻进 `tasks.state` 的快照**，`fold()` 读的是它。而且没有任何受支持的改法：`drover done` 不吃覆盖参数，`drover edit` 只能动「队列里还没开始的」。

所以「派出去之后才发现任务正文写错了」目前无解，只能 `drop` 重来。**这次是靠删掉整条语法绕过去的，不是解决了这个问题。**

### 两件活

**T3 → `m13-detail-flatten`**（欠账 12）。`detail_lines` 的 `" ".join(why.split())` 按 Unicode 空白切，把 `m12` 刚修好的分支名又改写回去。改成 `re.sub(r"[\t\n\v\f\r\x1c-\x1f]+", " ", why)`。

> **主控审查逮到一条必须改**：dev agent 第一版只写 `[\r\n]+`，放进来 7 个旧 `split()` 挡着的 ASCII 控制字符。实测（真 pty + curses，80x24）末行右边缘 `addstr` 带 tab 直接 `error('addwstr() returned ERR')`，`put()` 没有 try/except，看板当场崩。**这是改动引入的回退，不是既有问题**，所以是必须改而不是建议改。tab 进得了 `why`——`CHECK_CMD` 和收尾记号的 commit subject 都是人写的。

**T5 → `m14-print-surrogate`**（欠账 11）。`criteria_report` 输出路径上 `print(line)` 遇到孤立代理码抛 `UnicodeEncodeError`。加 `print_text()` 只在输出时转义 `\udxxx`，原始正文和理由一个字不改。**连带把 `84f76c3` 撤掉的那条回归补回来了**——新写法用 `-I` 忽略环境变量、显式固定严格 UTF-8，主控在四种 `PYTHONIOENCODING` 下各验过一遍全绿。欠账 11 到此了结。

### 今天新学到的

**造含代理码的测试数据不能过 shell。** 主控自己踩了一次：`echo` 里写 `\ud800`，吐出非法 UTF-8 字节，把 `route.py` 的 stdin 解码打崩了。要用 Python 直接写文件，或写 JSON 的 `\udXXX` 转义让 `json.loads` 解出来。

**`git merge -F -` 不认 stdin**（`could not read file '-'`）。消息复杂时写临时文件再 `-F <文件>`，和 `git commit` 那条教训同源。

**两条提醒通道的时间差今天看得很清楚**：后台 `corral wait` 几乎立刻叫醒主控，`corral send --after` 那条要等主控彻底闲下来才进得来（人工操作避让）。**两条都要挂**，先到的处理，晚到的查一下 `not_found` 就忽略。

### 主控这次犯的错

**抢跑了 `drover done T3`。** 档位写死「每一步都要人动手」，主控自己跑了。`drover done` 是只读核对、只追加一条事件，没有破坏性，但它污染了「5 件零误判」里「人亲眼看每一步」那部分。**T5 之后没再抢。**

**在「能不能省掉这一步」上来回误解了四轮。** 用户问的是「这个步骤能不能整个不存在」，主控一直往档位上扯（「翻到档 2 就由引擎跑了」）——**那一步还在，只是换了人跑**。结论是 `T6`：把记 done 折进 `drover go`。设计和理由在 ROADMAP「记 done 折进 drover go」。

### 下一步

按 `n` 发 T4。T6 排在 T4 后面，位置用 `drover move` 随时调。

**验收口径不变**：一律放行模式跑够 5 件、没有「没做完却判成做完了」。现在 **4/5**，四件里零误判。

---

## 2026-09-21（晚）—— 自举 2/5，看板补了两块

**（已被上面那节接替。）** 自举跑完两件活（`T1`、`T2`），另外**主控直接改了一次看板**（用户授权的破例）。main 干净、没有 worktree、没有别的分支、没有开着的 dev agent，四套全绿，收尾记号 9 条。

**队列里排着两件，都还没发**（用户回家后要亲自按 `n` 发 `T3`，看正文渲染效果）：

- `T3 看板详情把 NBSP 和 U+2028 折叠掉了`（欠账 12，轻量）
- `T4 install.sh 把整条 PATH 烤进 plist`（欠账 6，**先调研再定方案**）

**档位仍是「什么都不自动」**：`loop` 关着，`TASK_GATE=1`。**不要擅自 `drover loop on`。**

### 主控直接改的那次（2026-09-21 晚，用户授权破例）

用户看板用了一阵之后提了三件，并说「直接你来完成，不走委派和任务」。`AGENTS.md` 写着「主控不自己写功能代码」，**这是一次有意的破例**，提交 `a5ca8dd`、设计记在 ROADMAP「任务正文和『干活的 agent』上屏」。

1. **任务正文上屏**——`card_vm` 带 `body`，在「当前这件活」标题下缩进摊开，不截断（长了有 `PgUp`/`PgDn`）。**队列项不带正文**（会让列表暴涨）。正文只进显示，和「路由：」行同一条边界。
2. **多任务展示**——本来就全列，不用改。之前看到「空了」是因为队列真空。
3. **干活的 agent**——`crew_of()` 用 `corral ls` 的 cwd + `git worktree list` 映射，只显示名字 / 状态 / 闲了多久 / 哪个 worktree。

> **`crew_of` 是 `m5` 有意删掉过的东西**（当时的注释：「drover 是任务看板，不是 agent 看板」）。这次加回的是轻量版，**边界照旧不加「在调什么工具」「接入去看」**，测试里有 `lacks '在调'` 守着。以后别顺着往 agent 看板长——那是 corral board 的活。

**刷新仍是 30 秒**：`corral ls` 本来每次就调，新增的只有每个 agent 一次 `status`（实测 39ms，典型 0–2 个）。`CREW_TIMEOUT = 2` 秒防单个 agent 卡住拖垮看板；查不到状态标 `unknown` 照样列出来。

**这次自己抓到一回假绿**：六次缺陷植入里，「不显示正文」那次居然全绿——正文根本没有断言守着，补了数据层和渲染层两条才守住。**植入自检不是走过场。**

### 前两件活留下的经验

**交叉审查要按 `core_rules` 决定给不给。** `T1` 只有 0.37，跳过了，一轮就过；`T2` 是 0.82（和 `m9` 的 0.9 同量级），给了，挖出真问题。**别每件都给。**

**派交叉审查时必须带这句**（`m10` 磨了三轮的教训，`T2` 带上之后直接 0 条必须改）：

> 分级按「正常用会不会撞上」来定：需要特意构造畸形输入才触发的，标「建议改」或「可以不改」，不要标「必须改」。

### 主控自己犯的错，都记下来了

1. **写审查结论时用 `git add -A`，把 dev worktree 的 `.pyc`（81KB）提交进了分支**，交叉审查逮到的。`m10` 踩过一次没修根因。现在 `.gitignore` 补了 `__pycache__/` 和 `*.pyc`。**审查结论用 `git add <具体文件>`。**
2. **`git commit -F -` 的 heredoc 里别写带引号转义的内容**，shell 会炸。消息复杂时写临时文件再 `-F <文件>`。
3. **`corral send` 的消息要用单引号包**——双引号里的反引号会被 shell 当命令替换执行掉（实际发生过，消息内容缺了一段）。

### 提醒为什么总是晚到（查清楚了）

不是 bug，是 corral 的**人工操作避让**。契约里 `send` 有两条拒绝规则：目标不是 `idle` → 退出码 7；**最近 30 秒内有人在窗口里操作过** → 退出码 8。`--after` 的第二步「规则同普通 send，遇到 7、8 隔几秒再试」。

主控在和人对话时一直是 `working`、人也一直在打字，所以提醒一直被避让，要等主控彻底闲下来才进得来。**这是必要的**（不避让的话送进来的文字会和人的草稿拼在一起）。**别把「提醒没来」当成「活没干完」**——直接 `corral status` 查。

### 下一步

用户回家后按 `n` 发 `T3`。流程照旧：`n` 发 → 主控干活 → `drover done T3` → `g` 放行 → `n` 发下一件。

**验收口径不变**：一律放行模式跑够 5 件、没有「没做完却判成做完了」，再上靶场。现在 **2/5**。

---

## 2026-09-21（深夜）—— 自举开跑了

**（已被上面那节接替。）** **D2 第 3 层自举正式开始了**：`MAIN_AGENT=drover/main` 已填，drover 从自己的队列里发任务给主控，主控照 corral-dispatch 拆活派 agent。**头两件（`T1` / `m11-single-check`、`T2` / `m12-ref-names`）已经走完整个循环并合并**。

**档位是「什么都不自动」**（用户 2026-09-21 定的）：`loop` **关着**，`TASK_GATE=1`。每一步都要人动手——按 `n` 发任务、跑 `drover done` 核对、按 `g` 放行。**不要擅自 `drover loop on`**，那是下一档。

### 第一件活跑完了：T1 → `m11-single-check`

`cmd_done` 原来跑两次验收命令（`check_done()` 一次、`criteria_report()` 又一次，drover 自己是 22 秒 × 2）。现在 `check_done` 返回 `(probs, rows)`，报告复用同一次结果，**`drover done` 快一半**。

流程按 corral-dispatch 完整走了一遍：拆任务 → 写任务文件 → Codex 常规档 → 主控审查（含独立植入自检）→ 合并 → 清 worktree 和分支 → 关 agent → 补收尾记号。**没做交叉审查**（路由 `core_rules` 只有 0.37，远低于 `m9` 的 0.9、`m10` 的并发 0.6；改动是纯粹「复用已算好的结果」，不引入新逻辑）。

**这件活验证了一个判断**：`m9`/`m10` 那种一轮轮返工不是常态，小活一轮就能过。**交叉审查要按 `core_rules` 的高低决定给不给，不要每件都给。**

### 看板上现在看得见什么（第一次真实运转）

三条门全都真在判了，而且前两件活的成果这时候才显出来：

- **门 2** 会显示「还没合进 main: `<分支名>`」——`m9` 之前这里永远是「不适用：没配 `BRANCH_GLOB`」。
- **门 3** 会显示「没跑过（无有效核对记录）」，跑过 `drover done` 之后变成「✓（Xm 前跑的）」——`m10` 之前只显示命令本身。

**一个容易误会的显示**：主控状态常常是「空闲」，但那恰恰是它最忙的时候（派完活等 dev agent，可能几十分钟）。这是 ROADMAP 记死的三个死路之一，所以 drover **绝不拿 idle 当完成信号**，依据只认收尾记号。

### 第二件活：T2 → `m12-ref-names`（2026-09-21）

修了一个**危险方向的误判**：`task_branches` 用 `splitlines()` + `strip()`，把末尾带 NBSP 的未合入分支裁成了同名的已合入分支，门 2 判过。主控实测复现：

```
git 眼里：    feature/merged / feature/merged\u{a0}（未合入）/ main
drover 眼里： 'feature/merged' / 'feature/merged'      ← 裁成同一个
门 2 → ✓ 过 | 2 个都已经是 main 的祖先：feature/merged、feature/merged
```

改法：`%(refname)` 完整 ref、只按 ASCII 换行切、不做 Unicode `strip()`、查询用完整 ref（顺带消解同名 tag 的歧义）。

**这次交叉审查给了「可以合并，0 条必须改」**——比 `m10` 那三轮利落得多。原因是任务文件里加了一条分级标准：

> **分级按「正常用会不会撞上」来定**：需要特意构造畸形输入才触发的，标「建议改」或「可以不改」，不要标「必须改」。

**这条以后每次派交叉审查都写上。**

### 两个主控自己犯的错，记下来

1. **写审查结论时用 `git add -A`，把 dev worktree 的 `bin/__pycache__/*.pyc`（81KB 二进制）提交进了分支**，交叉审查逮到的。`m10` 审查时踩过一次、手动清掉但没修根因。这次连同 `.gitignore` 一起补了（`__pycache__/`、`*.pyc`）。**审查结论用 `git add <具体文件>`，别用 `-A`。**
2. **`git commit -F -` 的 heredoc 里别写带引号和反斜杠转义的内容**，shell 会炸（`unmatched "`）。消息复杂时写到临时文件再 `-F <文件>`。

### 下一步

队列空了。往里加活、按 `n` 发下一件即可。**验收口径不变**：一律放行模式跑够 5 件、没有「没做完却判成做完了」，再上靶场。现在是 **2/5**。

队列里的活可以直接从下面欠账清单取（还剩 5 条）。

---

## 2026-09-21（深夜）

**（已被上面那节接替。）** 做完了 `m10-check-result`：**判据第 3 条的核对结果终于上看板了**。已合并进 main（本地，**未推送**），两个 worktree 清掉、两个 agent 关掉、收尾记号已补（现在**七条**）。四个套件全绿，工作区干净，没有 worktree、没有别的分支。

**drover 现在可以开跑自举了**（`MAIN_AGENT` 还空着，见最后一节）。

### 做了什么

第 3 条要跑人配的验收命令（drover 自己 22 秒），看板 30 秒刷一次不能跑，所以渲染一律 `do_check=False`、**永远显示「—」**。现在：跑了第 3 条的那一次把结果写进交接目录的 `.check-result`，看板只读它。

```
改之前：  — 3 验收命令过了 : `for t in criteria drover ...`
改之后：  ✓ 3 验收命令过了（3m 前跑的）: ...
```

设计在 `docs/ROADMAP.md`「判据第 3 条的结果怎么上看板」。要点：归 `bin/drover` 所有、原子写、**看板只读不写**（「看板不存自己的状态」这条不破例）；陈旧判定比 `task` / `main` sha / `cmd` 三样；**只喂显示，绝不参与判断**。

### 这件活的教训：审查越往后越细，该在什么时候收手

一次主控审查 + 一次交叉审查 + 三轮返工。**前三条是真问题，后两条已经在边界上打转**：

| 轮次 | 挖出什么 | 现实里会遇到吗 |
|---|---|---|
| 交叉审查 | `printf '\000'` 当验收命令就让**看板崩掉退出 1** | 会 |
| 交叉审查 | 写文件失败**挡住 `drover done`**，显示功能绑架了判据 | 会（磁盘满、权限） |
| 交叉审查 | 守线能被四种读法绕过（`io.open`、`os.open`、吞异常、子进程） | 测试有效性问题 |
| 复核 1 | 上面那条修复漏了 `UnicodeEncodeError` | 要特意构造孤立代理码 |
| 复核 2 | 上面那条的**测试**依赖 `PYTHONIOENCODING` | 纯测试问题 |

**用户在第 4 轮喊停**（「如果都是边界 case，就不要来回弄了，先跑起来再说」），第 5 轮定了撤掉那条测试、保留修复、记欠账。**这个判断是对的**：功能第一版提交就能用，后面全在修边界，而边界的收益一轮比一轮低。

**给以后的口径**：交叉审查挖出「正常用就会撞上」的问题（前三条）必须改；挖到「要特意构造畸形输入」时（后两条），先问一句「这条值不值一轮返工」，不要默认往下走。

### 三个把主控自己绊倒的坑（都记下来了）

1. **植入自检要把 `drover` 和 `drover-board` 两个文件一起复制到同一临时目录**——`task_bin_path()` 找的是 board 副本旁边的 `drover`，只复制一个会 `FileNotFoundError`，看起来像「测试红了」其实是没跑起来。
2. **植入代码里别写 `except Exception: pass`**——会把守线抛的 `AssertionError` 吞掉，造成「断言守不住」的假象。主控因此误判过一次，加探针才看清。
3. **`DROVER_BIN` / `DROVER_BOARD_BIN` 要传绝对路径**——测试内部会切 cwd，相对路径失效，跑出一堆假 failure。

还有一条和 agent 打交道的：**`corral send` 的消息内容要用单引号包**。用双引号时里面的反引号会被 shell 当命令替换执行掉（实际发生过：`command not found: 验收：true`，消息内容缺了一段）。

### 下一步：可以开跑自举了

D2 第 3 层。**只差两步**：填 `MAIN_AGENT=drover/main`、往队列加第一件活，然后 `drover loop on` + 起引擎进程（记 PID，别用 `pkill`）。

**填 `MAIN_AGENT` 等于自举正式开跑**——drover 会往主控（也就是读这份文件的你）的输入框送任务正文。一律放行模式跑够 5 件、没有「没做完却判成做完了」，再上靶场。

**三条门现在全都真在判了**：门 1 一直有效；门 2 `m9` 之后对 drover 自己生效（以前标「不适用」）；门 3 的结果现在也上屏了。

队列里的头几件活可以直接从欠账清单里取——ROADMAP 里 D2 第 3 层的原话就是「把 drover 剩下的活写进它自己的队列」。

---

## 2026-09-21（夜）

**（已被上面那节接替。）** 这一件是 `m9-drop-branch-glob`：**把「里程碑」这个概念从设计里整个删掉**，判据第 2 条改成只认 `base_sha`。已合并进 main（本地，**领先 `origin/main` 9 个提交，未推送**），两个 worktree 清掉、两个 agent 关掉、收尾记号已补（现在**六条**）。四个套件全绿，工作区干净，没有 worktree、没有别的分支。

drover 自己**仍旧没有开跑**（`MAIN_AGENT` 空着）。

### 判据第 2 条变了，`BRANCH_GLOB` 没有了

用户定的：**不存在「里程碑」这种东西**，那只是他对「某次功能完成」的叫法（可能自己打个 tag，**drover 不读 tag**，硬规矩第 3 条）。所以第 2 条认的是「这次任务建的分支」，依据是 `base_sha`——`m6` 其实已经这么做了，glob 只是套在外面的一层壳。

**`BRANCH_GLOB` 整个删掉了，连同空真防护。** 现在是：`for-each-ref refs/heads/` 枚举所有本地分支（排除 `main`），用 `merge-base --is-ancestor base_sha <分支>` 过滤出本次的，问它们合进 main 了没有。

**最大的收益：那个死锁解开了。** 守纪律的项目（合并后删分支）过滤完一个不剩，现在判**过**——不必再把整条标「不适用」。drover 自己刚验过，合并后跑判据：

```
✓ 1 main 前进了 : 3c2246a → cabc357
✓ 2 这次建的分支都合进去了 : 没有未合并的分支     ← 以前这里是「— 没配 BRANCH_GLOB」
✓ 依据 收尾记号 : cabc357 收尾: m9-...
```

**换机器 / 重新 clone 时，`.drover.conf` 里不要再填 `BRANCH_GLOB`**（`drover init` 也不再生成它）。老配置里残留那一行无害，解析器忽略未知键，不需要迁移。

**代价写在 ROADMAP「这条判据的已知限制」里**：本次分支若从 `base_sha` 之前的点建出会被当遗留分支排除，少了空集合保护挡着，**比以前更容易触发**，方向在危险那侧。人自己开的实验分支若包含 `base_sha` 则会挡住门 2（假阴性，看得见）。两条都是明知的代价。

### 这件活真正的价值在交叉审查，三次一次比一次深

一次主控审查 + 一次交叉审查 + 两轮复核，**全是真问题，没有一条是挑措辞**：

1. **交叉审查第 1 条**：枚举分支用的 `git()` 封装丢弃退出码和 stderr，**枚举失败会被当成「没有分支」**——空集合一翻转成「过」，这就成了危险方向的假阳性。旧版判「不过」时反而安全地卡住。三种复现，其中「非法 `branch.sort` → 128」不需要对象损坏。
2. **交叉审查第 2 条**：`git branch --list` 在 detached 的 cwd 下输出 `(HEAD detached at …)` / `(no branch)` 伪条目，代码只排除 `main`，伪条目交给 `merge-base` 得 128 → 门 2 永远不过。改用 `for-each-ref refs/heads/`。
3. **第 1 轮复核**：修法里 `"warning: ignoring broken ref" in stderr` **只匹配英文，而 git 会按 locale 翻译它**。实测 `zh_CN.UTF-8` → 「警告：忽略损坏的引用」、`fr_FR.UTF-8` → 「avertissement : réf cassé…」，非英文环境下退出码仍是 0、清单却不全，**假阳性从侧门溜回来**。修法：枚举子进程固定 `LC_ALL=C`（局部 `env`）。

**第 3 条是这次最该记住的**：本机 `LANG=en_US.UTF-8`，所以这个 bug **此刻根本不会触发**。靠「本机恰好是英文」保证判据正确，正是 ROADMAP 里反复记的那种「时机赶巧」。以后凡是解析 git 的**人类可读输出**（不是 `--porcelain` / `--format` 那种机器格式），都要想到 locale。

配套的测试写法也值得沿用：外层 `patch.dict` 设成中文/法文，**先用真实 git 断言警告确实被翻译了**（前提断言，防 locale 缺失导致假过），再断言诊断里是英文原文——反过来证明 `LC_ALL=C` 作用到了子进程。审查者那句「不要仅在测试外层统一成英文而掩盖生产缺口」是这条测试的由来。

> 代价：这条回归依赖本机装了 zh_CN / fr_FR 的 git 翻译目录。审查者判为**测试环境问题、不是生产回归**，没有目录的机器前提断言会失败（测试红、消息明确，失败方向安全）。不要为此加跳过或模拟逻辑。

### 下一步

还是 D2 第 3 层自举，**和昨天一样只差两步**：填 `MAIN_AGENT=drover/main`、往队列里加第一件活，然后 `drover loop on` + 起引擎进程（记下 PID，别用 `pkill`）。一律放行模式跑够 5 件、没有「没做完却判成做完了」，再上靶场。

**和昨天不同的一点**：门 2 现在对 drover 自己**真的生效了**（以前标「不适用」，等于自举全程不验这道门）。

用户的安排：**剩下的欠账等这件落地之后再排。**

---

## 2026-09-21（晚）

**（2026-09-21 夜已被上面那节接替。）** 今天最后干的一件事是**拿一件真活去验 corral-dispatch 新改的收尾流程**（用户当天的要求）。活和流程都完成了：`m8-detail-scroll` 已合并进 main（本地，**未推送**），两个 worktree 清掉、两个 agent 关掉、收尾记号已补（现在**五条**）。四个套件全绿，工作区干净，没有 worktree、没有别的分支。

drover 自己**仍旧没有开跑**（`MAIN_AGENT` 空着），配置现值和「换机器要重填」见下面 9-21 那一节，没有变化。

**corral-dispatch 的 SKILL.md 当天又改过一轮**（我报上去的四条它全采纳了），**派活前先重读**，要点见下面那一节。

### 今天做完的活：欠账 4（TUI 详情区滚动）

`detail_viewport(total, room, offset, pages)` 纯函数统一算夹限和翻页；`draw()` 每帧把项目身份 / 行数 / 可视高度 / 归一后的偏移写回进程内 `state`；`key_action()` 读它、保持纯函数。PgUp/PgDn 翻页，切项目归零，缩窗和内容变短都重新夹住。**偏移只活在内存里，刷新照旧等于重跑。**

ROADMAP D1 第 5 步第 3 小节那句「**还欠**：详情区不滚动」可以划掉了。

**这件活真正的教训不在功能上，在测试的有效性上**：三轮返工，**生产代码一个字节没改过**，全部问题都是「断言被别的机制顺带满足，守不住回归」。

- 第一轮（我抓的）：「切项目归零」的断言切到 `queue=None` 的短项目，`limit` 算出来是 0，**任何偏移都会被夹限压成 0**，断言分不出归零逻辑有没有生效。
- 第二轮（交叉审查抓的两条，我判定同类、全部采纳）：「内容缩短」用例同样被归零掩盖；翻页的「输入未变」断言被 PgDn/PgUp 一来一回抵消。

**用的办法是往实现里植入缺陷，看测试红不红**，不看测试数量。五次植入里第一轮就抓出一次假绿。这个办法以后审代码继续用。更干净的跑法是 dev agent 教的：`DROVER_BOARD_BIN=<临时副本> bash tests/drover-board.sh`，被审 worktree 一个字节不动。

### corral-dispatch 新流程实测结果：跑通了，反馈的四条上游已全修

5 处改动全部走到，**一次跑通、零卡壳**。第 7 节那五步按顺序无摩擦：两个 agent 都 `idle`/`attached=0` → 两个 worktree 都干净 → 分支是 main 的祖先 → `worktree remove` 不加 `--force` 两个都删掉、`branch -d` 删掉 → 关掉两个 agent。「第 5 步的扳机是第 4 步成功返回」和「关之前确认记录都落盘」这两条实际都用上了。

跑完把四条使用者视角的事实交给了 `corral/main`（只交事实、不提改法——上游的设计不归 drover 管）。**它四条全采纳改掉了**，还多补了一条。所以**派活前重读一遍 SKILL.md**，下面这些是新的：

- **第 6 节，复核前先把审查者的 worktree 推到新提交**：`git -C <审查 worktree> checkout <新 sha>`（仍 detached）。不推的话它 `git diff` 看得到对象、跑测试却还是旧代码。这是四条里唯一够得上「缺口」的一条，原先要主控自己想到。
- **第 6 节，审查任务文件放主仓库工作区**，派的时候给绝对路径，并写明它不在审查 worktree 里、只写这一个文件。
- **第 6 节，detached 的真正理由补全了**：同一个分支不能在两个 worktree 里同时 checkout，git 会拒绝，所以只能 detached（原先只写了「顺带防手滑提交」）。这条是上游自己补的，不是我报的。
- **第 1 节，残骸判据只看 `cwd` 在不在**，名字后缀不是判据（`--unique` 第一次开就补 `-1`）。原先那句「`-2`、`-3` 多半也是」删掉了。
- **第 7 节第 5 步末尾**：关掉 agent 之后，之前挂在它身上的 `--after` 提醒照样会送来，查到 `not_found` 忽略就行。

> **这些改动此刻还在 corral 工作区里没提交**（`git -C ~/Developer/personal_projs/corral status` 看得到 `M SKILL.md`）。技能是软链过去的，所以**未提交也已经生效**。

### 下一步

还是 D2 第 3 层自举，**和昨天写的一样，只差两步**：填 `MAIN_AGENT=drover/main`、往队列里加第一件活，然后 `drover loop on` + 起引擎进程（记下 PID，别用 `pkill`）。一律放行模式跑够 5 件、没有「没做完却判成做完了」，再上靶场。

欠账清单少了一条（欠账 4 做完了），剩下的见最底下那一节。

---

## 2026-09-21（下半天）

**这一节是给新开的主控看的,先读它。** 今天 drover 这边全部收尾干净:main 已推送、工作区干净、没有 worktree、没有分支、三条收尾记号齐、四个套件全绿。**drover 没有开跑**(`MAIN_AGENT` 空着),不会往任何人的输入框送东西。

### 新主控开场要注意的两件

1. **corral-dispatch 的技能可能已经改了,重新读一遍再派活。** 2026-09-21 下午跟 corral 主控讨论了「委派出去的 agent 堆积」这个问题,它给了一份 5 处文档的改动清单(SKILL.md 第 3、6、7 节和开头,加项目 AGENTS 模板),**还在等人拍板两件事**:
   - 「清完 worktree 顺手关 agent」算对「关 agent 先问用户」开的第二个例外(第一个是清 worktree 本身);
   - 取消跨任务复用 agent(复用收窄到同一个 worktree 内的返工/复核)。

   **这件事不归 drover 管,也不要再插手**——根子在 corral-dispatch(拆掉 drover 照样堆),而 drover 主控参与上游规矩设计会侵蚀「只能往下依赖」那条线。要提供的只有使用者视角的事实,判断由人和 corral 主控做。

   （**2026-09-21 晚已全部落地**：两件都拍板进了 SKILL.md，我又拿一件真活实测了一遍、报了四条事实、上游全采纳。见顶上那节。**这一节以下都是当天早些时候的历史记录。**）

2. **`AGENTS.md` 的派活规矩今天改了两条**(用户定的,提交 `4f587b5`):交叉审查用 detached worktree `../drover-worktrees/review-<分支>`;清掉某个 worktree 时把住在里面的 agent 一并关掉,其余仍旧用户说关才关。

   **两个保护有意先不补,等真实跑出来再说**(用户 2026-09-21 定的):
   - **`worktree remove` 之前没有「确认 agent 是 idle」这一步。** 实测过:起一个进程占着 worktree 的 cwd,`git worktree remove` **不加 `--force` 照样删成功**,进程还活着但 cwd 悬空。**git 在这件事上不兜底**,所以如果 agent 还在跑,这一步会把它的工作目录直接抽掉。
   - `attached` 非 0 时不关(人可能正接在那个窗格里看输出)。

   踩到了再补,别提前加。

### 今天做完的两件活

1. **`m6-branch-filter`** 修废弃分支死锁(欠账 1)。判据第 2 条现在只认包含 `base_sha` 的分支。交叉审查还挖出一条假阳性:`returncode == 0` 把 git 查询失败(128)和「确定不包含」(1)混成一类,查坏的分支被当遗留分支静默排除,同时有个已合入的兄弟分支时第 2 条会**误过**。已修。
2. **`m7-accounting`** 记账 + 路由行(待定 3、4 的落地)。ROADMAP「待定」四条现在全部有结论。

## 2026-09-21

**欠账 1（废弃分支死锁）修完了**，`m6-branch-filter` 已合并进 main（本地，**未推送**），worktree 和分支都清了，收尾记号已补。四个套件全绿。

判据第 2 条现在只认包含 `base_sha` 的分支（`git merge-base --is-ancestor`），和判据第 1 条同一个基准，没加配置项。交叉审查（Codex 重档）还挖出一条一轮时看不出来的**假阳性**：`returncode == 0` 把 git 查询失败（128）和「确定不包含」（1）混成一类，查坏的分支被当「遗留分支」静默排除，同时有个已合入的兄弟分支时第 2 条就误过。已修。设计和理由全在 `docs/ROADMAP.md` 判据第 2 条那一节。

```sh
# 一分钟自检
cd ~/Developer/personal_projs/drover
for t in criteria drover install drover-board; do bash tests/$t.sh >/dev/null 2>&1 && echo "✓ $t" || echo "✗ $t"; done
git log --grep '^收尾:' --oneline      # 现在应该有三条
git worktree list                      # 只有 main，没有残留 worktree
```

### 开跑自举之前必须先定这个（今天实测撞出来的）

> **（2026-09-21 夜作废：`BRANCH_GLOB` 已经整个删掉，下面三条路等于走了第 3 条的变体——分支删掉后「一个不剩」直接判过。见顶上那节。以下保留作历史。）**

**`BRANCH_GLOB` 对 drover 自己必须留空，填了第一件活就卡死。**

两条规矩正面撞车：

- **空真防护**：`BRANCH_GLOB` 非空却匹配到 0 个 → 第 2 条判不过（防的是模式写错、没建分支）。
- **corral-dispatch 收尾纪律**：合并后立刻 `git worktree remove` + `git branch -d`。

守纪律的项目，一件活做完分支就没了 → 匹配 0 个 → 第 2 条永远判不过。今天在本仓库实测就是这个结果（`m[0-9]*` → `ok=False 一个都没匹配到`）。

ROADMAP 里「改成问『活合进去了吗』，删不删分支都成立」那句，是在空真防护加进来**之前**写的，后者推翻了它的一半。**这一条还没改 ROADMAP，也没改代码——要人先拿主意**，三条路：

1. `BRANCH_GLOB` 留空，第 2 条对 drover 自己标「不适用」（最省事，但等于自举全程不验这道门）。
2. 承认「删分支」和「空真防护」不兼容，把空真防护的适用条件写清楚（哪些项目该填 `BRANCH_GLOB`、哪些不该）。
3. 改判据：分支不存在但区间里有合并提交，也算数。**这是设计变更，要问人。**

注意方向：填了 `BRANCH_GLOB` 导致的是**假阴性**（卡住，看得见），不是误推进。所以不紧急，但会让自举第一件活就停在门 2 上。

### `.drover.conf` 已经生成了（2026-09-21）

跑了 `python3 ./bin/drover init drover`：仓库里有了 `.drover.conf`（**已进 `.gitignore`**），`~/.drover/drover` 交接目录和 `~/.drover/projects` 登记都就绪。

**（2026-09-21 夜：`BRANCH_GLOB` 已删除，这一整段作废。）** **`BRANCH_GLOB` 留空，这是有意的，别去填**——drover 自己守「合并后删分支」的纪律，填了第 2 条必然判不过。理由写进 `docs/ROADMAP.md`「什么项目该填 `BRANCH_GLOB`」了。以后上靶场（分支合了还留着）那边才填。

`MAIN_AGENT` **还空着**，这是故意的：一填上 drover 就会往主控的输入框送任务，那等于自举正式开跑。**要开跑再填，填之前跟人确认。**

### 待定 3、4 定了并且做完了（`m7-accounting`）

**待定 3（记账记什么）**：耗时（`start`→`done`）、提交数、等放行时长（`done`→`go`）。**「返工轮数」放弃了**——drover 看不见主控和 dev agent 的往返（硬规矩第 4 条），按 ROADMAP 自己那句「记不出数就等于没记」。测试里有一条 `lacks '返工'` 守着措辞，不许把提交数说成返工轮数。

**待定 4（读不读「路由：」行）**：读，但降级处理。配套加了 `TASK_FILE_DIR`。找任务文件两条路：队列正文里 `任务文件：<路径>` 优先，否则在 `TASK_FILE_DIR` 里按标题前缀找、**挑含「路由：」行的那份**（自然跳过「交叉审查.md」这类同前缀的附属文件——这是审查时用真数据撞出来的）。

**「绝不参与任何判断」有两条测试守线**：带/不带路由行的 `view_model` 摘掉 `route` 必须完全相等；把 `task_route` 换成一碰就炸再跑 `loop_tick`，照样自己记 done。

已知边界（不是缺陷）：队列条目标题写长了，80 列窄屏下记账会被截掉大半；截断顺序是对的（先掉「等放行时长」），「当前这件活」那里有全的。

### `.drover.conf` 现在的值（本地文件，不进仓库）

```
HANDOFF_DIR=~/.drover/drover
MAIN_AGENT=                    ← 还空着，填上就等于自举开跑
BRANCH_GLOB=                   ← 2026-09-21 夜已删除这个配置项；残留此行无害，解析器忽略未知键
CHECK_CMD=for t in criteria drover install drover-board; do bash tests/$t.sh || exit 1; done
DONE_MARK=收尾
TASK_FILE_DIR=docs/任务
```

**换机器或重新 clone 要重跑 `drover init drover`，再把 `CHECK_CMD` 和 `TASK_FILE_DIR` 填回去**（`.drover.conf` 在 `.gitignore` 里，不跟着仓库走）。

门的现状：第 1 条（main 前进了）有效，第 2 条不适用（`BRANCH_GLOB` 空，有意的），第 3 条有效（跑四个套件，实测 20 秒，且套件挂掉时确实判不过）。依据（收尾记号）有效。

### 下一步还是 D2 第 3 层自举

一律放行模式（`TASK_GATE=1`），跑够 5 件、没有「没做完却判成做完了」，再上靶场。**现在只差两步**：填 `MAIN_AGENT=drover/main`，往队列里加第一件活，然后 `drover loop on` + 起引擎进程（记下 PID，别用 `pkill`）。

---

## 2026-09-20

**D1 全部做完了**（第 0–5 步）。四个套件全绿：`tests/criteria.sh`、`tests/drover.sh`、`tests/install.sh`、`tests/drover-board.sh`（10 块）。工作区干净，只有 `main` 分支。**远端 `origin` 2026-09-20 建好了**：`github.com/firegnu/drover`，公开，157 个提交已同步。

```sh
# 一分钟自检
cd ~/Developer/personal_projs/drover
for t in criteria drover install drover-board; do bash tests/$t.sh >/dev/null 2>&1 && echo "✓ $t" || echo "✗ $t"; done
git log --grep '^收尾:' --oneline      # 应该有两条
```

### 今天做完的四件

1. **收尾记号落地**——drover 终于判得出「这件活完了」。约定写在**项目自己的 `AGENTS.md`** 里（corral 侧只改了 `项目AGENTS模板.md`，提交 `649ca22`，**SKILL.md 一个字没动**）。drover 这边四道闸 + `DONE_MARK` 配置 + 检测器。owlet 的 `AGENTS.md` 已加并推送（`3035c55`）。
2. **引擎拆出来**——`drover loop` 独立进程，看板退回纯观察者。拆的时候揪出一个只有常驻进程才会暴露的 bug（`NOW` 不刷新 → 节流永远成立 → 查一次之后再也不查）。
3. **看板改 curses TUI**——`bin/drover-board` 1598 → 927 行，网页那一整层删光。派给 `drover/dev-tui` 做的，审了三轮。
4. **安装脚本 + launchd 模板**——派给 `drover/dev-install` 做的。**只写不跑**：真装、真挂 launchd 要人点头。

两份文档也写了：`docs/QUICKSTART.md`（纯步骤）、`docs/手册.md`（为什么这么设计 + 排查 + 止损）。

### 下一步：D2 第 3 层「自举」

drover 驱动自己的开发。**一律放行模式**（`TASK_GATE=1`，每件做完等人按 `g`），跑够 5 件任务、没有「没做完却判成做完了」，再上靶场 `~/Developer/personal_projs/drover-sandbox`。

**开跑之前先修下面第 1 笔欠账**，否则自举第一件就可能卡在已知 bug 上，污染「5 件零误判」这个验收信号。（**2026-09-21 已修完**，见顶上那节；这一节以下都是当天的历史记录。）

---

## 欠账（按要不要先修排）

1. ~~**废弃分支死锁**~~（**2026-09-21 修完**，`m6-branch-filter`，详见顶上那节）。`BRANCH_GLOB` 撞上一个**被遗弃、永不合并**的分支，门 2 就永远过不了，之后每件活全卡死；靶场上现成两个（`m4/parallel-scalable-generation`、`m4/planning`）。这不是危险方向的误判，是反过来——**做完了永远判不成做完**，而且看起来跟「主控还在干活」一模一样。
2. ~~**「做完的」栏记账整块**~~（**2026-09-21 做完**，`m7-accounting`）。
3. ~~**待定 4：读任务文件开头那行「路由：…」**~~（**2026-09-21 做完**，`m7-accounting`）。
4. ~~**TUI 详情区不滚动**~~（**2026-09-21 晚做完**，`m8-detail-scroll`，详见顶上那节）。
5. ~~**判据第 3 条在看板上只显示命令、不显示结果**~~（**2026-09-21 夜做完**，`m10-check-result`，详见顶上那节）。
6. ~~**`install.sh` 把安装 shell 的整条 PATH 写进 plist**~~（**2026-09-22 T14 / m23-install-path 已修复**）。服务 PATH 改为固定常用目录；无关 PATH 变化重装、临时 Python 不被保存两项回归通过。旧 plist 不一致仍人工处理，不自动迁移；未实际安装或启用服务。
7. ~~**分支名里的 Unicode 空白会被 `splitlines()` / `strip()` 改写**~~（**2026-09-21 做完**，`m12-ref-names`——**自举第二件活**）。`%(refname)` 取完整 ref、只按 ASCII 换行切分、不做 Unicode `strip()`、查询用完整 ref（顺带消解同名 tag 的歧义）。

8. ~~**`cmd_done` 判据通过时会跑两次验收命令**~~（**2026-09-21 深夜做完**，`m11-single-check`——**drover 自举的第一件活**）。`check_done()` 返回 `(probs, rows)`，`criteria_report(task, rows)` 复用同一次结果；`B.criteria` 的调用点 2 处减到 1 处，`drover done` 快一半。老的计数器 fixture 留着当回归探测器。
9. **强杀进程会留下 `.check-result.<pid>.tmp`，没有清扫机制，会积累。** 交叉审查实测：精确 kill 掉正在写的进程，旧 `.check-result` 完好，但 tmp 残留下来。看板不把它当核对记录，无害但会攒。不要贸然加清扫——会误删正在写的文件。
10. **两个 `drover done` 并发会写出两条 `done` 事件**（实测退出码 `[8, 8]`、事件 `start, done, done`）。来自既有的「先 fold 再 append、无锁」流程，**不是 `m10` 引入的**，`.check-result` 的写法既没解决也没加重它。
11. ~~**`drover done` 遇到任务正文含孤立代理码时 `print(line)` 会崩**~~（**2026-09-21 夜做完**，`m14-print-surrogate`——自举第四件活）。加 `print_text()` 只在输出时把 U+D800–U+DFFF 转义成 `\udxxx`，原始正文和判据理由一个字不改；只换三个真正承载正文/理由的出口。**连带项也了结了**：`m10` 那个 `except (OSError, UnicodeError)` 重新有测试守着，新写法用 `-I` 忽略环境变量、显式固定严格 UTF-8，四种 `PYTHONIOENCODING` 下都绿。
12. ~~**看板详情会把修好的分支名再次折叠**~~（**2026-09-21 夜做完**，`m13-detail-flatten`——自举第三件活）。压平改成 `re.sub(r"[\t\n\v\f\r\x1c-\x1f]+", " ", why)`：旧 `split()` 吞的 ASCII 控制字符照旧全吞，非 ASCII 逐码位原样保留。
13. **其它 `main` 查询仍用短名**（门 1、收尾记号范围、提交统计、`.check-result` 新鲜度、派发起点；`bin/drover-board` 多处 + `bin/drover:110`）。同名 tag 时 git 优先解析 tag。`m12` 把门 2 改成完整 ref 了，**其余没动**——既有边界，本次没制造也没扩大。要改就得把这些读点连同派发保存的起点一起核对，配同名 tag 回归。
14. ~~**`drover list` 仍会被标题里的孤立代理码打崩**~~（**2026-09-22 T10 / m19-list-surrogate 已修复**）。cmd_list四处任务文字输出复用print_text，进行中/待办/已完成/已放弃标题及放弃原因均覆盖；严格UTF-8定向回归与旧代码缺陷自证通过，原始数据不变。
15. **派出去之后才发现任务正文写错了，目前无解。** 进行中的任务，正文是派发那一刻冻进 `tasks.state` 的快照，`fold()` 读的是它——改 `queue.md` 无效，`drover edit` 只能动「队列里还没开始的」，`drover done` 不吃覆盖参数。只能 `drop` 重来。T3 那次是靠删掉整条「验收：」语法绕过去的，**不是解决了这个问题**。

## 悬着等人定的

- ~~待定 3、待定 4~~：**2026-09-21 都定了并做完了**，见顶上那节。ROADMAP「待定」一节现在四条全部有结论。
- **推送**：每次都要人点头（`AGENTS.md`「先问人」），推前照例扫 `gitleaks` / `trufflehog`。**2026-09-21 夜用户点头推过一次**（T3 / T5 两件活 + 删验收语法 + 文档，扫描器干净）。
  > 上一批（m6/m7/m8，13 个提交）**2026-09-21 夜已推送**，`17df7d2..daa421e`，两个扫描器都干净。当时新进公开仓库的还有 **3 处绝对路径** `/Users/firegnu/...`（在 `docs/任务/m8-detail-scroll 交叉审查.md` 里，给审查 agent 指路用的）——用户名本来就在远端 URL 里公开，判断为可推。**这次 m9 的两份任务文件里同样有绝对路径**，推之前心里有这条。
- **`~/.drover/board-notified.json`**：`drover/dev-tui` 测试疏忽漏出来的残渣，内容是合成仓库（`iota/main`）的条目。按「删东西先问人」留着没删。
- ~~两个 dev agent 关不关~~：**已关**（`drover/dev-tui`、`drover/dev-install`，2026-09-20 收工时用户定的）。明天要派活得重新 `corral start`，别忘了带免确认参数（`claude --dangerously-skip-permissions` / `codex --yolo`），见下面「派活的规矩」。

## 派活的规矩（这个仓库已经是 corral-dispatch 项目了）

`AGENTS.md`「开发方式（主控分派）」那节是今天加的：agent 名字 `drover/dev-`、任务文件放 `docs/任务/`、worktree 放 `../drover-worktrees/<分支>`、**本地合并、不自己推送**、收尾记号和 owlet 用同一套措辞。

> **远端有了，但推送规矩没变。** `origin` 指向公开仓库 `github.com/firegnu/drover`，推之前每次都要人点头（`AGENTS.md`「先问人」那一节）。派出去的 agent 一律只在自己分支上提交，不合并也不推。
>
> **公开仓库意味着写进去的东西都会公开。** 当天推之前扫过：`gitleaks` 和 `trufflehog` 全干净，没有绝对路径、没有邮箱。（**`.drover.conf` 当时并不在 `.gitignore` 里**——只是那会儿这个文件还不存在，所以没漏。2026-09-21 跑 `drover init` 时才补上，`drover init` 本来就会加这一行。）但文档里提到的 `owlet` 和 `jb-finetune` 是**私有仓库**——用户判断过：名字和工作流细节公开无妨，私有仓库本身访问不了。**往 `HANDOFF.md` / `ROADMAP.md` 里写那两个项目的细节时，心里有这条。**

今天派活时踩过的两个坑，别再踩：

- **照 SKILL.md 派活要通读整份再动手。** 按需跳读跳过了第 4 节「派出去」，结果漏了「被委派的 agent 一律免确认启动」（`claude --dangerously-skip-permissions` / `codex --yolo`），派出去的 Codex 两分钟就卡在权限框上。
- **别用 `pkill -f drover` 这类按名字批量杀进程**，主控和别的 agent 的命令行里都带着项目名和工作目录。要停就用起的时候记下的 PID。

## 绝对别碰

`AGENTS.md`「绝对不许碰」那张表照旧：真的 jb-finetune、`~/.review/`、`~/wt/`、herdr、`dev.herdsman.*` 的 launchd 任务、`~/.local/bin/` 里那六个老命令、`~/.config/review/`。**老 herdsman 此刻还在推一个真实项目。**

要真跑用靶场 `~/Developer/personal_projs/drover-sandbox`。
