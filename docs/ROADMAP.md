# 路线图

启动于 2026-09-15。只放「要改机制」的大事；小修小补照常走提交，不进这里。

每项都有**止损点**：到点没达到成功标准就停下、记下原因、回到原样。「尝试」的意思是允许失败，不允许半途挂着。

| 编号 | 事项 | 状态 | 前置 |
|---|---|---|---|
| R1 | 尝试剥离 herdr | 未开始 | 无 |
| R2 | 引入 RSI（流程的递归自我改进） | 未开始 | 无；R2 第一步建议先做，见文末「顺序」 |

---

## R1 尝试剥离 herdr

### 目标

herdsman 能在**没有 herdr 的机器上**跑完一个完整评审周期；herdr 降为可选的传输后端之一，而不是硬依赖。
不追求功能对等：先让评审方 / 规划者脱离 herdr，写手这一侧放到最后、做不好可以留在 herdr 上。

### 为什么

- **单点依赖**：`install.sh` 把 herdr 当必需依赖检查；herdr 不在，`request-review` 一开头就 exit 2。
- **已经踩过它的坑**，都要靠脚本兜底：`agent prompt` 进程还没起来时静默吞掉 prompt 却返回成功（09-06 复现）；`stalled` 误报；codex 杀掉了后台唤醒进程（后来用 setsid 修）。
- **协议本来就是文件**：request.md、findings.md（带 REVIEW-COMPLETE 哨兵）、responses、decision 全是文件。herdr 实际承担的只是「把 agent 拉起来、把话送进去、看它在不在干活」。

### 现状：herdr 用在哪

| 位置 | 用途 | 调用 |
|---|---|---|
| `request-review` 传输层（已集中在一段里） | 按 cwd 找评审方 / 规划者 | `agent list`、`pane get`、`agent get` |
| 同上 | 没有就开 pane 并拉起 agent | `pane split`、`pane process-info`、`agent start` |
| 同上 | 送 prompt，并以状态变成 working / blocked 为送达证据 | `agent prompt --wait --until` |
| 同上 | 轮询评审方状态、判断卡住 | `agent get` → `agent_status` |
| `request-review` 唤醒（REVIEW_WAKE） | 评审交付后叫醒写手 | `HERDR_PANE_ID`、`agent get`、`agent prompt` |
| `review-board` | 顶栏和面板的 agent 状态、正在做什么 | `agent list`、`agent get`、`agent read` |
| `review-board serve` | 健康检查、外层循环叫醒写手 | `agent list`、`agent get`、`agent prompt` |
| `review-task` | 循环停下时记下写手所在 pane（本身不调 herdr） | `HERDR_PANE_ID` |

### 要抽出来的接口

把上表收成六个操作，所有脚本只调这六个，后端各自实现：

1. **找**：给定 cwd 和角色，返回一个 agent 句柄或空。
2. **起**：在给定 cwd 拉起指定种类的 agent，返回句柄。
3. **送**：把一段 prompt 送给句柄，**必须有送达证据**，不能只看命令返回码。
4. **状态**：working / idle / blocked / 不存在。
5. **读**：最近若干行输出，只给看板显示用，允许不支持。
6. **自己是谁**：当前进程所在的句柄，给唤醒和外层循环用，允许不支持（不支持就退回前台等待，现在已有这条退路）。

### 候选后端

- **herdr**：现有行为原样搬进来，作为默认，保证零回归。
- **无头子进程**（评审方、规划者的首选）：直接跑 `claude -p` / `codex exec` 这类非交互模式。「状态」= 进程是否还活着加 findings 是否带哨兵；「送」= 进程启动参数，天然有送达证据。代价是人看不到评审方的实时终端，看板要改为读它的日志文件。
- **tmux**（写手侧的候选）：`send-keys` 送话、`capture-pane` 读输出。难点是**没有 agent 状态**，idle 只能靠输出静止多久来猜，会误判。

### 分步

1. **只抽不改**：把 herdr 调用收进一个传输模块，行为逐字节不变；现有测试全过。这一步单独有价值：以后换哪个后端都只改一处。
2. **评审方走无头后端**：`.review.conf` 里一个开关，默认仍是 herdr。在 jb-finetune 上开关打开跑真实周期。
3. **规划者走无头后端**：同上。
4. **写手侧评估**：看 tmux 能不能可靠地判断写手空闲。不可靠就结论为「写手留在 herdr 上」，不硬做。
5. **install.sh 把 herdr 从必需改为可选**：只有第 2、3 步都达到成功标准才做。

### 怎么算成功

- 一台没有 herdr 的机器，按 QUICKSTART 能跑完一个两轮的代码评审周期。
- 无头后端在 jb-finetune 连续跑 **10 个真实周期**：零次「送了没到」，评审方卡住能在看板上看出来。
- 评审质量不下降：用 R2 第一步的度量对比切换前后（这是建议 R2 第一步先做的原因）。

### 止损点

第 2 步开关打开后 **两周或 10 个周期**，任一条出现就停，开关拨回 herdr，第 1 步的抽离保留：

- 出现一次无法事后发现的「评审没跑却以为跑了」；
- 你为了看评审方在干什么，不得不回去开 herdr 窗格。

### 不做

- 不重新实现 herdr 的窗格管理和 UI。
- 不做 Windows / 远程机器支持。
- 写手侧不强求脱离。

---

## R2 引入 RSI

### 先对齐：这里的 RSI 指什么

**按「递归自我改进」（Recursive Self-Improvement）理解**：流程用自己产生的记录，发现自己的弱点，提出对自己规则的修改；修改本身也走同一套对抗评审和人的批准，生效后再被度量。一圈接一圈，所以叫递归。

改进的对象分两层，**先做第一层**：

1. **项目里的评审规则**：rubric、项目简报、风险图（`.review-map`）、AGENTS.md 里的评审段落、评审方和规划者的提示词。
2. **herdsman 自己的代码**：脚本、路由、看板。等第一层跑通、证明提案质量靠得住再说。

> 待确认：如果你说的 RSI 不是这个意思，或者想直接从第二层开始，改这一节再动手。

### 为什么

流程的自我评估机制实际上已经停转了，这正是手册第 12 部分预言的失效方式。jb-finetune 截至 2026-09-15：

| 记录 | 现状 |
|---|---|
| 归档的评审周期 | 143 个 |
| `precision.md` 里「误报 ?」没人填的行 | 267 行（填过的只有 1 行） |
| `escapes.md` 漏检记录 | 0 条，只有表头 |

也就是说，143 个周期里评审方有多少误报、漏了什么，**没有任何数据**。「报告连续干净」和「它什么都没抓到」从内部看一模一样。靠人手填表已经证明行不通。

已经有一个很小的雏形：风险图会根据未送审记录提出升级建议，看板上显示「风险图有 N 条建议，等你点头」。RSI 就是把这个模式推广到整套规则上。

### 闭环

```
记录（归档、裁决、timing、未送审、git 历史）
  → 评估（自动算误报、找漏检候选）
  → 复盘（每 N 个周期，由独立 agent 读记录，写修改提案）
  → 送审（提案作为 kind: plan 走对抗评审）
  → 人批准（看板上点头，和风险图建议一样）
  → 生效（提交规则文件）
  → 度量（下一批周期的误报 / 漏检 / 耗时，和生效前比）
  → 回到开头
```

### 分步

1. **自动评估**（先做，R1 也要用）
   - **误报**：从已有记录推出来，不再让人填。写手带证据拒绝、人裁决改判的，算误报；人维持原判的，不算。
   - **漏检候选**：扫 git 历史，找「后来的修复提交改的正是某次已评审提交碰过的行」，列为候选，写进 `escapes.md` 并标「待确认」。这是风险路由方案里一直没做的第 4 步。
   - 看板显示误报率和漏检候选数，按周期滑动。
2. **复盘提案**：每 10 个周期（或手动触发），让一个**不是当前评审方的模型**读这批周期的归档和第 1 步的度量，只输出提案文件：改哪个规则文件、改成什么、依据是哪几个周期、预期改善哪个指标。复盘者不直接改任何东西。
3. **提案送审和批准**：提案作为 kind: plan 走现有评审；评审通过后在看板上等人点头；点头才提交。
4. **前后对比**：每条生效的提案记下生效时的周期号，下一批 10 个周期后自动对比指标。变差的提案在看板上标出来，人决定是否回滚。
5. **第二层（herdsman 自身）**：第 1 到 4 步稳定运行、至少有 3 条提案被证明有效之后，才讨论要不要让复盘也提 herdsman 的代码修改。

### 护栏（不可被 RSI 修改的东西）

这些是手册第 12 部分认定的真正价值，复盘提案**不得触碰**，碰到的提案直接作废：

- evidence 门槛：每条 blocking 必须带 `file:line` 或复现命令。
- 稳定编号和范围冻结。
- 轮次上限、人裁决、止损点本身。
- 度量的定义。不允许「改规则」和「改怎么算好」出现在同一条提案里，防止自我刷分。

另外：

- 任何提案都要人点头才生效，没有自动合并。
- 复盘者和评审方用不同模型，避免自己给自己打分。
- 复盘有成本上限：一次复盘只读最近一批周期，不读全量归档。

### 怎么算成功

- 第 1 步上线后，`precision.md` 和 `escapes.md` 不再需要人手填，数字每个周期自动更新。
- 前 3 条生效的提案里，**至少 1 条**在对比期内让误报率或漏检数变好，且没有让评审耗时明显变长。

### 止损点

第 2 步开始后 **一个月或 3 次复盘**：

- 提案大多是空话或重复（你点头率低于三分之一），就停在第 1 步：保留自动度量，不做复盘提案。
- 发现提案绕过护栏（哪怕被拦下），先修护栏，再决定是否继续。

### 不做

- 不让任何 agent 在无人批准的情况下修改规则或代码。
- 不做在线学习、微调模型。
- 不追求全自动：人始终是批准者。

---

## 顺序

两件事互不阻塞，但建议：

1. **先做 R2 第 1 步（自动评估）**。它小、低风险，而且 R1 第 2 步需要它来回答「换了后端，评审质量有没有变差」。
2. 然后 **R1 第 1 步（只抽不改）**，同样零行为变化。
3. 之后两条线按各自的分步推进，谁先碰到止损点谁先停。
