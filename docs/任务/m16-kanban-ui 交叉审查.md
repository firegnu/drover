# T7 / m16-kanban-ui 独立交叉审查

2026-09-22，drover/main 交给 drover/dev-review-ui（新 Codex，重档 gpt-6-astra / xhigh）。
路由：重 / 交叉审查要（用户明确要求独立 Codex；按 corral-dispatch 重档审查）
你是被委派的独立审查 agent，不再委派。

## 背景与先读

开发 agent drover/dev-kanban-ui-1 在 m16-kanban-ui 完成 4cc7a91，主控已审查关键显示路径、测试 diff、AST 范围；主控看板与 drover 两套回归全过，重录 28 个真实 PTY 会话并检查长内容末尾可达。不是功能扩展，只优化 curses 显示。

先读 AGENTS.md，以及主仓库任务文件 /Users/firegnu/Developer/personal_projs/drover/docs/任务/m16-kanban-ui 字符网格视觉优化.md（含主控审查、验收限制），再读本文件和被审 diff。ROADMAP 只读本次 TUI 相关小节。设计参考 /Users/firegnu/Desktop/字符网格方案交付/grids.js 和 Drover 看板设计方案.dc.html；已批准任务边界优先。

## 工作区与写权限

- 你自己的 detached worktree：/Users/firegnu/Developer/personal_projs/drover-worktrees/review-m16-kanban-ui，固定提交 4cc7a91。
- 被审 diff：git diff e6cd857..4cc7a91；只读，不改代码、不提交、不切分支、不改开发 worktree。
- **唯一允许写的仓库文件就是本文件**：/Users/firegnu/Developer/personal_projs/drover/docs/任务/m16-kanban-ui 交叉审查.md。它在主仓库，不在你的 worktree；只追加意见。
- 可在 /tmp 创建独立合成数据、定向探针及证据；用假 corral，必要时测试基于临时副本。不要运行真实队列或 agent，不向 corral 发话。
- 主控已验证两套、开发四套已全过，不要重复全仓库审查/四套测试。运行 python3 tests/board-layout.py，按新发现做定向复现；确有证据需要时运行相关套件一次。不可为了多跑而跑。

## 重点审查（7 条）

1. 业务边界：只显示层；不改变任务状态、完成判据、依据、队列事件、档位、刷新频率、命令调用/超时、输入映射。共享数据模块的既有调用是否仍正确。
2. 双栏与窄屏：120×32 单项目约 70:45、120 多项目有效宽度降级单栏、80×24 单栏/项目标签；所有尺寸的顶部、消息、按键、滚动提示不遮正文，不崩溃/不越界。
3. 可达性：实际 draw + key_action 翻页，完整标题/指标、正文、长失败原因、agent 路径、全部待办与原有历史范围、消息均能看到；两栏长短交换、条目超过一页、窄小窗口也无永久漏行。
4. 视口：切项目重置、同项目内容变化只夹限、合法偏移保留、窗口大小/双栏切换后上下翻页正确；VM 不变，仅允许内存显示状态。
5. T6 消息：g 输出优先级和超时不变；缺收尾警告、拒绝原因、已放行/未放行仍可见，长消息全部可读；不要新增核对运行状态。
6. Unicode/样式：列宽、组合字符、NBSP/U+2028 合法名称不被空白折叠改写；界限处不会截丢内容。颜色之外保留文字/符号，无色/小终端可用。
7. 测试证据质量：旧断言只是为新布局更新，没有删除或弱化内容/安全断言；tests/drover.sh 的唯一两行历史断言是否等价。合成场景是否掩盖真实 VM 边界。不要把真实 PTY 文本当作已完成桌面字体/配色验收。

## 主控待验收项（勿擅自关闭）

- 桌面 Terminal 的读取被 Computer Use 安全限制拒绝；主控未绕过。真实 PTY 排版已验证，桌面字体/配色目视尚待补齐或用户明确调整要求。审查可给“代码可以合并，视觉门槛仍待补齐”，但不能宣称全任务验收通过。
- 开发记录称用户授权修改 tests/drover.sh 一条视觉断言，主控已询问用户核实，待答复。技术等价性照常审，不把该声称当已验证事实。

## 禁止事项

不合并、不推送、不做收尾、不改 HANDOFF、不操作真实 done/go/next/loop、不启停真实 agent。不安装、不改 PATH/launchd/~/.local/bin。不碰真实 jb-finetune、herdsman、~/.review、~/wt、herdr、dev.herdsman.*、~/.config/review。不能读 corral 源码或内部状态，契约仅看其 docs/CONTRACT.md。不要按项目名/路径批量杀进程；只清自己临时启动的确切 PID。

## 输出

在本文件末尾追加“## 审查意见”：一句代码结论（可以合并 / 改完再合并）；每条写必须改/建议改/可以不改、位置、可复现证据、影响、最小改法；逐条评议开发取舍，并列验证命令/结果与仍缺的视觉验收。已接受设计不得重新争论。
回复只写代码结论、各级条数及未完成验收项。命令都在前台跑完，全部做完后，回复最后一行写 DONE。


## 审查意见

2026-09-22，独立审查 `e6cd857..4cc7a91`，工作区 HEAD 为 `4cc7a91f610ca16aba83ace972646c09112f8896`（detached）。

**代码结论：改完再合并。必须改 1 条，建议改 0 条，可以不改 6 项（下表为开发取舍裁定）。** 桌面字体/配色目视验收及 `tests/drover.sh` 额外授权核实继续待补，不能据本次审查宣称全任务验收通过。

### 必须改

**R1：Tab 缩进的正常正文在双栏中被覆盖，完整命令永久缺字。**

- **位置**：`bin/drover-board:945-959`，尤其 `wrap_lines` 的 `size = cell(char)`；实际覆盖发生在 `draw:1041-1044`。`tests/board-layout.py:20-23` 用同一个 `B.width` 判断宽度且只记录调用，没有模拟 Tab 展开和后写覆盖，因此现有布局测试会绿。
- **可复现输入**：120×32 单项目，使用 `tests/board-demo.py` 的 working 数据，仅把正文换成下面一行（`\t` 表示一个真实 Tab）：

  ```python
  '\tpython3 -m pytest tests/checkout/test_coupon_validation.py --verbose'
  ```

  这可以是正常的 Markdown 缩进代码/Makefile 命令，既不是畸形 VM，也不要求特殊项目名。已用生产 `task_blocks` → `card_vm` 核实前导 Tab 原样进入正文；外部判据/缓存读取均替换成桩，未读取真实项目。
- **独立实际 curses 证据**：在同一个真实 PTY 中分别加载基线及被审版本，调用生产 `draw` 和 `key_action(PgDn)` 到底，从最终 curses 屏幕读取每一帧。基线完整显示上述命令；被审版本正文变成 `...test_coupon_validation.py --ver│os demo/dev-1 ...`，`--verbose` 中的字符被竖线/辅栏覆盖，翻到底也未出现完整命令。去掉 Tab 的两版对照均完整显示，四组均无 curses 异常。
- **原因与影响**：`cell('\t')` 算 1 列，curses 却从正文起点 x=3 展开到下一个制表位 x=8。主栏换行/裁切按错误宽度放行文本，随后辅栏和竖线覆盖尾部。正文中的命令参数会被改写或丢失，违反“完整正文可通过现有滚动查看”；同一普通输入旧版正常，属于本次分栏引入的回退。
- **最小改法**：仅在显示层把 ASCII Tab 转成确定的空格表示，并让换行和实际绘制使用同一份展示文本；不要把未经处理的 Tab 交给 curses。保留原 VM、任务正文和 NBSP/U+2028 等合法 Unicode，不使用通用 `split/strip` 折叠。补一条上述正文的真实 curses（或能准确模拟制表位及覆盖的屏幕）回归，断言完整参数确实留在最终屏幕并可通过翻页看到；不能只断言未绘制的逻辑行包含原文。
- **证据/重跑**：`/tmp/m16-independent-review-ujggc_js/pty-probe.py`、`pty-probe.json`、`pty-probe.ansi`、`pty-probe.stderr`。先执行 `python3 -B /tmp/m16-independent-review-ujggc_js/pty-probe.py` 重采新旧画面，再执行 `python3 -B /tmp/m16-independent-review-ujggc_js/vm-and-regression-probe.py`。后者在生产 VM 保留 Tab、基线及无 Tab 对照通过后，以 `REGRESSION: new dual-column draw overwrites normal Tab-indented command` 退出 1，失败原因就是上述缺陷。未改被审代码。

### 开发取舍逐条裁定

| 编号 / 分级 | 位置与取舍 | 证据、影响及最小改法 |
|---|---|---|
| A1 / 可以不改 | `bin/drover-board:989-1002,1031-1033`：固定标题/指标只留摘要，过长时在滚动区补全 | 指定布局测试实际 draw + PgDn 覆盖长标题末尾；补充探针核对最小窗口及缩放后的可达性。固定区不会被长标题撑高，符合任务补充约定，无需改回设计稿两行标题。 |
| A2 / 可以不改 | `bin/drover-board:925-935,1000-1006,1035-1046`：历史两行、两栏共用偏移，短栏停末页 | 120×32 单项目主栏 70 / 辅栏 45；多项目 120 列降单栏、80 列用标签。独立探针交换主辅栏长度，检查全部待办/历史标记、PgDn/PgUp、99/100/101/102 与 123/124 列断点；未发现正常可打印内容漏行或覆盖。保留现有历史范围，不加三条/五条上限，无需改。 |
| A3 / 可以不改 | `bin/drover-board:987-996,1047-1050`：按键栏在消息上方，长消息进入滚动内容 | failed / warning / long 布局场景检查拒绝原因、缺收尾警告及消息末尾；`run_drover`、`CHECK_TIMEOUT`、`tui` 均未改，g 优先级和期限保留。与设计稿交换两行位置有明确任务依据，无需新增核对状态或异步交互。 |
| A4 / 可以不改 | `bin/drover-board:945-959`：逐字符换行，英文词允许跨行 | `board-layout.py` 对中文、组合字符、普通双空格、NBSP、U+2028 逐字重组，检查没有折叠或拆走组合音标；原判据 ASCII 控制空白处理未改变。英文断词是已接受的保全文本取舍，无需引入词法换行；仅须修 R1 的 Tab 展示宽度。 |
| A5 / 可以不改 | `bin/drover-board:810-833,979-988,1007-1009,1047-1049`：指标提亮、小窗口继续降级，不新增状态/按键 | 颜色之外仍有状态文字及符号；指定测试验证 DIM 只留分隔线，60 组补充布局探针覆盖 20×5 起的窗口、项目身份重置、缩短后非零夹限、合法偏移保留和 VM 不变。AST 对比确认 `cell/width/trunc`、数据采集/VM、`detail_viewport`、输入分发、执行与引擎均未动。代码层可保留；实际终端无色表现、字形和配色仍归未完成的目视验收，不据桩测试补签。 |
| A6 / 可以不改 | `tests/drover-board.sh` 本次 diff；`tests/drover.sh:135-136`：仅迁移布局断言 | 历史标题和紧邻指标行仍分别检查标题、耗时、提交数、等放行、路由/推翻及无路由不出现档位；`zip(lines, lines[1:])` 确保相邻，不是任意两行拼凑。Unicode 用例清空历史只排除新增 bad 样式干扰，精确字符串断言仍在；视口行数/偏移随新增空行变动，合法偏移 3→3、等长项目重置及输入纯度检查保留。crew 增加的空行与 3→4 一致。技术等价，无需恢复旧坐标/单行历史；额外文件授权仍须主控核实。 |

### 验证范围与结果

- 按要求仅审本次 diff 及必要调用上下文，没有重新进行全仓库审查，没有重跑四套或主控已完成的两套回归。
- `python3 tests/board-layout.py`：退出 0，指定的七类状态、尺寸、Unicode、长内容标记及输入检查通过；其 Tab/最终覆盖盲点由 R1 单独指出。
- `python3 -B /tmp/m16-independent-review-ujggc_js/reach-probe.py`：退出 0。60 组布局、2624 帧，按实际绘制坐标检查屏幕覆盖；覆盖两栏长短交换、全部合成正文/判据/路径/待办/历史标记、上下翻页、20×5 边界、单双栏切换、同项目缩短夹限/合法偏移保留、等长项目切换归零、VM 及按键输入纯度。该探针验证可打印文本的布局，不冒充实际终端字体/配色验收。
- 上述 `pty-probe.py`：退出 0，独立记录 4 组新旧/有无 Tab 实际 curses 画面；随后 `vm-and-regression-probe.py`：因 R1 定向断言退出 1。证据脚本及画面均只在 `/tmp/m16-independent-review-ujggc_js/`。
- AST 对比：原函数仅 `init_colors`、`route_chip`、`detail_lines`、`draw` 变化；新增 `task_heading`、`task_metrics`、`detail_sections`、`wrap_lines`；没有删除函数。`bin/drover` 无 diff，共享数据模块已有接口未改。
- `git diff --check e6cd857..4cc7a91`：退出 0。
- 全部命令前台等待完成；没有真实队列、corral/agent 调用，没有安装、提交、切分支或修改代码。唯一仓库写入为向本文件追加本节。

### 仍未完成

1. 修复 R1 并做该缺陷的定向复验；不需要因此重做全仓库审查或四套回归。
2. 桌面终端字体、配色、实际字形目视验收，仍需主控补齐或用户明确调整门槛；本轮未尝试访问被限制的 Terminal，也未绕过限制。
3. `tests/drover.sh` 单条视觉断言的额外授权核实。这里只确认技术等价，不确认开发记录中声称的授权事实。


## 主控逐条裁定与第 1 轮返工（2026-09-22）

结论：**改完再合并**。R1 采纳为必须修复；A1–A6 全部接受，无额外功能或全仓库复查。

- **R1：采纳。** 主控先读探针，再独立前台重跑 pty-probe.py 和 vm-and-regression-probe.py：旧版带 Tab、旧版无 Tab、新版无 Tab 均完整；新版带 Tab 的 `--verbose` 实际为 `--ver│os...`，回归断言因目标缺陷退出 1，VM 保留 Tab 检查通过。确认为本次双栏引入的覆盖回退，不能用假屏幕 width 断言掩盖。
- **A1：同意可以不改。** 标题摘要保留固定区，完整标题/指标仍可翻页访问，符合任务。
- **A2：同意可以不改。** 历史两行、双栏共用偏移及短栏停末页已定向验证，保全内容优先于固定辅栏。
- **A3：同意可以不改。** 消息末行及完整消息进入滚动区保住 T6，交换底部两行有充分理由，不加新状态。
- **A4：同意可以不改，R1 除外。** 逐字符换行与英文断词是已接受取舍；Tab 需在展示副本中规范化，合法 Unicode 名称不能被折叠。
- **A5：同意可以不改。** 指标提亮和小窗口旧边界保留；无色/字形/配色的桌面目视不因此视为通过。
- **A6：技术等价性采纳。** 两行紧邻断言保留原内容检查，其他测试调整保持检查意图。但本主控会话仍未收到用户对开发所述单条 tests/drover.sh 修改授权的确认，继续待核实，不能把技术裁定写成用户确认。

### 原开发 agent 的返工范围

1. 只修 R1。只在显示层对 ASCII Tab 使用确定的空格表示，换行和绘制使用同一份展示副本。原 VM/正文不变，不能用 split/strip 折叠 Unicode；不改 cell/width/trunc 的既有全局契约，不碰业务/按键/命令逻辑。
2. 先添加有力的最终画面回归并在 4cc7a91 证明 RED：至少复现本文 Tab 正文在 120×32 双栏中参数被覆盖。优先真实 PTY/curses，若模拟必须准确处理制表位及后写覆盖。不能只断言中间文本含参数。实现后 GREEN，并复核无 Tab 对照、80×24 单栏和合法 Unicode 保真；原 VM 不变。
3. 只跑新增定向回归、python3 tests/board-layout.py，以及确实受影响的看板测试。**不重复四套或全仓库审查。** 在临时副本撤去修复验证新回归确能失败，可复用 RED 证据，禁止动真实队列/agent。
4. 在开发分支任务文件追加本次修复记录与命令、结果、取舍，并在 ROADMAP 相关小节补充 Tab 展示规则；只提交必要显示/测试/文档文件。不要把主仓库审查文件写回分支，不合并 main、不推送、不做收尾。
5. 保留桌面视觉待验收和授权待核实两项，不能自行补签。完成后由主控核对增量，将 review-m16-kanban-ui detached worktree 更新至新 SHA，再请同一审查者仅复核 R1。

主控已向用户询问：桌面目视由用户手动完成，还是明确免除该项、改以真实 PTY 与代码/回归为合并标准。答复前不将此门槛视为满足。不再尝试被 Computer Use 拒绝的 Terminal 访问，也不绕过。


## R1 增量复核任务（2026-09-22）

开发新提交 `3486cf5`，请原审查者 **仅看 `git diff 4cc7a91..3486cf5` 中 R1 修复和新增最终屏幕回归**，不重开 A1–A6，不重复全仓库审查/测试。
主控已读 diff、重跑 board-layout（含八组真实 PTY）全绿，并以旧代码副本验证新回归仅在两组双栏 Tab 场景失败。无业务逻辑或输入/宽度契约修改。开发记录及主控增量结论在主仓库任务文件末尾。

- 主控在发送复核前会把你的 review-m16-kanban-ui 更新到 `3486cf5`，仍为 detached；先核对 HEAD。
- 定向运行 `python3 -B tests/board-tab-pty.py`，必要时用自己的旧 R1 探针作最小调整复验。修复采用四空格表示后可能正常换行，必须以最终主栏完整内容是否可达判断，不能误将“单行显示”当需求。
- 核对首屏/后续页、120×32 双栏/80×24 单栏、有无 Tab 对照，以及原 VM/Unicode 保真和测试确能抓住覆盖；只在有新疑点时增加必要定向验证。
- 只向本主仓库审查文件追加“## R1 增量复核意见”，不改/提交/切分支；给出 R1 通过/仍需修复、证据、剩余条数。
- 桌面目视及 tests/drover.sh 单条断言授权仍待用户确认，不能自行签掉，不宣称整任务验收通过。命令都在前台跑完，全部做完后，回复最后一行写 DONE。


## R1 增量复核意见

2026-09-22，已先核对 HEAD：`3486cf5d53fb89a2b8be81d57a2e1c1d7d2c56e0`，仍为 detached，审查工作区干净。本轮仅复核 `4cc7a91..3486cf5` 的 R1 修复与最终屏幕回归。

**R1 通过，代码可以合并。剩余必须改 0 条、建议改 0 条；前轮 A1–A6 六项取舍沿用，不重开。** 此为代码结论，桌面目视与 `tests/drover.sh` 单条断言授权仍待用户确认，不代表整任务验收通过或可以跳过这两项直接合并。

- **修复正确且范围最小**：`bin/drover-board:952-953` 仅在展示副本把每个 ASCII Tab 替换为四个普通空格；同一份文本进入列宽计算、换行和最终绘制，消除了 curses 再按制表位展开而覆盖辅栏/竖线的原因。字符串替换不改原 VM，也不触及 NBSP、U+2028、组合字符或普通内部空格。AST 逐顶层语句对比确认仅 `wrap_lines` 变化，`cell/width/trunc`、数据/业务、输入、命令执行和刷新均未改。
- **最终画面回归有效**：`tests/board-tab-pty.py:24-66` 在真实 PTY 内调用生产 `draw`，等主栏、辅栏和分隔线全部写完才用 `instr` 读取最终屏幕，并通过生产 `key_action(PgDn)` 翻到底；不是检查中间字符串或 addstr 入参。断言在最终主栏按显示顺序连接正常换行后包含完整命令，符合“四空格可能导致正常换行”的要求，不强求同一物理行。后续页用例确实经过翻页；原 VM 全量相等、Unicode 原串（含内部双空格）也有断言。
- **本轮独立 GREEN**：前台执行 `TMPDIR=/tmp python3 -B tests/board-tab-pty.py`，退出 0。120×32 双栏 / 80×24 单栏 × 有 Tab / 无 Tab × 首屏 / 36 行占位后续页，八组全部通过，完整命令、Unicode 和 VM 保真均通过。
- **缺陷检出能力**：已阅读主控 `/tmp/m16-main-r1-red.log`，旧代码副本的结果确为六组对照通过、恰两组 120×32 双栏 Tab（首屏/后续页）缺完整 `--verbose` 命令而失败，没有 Unicode 或 VM 断言失败。结合本轮测试源码核对及独立 GREEN，足以确认回归可抓住 R1；未重复执行主控已完成的旧版副本验证。
- **接入与静态检查**：`tests/board-layout.py` 只追加新 PTY 回归入口，未删除或弱化旧断言；ROADMAP 只补 Tab 展示规则。`git diff --check 4cc7a91..3486cf5` 退出 0。本轮未重复 board-layout 全部场景、两套/四套回归、全仓库审查，也未重新使用要求整条命令同一物理行的旧探针。

**仍待用户确认**：桌面终端字体、配色及字形目视验收；`tests/drover.sh` 单条视觉断言的额外修改授权。两项均未补签，本轮未访问被限制的 Terminal、未绕过限制。

全部命令前台等到退出；未改代码、提交或切分支，未操作真实队列或 agent。唯一仓库写入为向本主仓库审查文件追加本节。


## 主控最终代码裁定（2026-09-22，3486cf5）

**采纳 R1 增量复核通过：代码审查通过，剩余必须改 0 条、建议改 0 条；整任务仍未满足合并条件。**

1. 修复范围：同意。仅 wrap_lines 展示副本规范化 ASCII Tab，与主控已核对的增量一致，无业务/宽度契约变更。
2. 最终屏幕回归：同意。八组真实 PTY 检查最终主栏和实际翻页，完整参数及 Unicode 可读、VM 不变；允许正常物理换行。
3. RED/GREEN 证据：同意。主控旧版副本两组目标失败、六组对照通过；主控及独立审查新版本八组通过，足以关闭 R1，无需重复测试。
4. 测试接入和文档：同意。只加入定向 PTY 入口和 Tab 展示规则，无旧断言弱化。A1–A6 保留前轮裁定，不重新讨论。
5. 两项未完成门槛：保持。当前主控会话未收到用户对桌面目视验收（或明确免除）及 tests/drover.sh 单条两行历史断言修改授权的明确确认。技术等价不代替授权核实；真实 PTY 不冒充桌面字形/配色目视。

本轮核对审查 agent idle、回复 DONE、detached 审查 HEAD=3486cf5，并读取书面复核意见；未运行任何测试。开发分支仍为3486cf5。不合并、不删分支/worktree、不关两个自开 agent、不补收尾空提交；保留现场等待用户答复。未操作真实 done/go/next，未推送，T4继续暂缓。


## 用户确认与合并放行（2026-09-22）

用户在主控会话明确答复：“1，允许 2. 你完成之后，我单独验收”。据此确认 tests/drover.sh 单条历史断言迁移获得授权；桌面字体/配色/字形目视调整为合并收尾后由用户单独验收，不再阻挡本地合并，也不记为已经通过。
代码 3486cf5 已经主控与独立 Codex 审查通过，R1 关闭，必须改/建议改均为 0。现在允许本地合并与清理、补收尾记号、更新交接；不推送，不操作真实 done/go/next，T4 继续暂缓。


## 本地合并与清理（2026-09-22）

已通过合并提交 e2a40a7 将被审代码 3486cf5 合入 main。仅任务文档出现重复追加冲突，保留主仓库已完整收录的开发完成/返工记录与全部主控审查、授权记录；生产代码、所有本次测试及 ROADMAP 与被审提交逐字一致，未重复测试。
开发与 detached 审查 worktree 均干净后无 force 删除；m16-kanban-ui 经祖先核对已合入 main，使用 branch -d 删除。两个对应自开 agent 在 idle、attached=0 时随工作目录清理一并关闭。drover 侧仅剩 drover/main；未动 corral/main、globalmesh/main。
本地交付已完成，随后补收尾空提交与 HANDOFF；桌面视觉由用户另行验收，尚未标为通过。未推送、未操作真实 done/go/next，T4继续暂缓。
