# T9：修正文档中已过时的验收命令说明

2026-09-22，drover/main 委派 drover/dev-check-docs（Codex，轻档 gpt-5.6-luna / medium）。
路由：轻 / 交叉审查不要（路由：tier轻概率1.0；cross_review不要，core_rules0.02）
你是被委派的开发agent，只做本文件，不再委派。用户已正式发出TASK T9，正文里“只加入待办”是入队时说明，不再阻止本次执行。

## 先读

AGENTS.md、HANDOFF.md、README.md；ROADMAP中“队列条目验收命令覆盖语法已删除”和CHECK_CMD唯一来源的说明；QUICKSTART加任务/配置段及手册配置/两份文件分工段。本任务只同步已有设计，不改ROADMAP。

## 工作区与允许文件

- 分支 `m18-check-docs`，worktree `/Users/firegnu/Developer/personal_projs/drover-worktrees/m18-check-docs`。
- 仅修改 `docs/QUICKSTART.md`、`docs/手册.md`、本任务文件的完成记录。
- 不装依赖。主仓库不写；HANDOFF由主控更新。

## 修改目标

1. QUICKSTART两处添加任务示例改成自然语言验收要求，如“检查导出列名和字段顺序”，说明正文可自由描述要求。
2. 两份文档删除正文可按任务覆盖CHECK_CMD的旧承诺，明确“验收命令仅来自项目配置CHECK_CMD，任务正文不执行命令”。
3. 明确CHECK_CMD为空不运行验收命令，第3条不适用；其它现有判据仍核对，不代表自动完成。
4. 保留正确的任务编号、正文自由书写、做完停说明。仅改相关几处；不要重排版、重写整份文档或顺带处理其它错误。

这是纯文档小任务，长队列正文只用于用户测试滚动，不代表需要复杂实现。

## 验证与交付

- 通读改动和邻近示例，搜索两份文档确认不残留同义“正文按任务覆盖验收命令”的承诺。
- 人工检查Markdown代码围栏闭合、列表/示例自洽，运行 `git diff --check`；核对diff只有上述允许文件。
- 不伪造RED、不写测试、不跑现有测试套件，不执行文档示例。
- 在本任务末尾追加完成记录：改了哪几处、为何符合现行行为、搜索/通读/diff检查结果。只提交这3个明确文件，不用git add -A。
- 不合并、不推送、不打收尾记号、不清worktree/分支；主控轻量审查后本地合并收尾，无独立交叉审查。

## 禁止

不改Python脚本、测试、配置、安装脚本或其它仓库文件；不新增功能/配置/检查器，不恢复正文验收语法、不改g语义。不动真实队列/tasks.state，不执行真实done/go/next/loop；不装软件、不切Python、不改PATH、不启动循环。T4暂缓。不得修改corral/corral-dispatch、读取其源码/内部状态，不碰AGENTS列出的受保护项目/服务。不按项目名批量杀进程，不开关任何agent。不恢复或删除T8兼容实验stash。

回复只写改动、验证结果、提交号。命令都在前台跑完，全部做完后，回复最后一行写 DONE。

## 完成记录

2026-09-22，Codex，在 `m18-check-docs` 分支完成。

- `docs/QUICKSTART.md` 的两个任务示例改为自然语言要求，并明确任务正文可自由描述要求；配置和任务说明改为验收命令仅来自项目配置 `CHECK_CMD`，任务正文不执行命令。
- `docs/手册.md` 的三道门、最弱配置、配置表和两份文件分工段同步现行行为：`CHECK_CMD` 为空不运行验收命令，第 3 条不适用；其它判据仍核对，但不代表自动完成；保留任务编号和「做完停」说明。
- 已通读两份文档的改动及邻近示例，搜索确认不残留正文覆盖 `CHECK_CMD` 的旧承诺；人工核对 Markdown 围栏、列表和示例自洽；`git diff --check` 通过，diff 仅包含上述两份文档和本完成记录。未运行测试套件，未执行文档示例。


## 主控审查（2026-09-22）

结论：通过，可本地合并。核对agent idle、attached=0，回复DONE；新增提交 `37c6ba6`，worktree干净。变更仅两份文档和任务记录，无代码、测试或配置改动。

通读改动及邻近示例，CHECK_CMD唯一来源、正文不执行、空值不运行且第3条不适用、其它判据和收尾依据仍保留，编号/做完停说明未丢失。两处示例改为自然语言，与现有行为一致；搜索无正文按任务覆盖命令的残留承诺，Markdown围栏/列表正常，`git diff --check main...m18-check-docs` 通过。文案有少量重复但准确，不为纯文档小任务扩大返工。

按路由及任务要求不做独立交叉审查、不运行测试或示例；不操作真实队列、不推送。T4暂缓，T8实验stash原样保留。


## 合并与收尾（2026-09-22）

已本地合并 `621027a`，收尾空提交 `d2b3978`。任务文件追加冲突保留完整开发记录和主控结论；两份实际文档与被审 `37c6ba6` 完全一致。确认worktree干净、分支已合并、agent idle且attached=0后，删除worktree、关闭自开的 `drover/dev-check-docs-1`（工作目录已删，一并关闭）、删除开发分支，再补收尾记号。

未推送，未操作真实done/go/next/loop，未运行测试或文档示例；T4继续暂缓，T8兼容实验stash保留。任务交用户按g核对并放行，主控不代操作。


## 用户放行（2026-09-22）

用户确认「我放行了，感觉还挺顺」。据用户反馈记录T9已放行，流程使用顺畅；主控未操作或另行查询真实队列。
