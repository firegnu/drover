# 任务：T7 按字符网格设计优化看板 UI（仅视觉）

2026-09-22，drover/main 交给 drover/dev-kanban-ui（Codex，常规档：gpt-6-astra / high）。
路由：常规 / 交叉审查要（路由：tier 拿不准，轻 0.01、常规 0.49、重 0.50；cross_review 不要；推翻：用户明确要求独立 Codex 交叉审查；设计已定且限显示层，常规开发、重档审查）

你是被委派的 agent：照本文件做，不要再开别的 agent。只用 Codex，不派 Claude。
T7 已由用户发出，当前真实队列处于进行中；队列正文末尾“当前只加入待办，不派发、不开始实现”是入队时的旧说明，已被本次正式派发取代。T4 继续暂缓。

## 先读

1. AGENTS.md、HANDOFF.md、README.md。HANDOFF 的 T6 待用户验收已过时，实时 list 显示 T6 已完成、T7 进行中。
2. docs/ROADMAP.md 的看板 TUI、任务正文和干活的 agent、记 done 折进 go；docs/QUICKSTART.md、docs/手册.md 的现有操作约定。
3. bin/drover-board 的显示宽度工具、card_vm/project_vm、detail_lines、detail_viewport、draw、key_action、tui、run_drover；tests/drover-board.sh 及被引用的测试。
4. 下列本地设计交付，尤其 grids.js 的 Z/A/B/C/D/E/F/W/T 网格和 HTML 排版规范；本任务范围优先于设计稿。
5. 本文件。

## 你在哪里干活

- worktree：/Users/firegnu/Developer/personal_projs/drover-worktrees/m16-kanban-ui，分支 m16-kanban-ui，从 main 建好。
- 无需安装依赖，只用 Python 标准库。
- 允许修改：bin/drover-board 的显示层、tests/drover-board.sh 及必要的 tests 下布局/合成视觉验收辅助文件、docs/ROADMAP.md、本任务文件。操作说明确有必要时仅同步本次视觉相关内容。
- 不改 bin/drover，不改判据、采集、队列、引擎逻辑；原则上保持现有 view_model 数据契约。需要调整函数接口时核对所有调用者，不顺手重构。
- 不改 HANDOFF.md（主控收尾），不动主仓库或别人 worktree。没有其它并行开发。

## 用户任务与范围

目标：按已审阅的 Claude Design 字符网格方案优化 drover 看板的排版和视觉层级。只改界面呈现，不增加业务逻辑、状态或操作；保持现有 curses TUI，不引入网页或第三方依赖。

设计来源：
- 在线整页：https://claude.ai/artifact/L77NSnNMqZDTZPipNVzRPK
- 本地交付：/Users/firegnu/Desktop/字符网格方案交付（Drover 看板设计方案.dc.html、TermGrid.dc.html、grids.js）。网页外围的说明卡片不是 TUI 实现目标。
- 主控已查看整页实际效果，整体视觉方向可采用；设计稿与以下范围约束冲突时，以本任务为准。

采用的排版：
- 单项目隐藏项目栏；多项目保留选择，窄屏紧凑呈现，沿用现有切换按键。
- 当前任务标题与指标分行；宽屏主内容和辅助信息双栏，窄屏单栏；状态栏、消息区、按键栏位置清楚。
- 当前任务正文、完成依据和判据、干活的 agent、待办、历史分区展示；历史标题和指标采用两行，多项目时也保持一致。
- 深色、有限颜色、粗体、反色和留白建立层级；灰色指标、耗时和提示适当提亮。状态同时有文字或符号，不能只靠颜色。
- 中文按显示列宽换行和对齐；长标题、长路径、长理由与空状态正常显示。真实数据含义优先，不能照抄设计样例中的不可能状态。

不可改变的边界：
- 不新增 h、Tab、?、e 等快捷键，不加历史模式、焦点切换、正文折叠、帮助面板；沿用现有按键和 PgUp/PgDn 翻页。
- 不增加实时核对计时、异步命令执行、输入锁定、新运行状态或刷新机制；现有刷新节奏、命令超时和调用行为不变。
- 判据、依据、队列解析、tasks.state、档位、循环、g 的核对/记账/放行语义均不变；写操作仍交给 bin/drover。看板不持久化显示状态。
- 不以固定栏高度或“只显示三条/五条”为由减少现有可查看内容。完整正文、长失败原因、待办及现有历史范围仍可通过现有滚动查看；必要时调整布局，不以新增交互解决。
- 保住 T6 的警告和拒绝原因可见性，不退回只显示输出首行；保住合法 Unicode 名称的显示，不用通用空白折叠改写内容。
- 不照搬设计稿 60×16 以下禁用界面的限制；小窗口继续合理降级、不崩溃、不越界。

完成标准：
- 用合成数据和假的 corral 验证，不操作真实队列的 done/go/next，不启动真实循环。视觉改动无需伪造 RED；若实际涉及行为修复，先给有效失败用例。
- 在真实终端核对 120×32、80×24，覆盖单项目/多项目、进行中/核对失败/等人工确认/空闲/空队列，另检查长内容、滚动末尾与窗口缩放。留截图或可复核的终端画面；HTML 设计稿截图、假屏幕测试不能代替实际 TUI 视觉验收。
- 做相关布局、中文列宽、滚动可达性和输入回归；保留已有断言，相关测试通过。主控对照设计逐屏审查，记录必要的 TUI 取舍。
- 实施时按项目规矩写任务文件、记录 ROADMAP 的视觉设计与取舍；开发和独立交叉审查暂时只用 Codex，不派 Claude。审查通过再本地合并、清理自己开的 worktree/分支及 agent、补收尾空提交、更新 HANDOFF；不推送。

## 实施补充

- 120×32 单项目以设计的约 70:45 主辅栏比例为参照；80×24 单栏。多项目栏扣除后的有效宽度决定是否双栏，不能为了双栏挤坏正文。
- 共用现有翻页按键和内存视口状态，允许显示层做必要的列宽、换行、偏移夹限计算。不引入新的交互模式、持久化状态、数据采集或任务状态；所有列的内容必须可达。
- 顶部可显示标题摘要，但完整长标题必须在可滚动内容中可达。辅助栏溢出时不得永久丢掉条目或原因。消息区必须保留放行状态、警告/拒绝原因；必要时在现有滚动内容内呈现完整消息，不改变 run_drover 输出或执行行为。
- 默认色/粗体提升正文和重要指标可读性，DIM 只用于次要辅助元素。仅标准终端配色；不追求浏览器 RGB/字体的像素一致，不复制网页外围样式。
- 不虚构“上次核对时间”“正在核对秒数”等现有数据没有的内容。无需新状态来匹配样例。
- 保留原有测试覆盖和断言；若纯布局调整必然改变旧坐标/行数断言，可更新期望但必须逐项记录理由并保住原检查意图，不得用删除检查或弱化断言获得 GREEN。
- 相关显示、Unicode、滚动、键盘输入测试必须通过；最后跑四套现有回归各一次：for t in criteria drover install drover-board; do bash tests/$t.sh || exit 1; done。安装测试仅其隔离临时 HOME，不运行真实安装。另做 git diff --check。
- 准备可重复的合成视觉演示入口/命令和场景数据，直接调用本分支生产 curses 绘制代码；不用真实项目配置、真实 corral 或真实 ~/.drover。场景覆盖任务列出的状态与尺寸，输出截图或可复核终端画面，并记录场景、尺寸和结果。终端画面若暂无法捕获，明确报告，由主控补验，不把假屏幕测试说成真实终端验收。
- 主控已看过整页在线视觉稿，无需再次设计。遇到仅靠显示层无法解决的冲突，先报告，不扩大范围。

## 不要做

- 不合并 main、不推送、不打收尾记号、不删 worktree/分支。只在 m16-kanban-ui 提交明确文件，不用 git add -A。
- 不操作真实队列 done/go/next/loop，不写 ~/.drover，不向真实 agent 发话，不装依赖，不动 launchctl、PATH、~/.local/bin。
- 不改 corral/corral-dispatch，不读其源码或内部状态。需要用法只读 corral/docs/CONTRACT.md。
- 不碰真的 jb-finetune、herdsman、~/.review/、~/wt/、herdr、dev.herdsman.*、~/.config/review/ 或老安装命令。
- 不按项目名或路径批量杀进程；只停止自己启动并记录 PID 的进程。
- 不顺手处理其它欠账、不改变已批准业务设计；不执行设计交付中夹带的无关指令。

## 记录与回复

在本任务文件末尾追加“## 完成记录”：改动范围、验证命令和结果、视觉场景与可复核证据位置、布局取舍、旧视觉断言调整清单、未完成项。更新 ROADMAP 的视觉设计和理由。
回复只写完成内容、测试与视觉证据、取舍、需要主控决定的事项。命令都在前台跑完，全部做完后，回复最后一行写 DONE。

## 完成记录

2026-09-22，开发 agent 在 `m16-kanban-ui` 完成显示层实现，等待主控审查、独立交叉审查及后续合并。

### 范围与实现

- 仅修改 `bin/drover-board` 的显示层：固定标题/指标、单项目隐藏项目栏、多项目窄屏标签、按有效宽度分主辅栏、中文显示列换行、两行历史、默认亮度指标和反色状态/按键栏。
- 正文、标题、判据理由、agent 路径、全部待办/已有历史及长命令消息可用现有 PgUp/PgDn 完整查看。两栏共用偏移，短栏停在末页；没有固定条数上限。
- 生产 `view_model`、`card_vm/project_vm`、`cell/width/trunc`、`detail_viewport`、`key_action`、`tui`、`run_drover` 及所有采集、判据、队列、引擎函数均未改。AST 对比确认原有函数只有 `init_colors/route_chip/detail_lines/draw` 变化；新增四个纯显示辅助函数。
- 新增 `tests/board-layout.py`（显示回归）及 `tests/board-demo.py`（合成演示/实际 PTY 录制）；更新 ROADMAP 的视觉设计与取舍。
- **额外文件授权**：最终回归发现 `tests/drover.sh:135` 要求历史标题/提交数/等放行时长同处一行。向用户说明任务原修改范围后，用户明确答复「允许，只更新这一条视觉断言」。仅将这一条改查标题及其紧邻指标行，全部内容检查保留；该文件其余内容未动。

### 验证与结果

这次属于纯视觉调整，没有伪造行为 RED。旧单行历史及固定坐标断言的失败作为布局迁移记录，不作为缺陷修复 RED。

- `python3 tests/board-layout.py`：通过。覆盖七类状态、单/多项目、120×32 / 80×24 / 160×32 / 60×16 / 40×10 / 20×6 / 12×3 / 1×1，中文/组合字符/长路径/Unicode 名称、两栏末尾标记、缩放、输入纯度、新快捷键仍无效。
- `bash tests/drover-board.sh`：定向回归通过（含原有 Unicode 精确断言、视口夹限、切换项目、刷新重排、真实 `tui` 输入分发、20 项 check-result 检查及新布局检查）。
- 最终完整命令：`for t in criteria drover install drover-board; do bash tests/$t.sh || exit 1; done`，退出 0、四套全绿。安装只运行测试自带的隔离临时 HOME（9 项），未真实安装。日志：`/tmp/m16-final-regression.log`。
- Python `compile()`、`bash -n tests/drover-board.sh tests/drover.sh`、`git diff --check`：通过。
- 所有命令均前台执行并等到退出。没有操作真实队列、真实 corral、真实循环或用户配置。

### 视觉场景与证据

可重复入口：

```sh
python3 tests/board-demo.py --scene working
python3 tests/board-demo.py --scene failed --multi
python3 tests/board-demo.py --scene long
python3 tests/board-demo.py --record /tmp/m16-visual-replay
```

交互演示仍使用生产 `draw` 和 `key_action`。g/n/p/a/l 在此合成演示中仅提示，不执行命令/编辑器；生产输入路径不变。入口不读真实项目配置、真实 corral 或 `~/.drover`。

本次证据目录：`/tmp/m16-visual-final/`。每个场景目录含 `terminal.ansi`（真实 PTY/curses 输出）、每帧 `.txt`（真实 curses 屏幕文本）、`.json`（尺寸/视口）及空 `stderr.txt`。**不是假屏幕生成图，也没有 HTML 截图代替 TUI。**

| 场景 | 单/多项目 × 120×32 / 80×24 | 核对结果 |
|---|---|---|
| working 进行中 | 4 组 | 单项目双栏 70:45；120 多项目、80 列单栏；标题指标分行 |
| failed 核对失败 | 4 组 | 未放行/拒绝原因在末行和完整消息区可见，不推导新任务状态 |
| waiting 等放行 | 4 组 | 已核对标识、等放行文字与既有 waiting 数据一致 |
| idle 空闲有待办 | 4 组 | 无任务正文/判据空壳，待办和历史上移 |
| empty 空队列 | 4 组 | 加任务提示、空待办和已有历史可读 |
| warning 人工确认警告 | 4 组 | 已放行与缺收尾记号警告同时保留 |
| long 长内容 | 4 组 | 完整标题/正文/判据/路径/消息、17 条待办、长历史原因及末尾均可达 |

共 28 个实际 PTY 会话、158 帧（含全部翻页、120×32→80×24→40×10→120×32 缩放，以及多项目切换）。复核系统 `libc.wcwidth` 后所有屏幕文本的实际显示列数与终端尺寸一致，全部会话 stderr 为空。macOS 对 U+2028 的实际列宽为 0，屏幕文本含该字符时不能用 Python 字符数来判越界；生产旧 Unicode 宽度工具和名称原文均保留。

**桌面截图未完成**：尝试捕获新开的合成验收 Terminal 窗口时，截图工具返回 `could not create image from window`；Computer Use 明确拒绝控制 `com.apple.Terminal`。随后停止 GUI 尝试，没有绕过限制。保留的 PTY 屏幕文本/ANSI 可复核，但不宣称已完成桌面字体、配色的目视验收；请主控用上面的演示入口补验。

### 布局取舍

- 固定标题只显示一行摘要；完整长标题/指标进入滚动区，避免固定区被长标题挤满。
- 主辅栏都可滚动，不采用设计稿的固定辅栏/三条五条上限；短栏停末页，所有内容可达。
- 保留末行消息区，按键栏在其上一行，与设计稿底部两行次序不同；守住 T6 原有提示路径及 40/80 列警告/拒绝原因断言。完整长消息在详情开头可滚动读取，刷新仍按原规则清消息。
- 逐字符换行，优先保留空格和 Unicode 名称；英文词可能跨行，不做通用空白折叠。
- 不增加核对计时、状态机、刷新动作或新按键。窄小窗口保留旧 20×5 最低边界，而非采用稿件 60×16 门槛。

### 旧视觉断言调整清单

1. 历史 T5/T7 的文字定位改为带状态符号的标题及其下一行指标；耗时、提交数、等放行、档位/推翻和无路由不出现档位等断言全留。52 列截断检查改查指标行，标题另验。
2. Unicode 判据精确测试的合成历史清空：历史放弃项现在也用 bad 色，原来按 bad 色提取判据会混入历史标题；仍逐字断言全部合法 Unicode 名称与 ASCII 控制字符处理。
3. 历史分区增加一空行，固定逻辑行数 25→26、12→13；六行页的末页偏移 19→20，缩短后 6→7，四行页末偏移 21→22，合法偏移 3→3 不变。纯 `detail_viewport` 原数值断言全部不动。
4. 有侧栏/无侧栏坐标例从 `(80,26)/(20,0)` 改为 `(120,23)/(80,1)`，对应新断点和边距；20 列越界冒烟保留，并由新布局套件额外覆盖 20×6 下翻与缩放。翻页后的任务04、末尾、切换归零、等长重排归零、输入不改 VM 等检查全部保留。
5. crew 增加区间空行，长度差 3→4，原姓名/状态/闲置时长/worktree 断言全部保留。
6. 经用户单独授权，`tests/drover.sh` 仅一条历史视觉断言改为紧邻两行查找；提交数、等放行及真实 g/go 调用/消息断言未删未弱化。

### 未完成项与主控事项

- 主控补做桌面终端字体/配色目视验收，对照设计逐屏审查上述取舍。
- 主控审查、独立交叉审查、合并、worktree/分支及 agent 清理、收尾空提交和 HANDOFF 更新均由主控负责。本开发分支不做这些操作，也不推送。


## 主控审查（2026-09-22，4cc7a91）

结论：显示结构、范围和相关回归暂通过，可交独立 Codex 审查；**尚不可合并**，桌面终端字体/配色目视验收待完成。

- agent 为 idle，回复以 DONE 结束，分支仅实现提交 4cc7a91，开发工作区干净。AST 对比确认旧函数只改 init_colors、route_chip、detail_lines、draw；新增四个纯显示辅助函数。bin/drover、业务数据模型、判据、队列、引擎、run_drover、key_action、tui 均未变。
- 主控前台重跑 bash tests/drover-board.sh、bash tests/drover.sh 均退出 0；后者包括 T6 真 g 的 40/80 列消息和受控慢 go 回归。git diff --check 通过。没有重复跑全仓库四套。
- 主控独立运行 python3 tests/board-demo.py --record /tmp/m16-main-review-visual，重录 28 个真实 PTY 会话、158 帧。逐屏检查单/多项目 120×32 与 80×24 的正文、核对失败、等放行、空闲/空队列、人工确认警告；自动核对全部 long 场景的标题/正文/判据/路径/历史/消息末尾和最后待办均实际出现在 curses 帧中，所有 stderr 为空。缩放与切项目画面有记录。证据位于 /tmp/m16-main-review-visual；相关 CLI 回归日志 /tmp/m16-main-review-drover.log。
- 同意：长标题固定摘要+正文完整版本、主辅栏共用翻页且短栏停末页、历史两行、正文和指标提亮、小窗口沿用旧下限、不虚构核对中状态。接受逐字符换行的英文断词；接受底部按键/消息两行与设计稿交换位置，T6 的反馈路径与完整消息可达性优先。旧纯视觉坐标/行数期望调整有清单，检查意图保留。
- tests/drover.sh 的唯一改动为历史显示断言由同一行改成紧邻两行，检查内容未减少，技术上与批准的两行历史相符。开发记录声称获得用户单独授权，主控已向用户核实，当前尚未收到答复；不能将该声称当作已核实事实。
- **视觉限制**：Computer Use 当前明确返回“Computer Use is not allowed to use the app 'com.apple.Terminal' for safety reasons.”，未绕过，也未用网页/假屏幕图冒充。真实 PTY 的排版和文字已核对，但桌面字体、配色和终端实际字形目视仍未完成。独立审查可继续，最终合并前须补齐或由用户明确调整该验收要求。
- 未操作真实 done/go/next/loop，未推送，T4 继续暂缓。独立审查意见返回后逐条裁定；只处理本次显示改动引入的问题。


## 主控审查续：独立审查裁定（2026-09-22）

独立审查 R1（Tab 正文在双栏被覆盖）经主控真实 PTY 复现成立，阻挡合并；其余 A1–A6 取舍全部采纳。逐条裁定和最小返工要求见主仓库 docs/任务/m16-kanban-ui 交叉审查.md 末尾。交回原开发 agent 只修 R1，定向 RED/GREEN，不重复全仓库测试。修复后先更新 detached 审查 worktree，再由原审查者仅复核本条。
桌面目视和单条 tests/drover.sh 断言授权仍待用户确认；本轮不合并、不清理、不打收尾记号，不操作真实队列，不推送。


## 第 1 轮返工完成记录：R1 Tab 正文覆盖

2026-09-22，按主仓库交叉审查末尾「主控逐条裁定与第 1 轮返工」只修 R1，A1–A6 不重开。

- **生产修复**：`wrap_lines` 在展示副本中将每个 ASCII Tab 替换为四个普通空格，再逐字符换行；后续 `draw` 使用该展示文本，不把 Tab 交给 curses 展开。生产 diff 仅两行，AST 对比确认只有 `wrap_lines` 改变。原 VM、正文、业务/按键/命令代码以及 `cell/width/trunc` 全局契约不变；未使用 `split/strip` 折叠 Unicode。
- **回归边界**：新增 `tests/board-tab-pty.py`，在真实 PTY/curses 中调用生产 `draw`，等主栏、辅栏、竖线全部画完，再从最终 curses 屏幕 `instr` 读取；用生产 PgDn 翻到底。断言完整 `python3 -m pytest tests/checkout/test_coupon_validation.py --verbose` 在最终画面可读，允许正常换行；不以中间逻辑行或 `addstr` 入参代替最终画面。另逐字检查 NBSP、U+2028、组合字符和内部双空格，并断言原 VM 不变。
- **矩阵**：120×32 双栏 / 80×24 单栏 × 有 Tab / 无 Tab × 正文首屏 / 36 行占位后的后续页，共八组。`tests/board-layout.py` 接入此 PTY 回归，使已有看板测试入口也会执行；未改已有断言。

验证命令和证据：

1. RED：在未修复的 `4cc7a91` 上先运行 `python3 -B tests/board-tab-pty.py`，退出 1。仅 120×32、Tab=True 的首屏和后续页两组报「最终屏幕缺少 … --verbose」，六组对照通过，Unicode 和 VM 保真断言均未失败。日志 `/tmp/m16-r1-red.log`。
2. GREEN：应用两行修复后，同命令退出 0，八组全部通过。日志 `/tmp/m16-r1-green.log`。
3. 定向布局回归：`python3 -B tests/board-layout.py` 退出 0，包含原布局/Unicode/输入检查及新增实际 PTY 回归。日志 `/tmp/m16-r1-layout.log`。
4. 撤修复自证：使用 Python `TemporaryDirectory` 创建生产脚本副本，只撤去上述两行；先断言副本逐字等于 `git show 4cc7a91:bin/drover-board`，再以 `DROVER_BOARD_BIN=<副本>` 运行新 PTY 回归。再次退出 1，仍仅两组双栏 Tab 缺完整命令、六组对照通过。日志 `/tmp/m16-r1-mutation.log`；工作树生产文件未撤回，临时副本随测试结束清理。
5. 对比 `4cc7a91` 的生产 AST，只有 `wrap_lines` 变化；三个涉及的 Python 文件 `compile()` 检查及 `git diff --check` 通过。

所有命令前台运行并等到退出；本轮未重跑四套或全仓库测试，未操作真实队列、corral、agent 或配置。没有修改主仓库交叉审查文件、`tests/drover.sh`，没有合并、推送或收尾。

**保留待办**：桌面字体/配色目视验收和主控会话中的 `tests/drover.sh` 单条断言额外授权核实，均仍待主控处理。本轮只确认 R1 的代码与定向回归，不补签上述两项，不宣称全任务验收通过。主控及原独立审查者后续只需复核 R1 增量。


## 主控 R1 增量核对（2026-09-22，3486cf5）

R1 主控通过，交原审查者仅复核本条；尚不等于整体验收可合并。
- 核对 4cc7a91..3486cf5：生产仅 wrap_lines 的展示副本 Tab→四空格，原 VM/Unicode/全局宽度契约/命令与按键均未改；新增最终 curses 画面回归并接入 board-layout，无旧断言删除。
- 主控前台运行 python3 -B tests/board-layout.py：退出 0，八组真实 PTY 与原布局检查通过；git diff --check 通过。
- 主控将 git show 4cc7a91:bin/drover-board 写入临时副本，以 DROVER_BOARD_BIN 跑新 board-tab-pty.py：退出 1，恰为两组 120×32 Tab 首屏/后续页缺完整参数，六组对照通过。证据 /tmp/m16-main-r1-red.log；未改工作树生产代码。
- 接受每个 Tab 在展示副本固定四空格的取舍；完整命令允许正常换行，因此复核旧探针时须按最终主栏可见字符重组，不能强求整条命令同一物理行。
- 不重跑全仓库测试。桌面目视及 tests/drover.sh 单条断言授权确认仍未收到用户答复，不自行补签。不合并、不操作真实队列、不推送，T4 暂缓。


## 主控最终代码裁定（2026-09-22）

被审代码 3486cf5 的 R1 通过独立增量复核；主控采纳全部结论，剩余必须改 0 条、建议改 0 条，A1–A6 取舍保留。代码与相关定向验证已完成，不再重复测试。
**暂不合并**：桌面目视验收或明确免除、tests/drover.sh 单条视觉断言修改授权两项，仍未收到用户明确确认。两 worktree 和两个自开 agent 保留；不收尾、不操作真实队列、不推送。最终交接及合并清理待门槛全部满足后进行。完整裁定见主仓库同目录交叉审查文件末尾。


## 用户确认与合并放行（2026-09-22）

用户在主控会话明确答复：“1，允许 2. 你完成之后，我单独验收”。据此确认 tests/drover.sh 单条历史断言迁移获得授权；桌面字体/配色/字形目视调整为合并收尾后由用户单独验收，不再阻挡本地合并，也不记为已经通过。
代码 3486cf5 已经主控与独立 Codex 审查通过，R1 关闭，必须改/建议改均为 0。现在允许本地合并与清理、补收尾记号、更新交接；不推送，不操作真实 done/go/next，T4 继续暂缓。


## 本地合并与清理（2026-09-22）

已通过合并提交 e2a40a7 将被审代码 3486cf5 合入 main。仅任务文档出现重复追加冲突，保留主仓库已完整收录的开发完成/返工记录与全部主控审查、授权记录；生产代码、所有本次测试及 ROADMAP 与被审提交逐字一致，未重复测试。
开发与 detached 审查 worktree 均干净后无 force 删除；m16-kanban-ui 经祖先核对已合入 main，使用 branch -d 删除。两个对应自开 agent 在 idle、attached=0 时随工作目录清理一并关闭。drover 侧仅剩 drover/main；未动 corral/main、globalmesh/main。
本地交付已完成，随后补收尾空提交与 HANDOFF；桌面视觉由用户另行验收，尚未标为通过。未推送、未操作真实 done/go/next，T4继续暂缓。
