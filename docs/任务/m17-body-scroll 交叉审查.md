# T8 任务正文限高与鼠标滚动：独立 Codex 交叉审查

2026-09-22，drover/main 委派新的 drover/dev-review-body（Codex，重档 gpt-6-astra/xhigh）。用户明确要求独立交叉审查，不再调用路由。你是独立只读审查 agent，不再委派。

## 最新范围（优先于分支历史记录）

用户已确认：直接采用 corral 同类原生 curses 滚轮方案，使用已有 `/opt/anaconda3/bin/python3` 支持运行时；不继续旧 ABI 的自制鼠标协议兼容。主仓库 `b4d4c23` 已记录，取代 `00a2f6a` 和旧 R1“默认Homebrew Python必须支持”的门槛。不要重开已裁定的运行时选择，不提出自写协议、安装或换PATH。旧环境可明确提示并退化为原有整页翻页；不要将其写成局部滚动达标。

## 先读与代码

- 自己 worktree 的 AGENTS.md、README.md；主仓库 `/Users/firegnu/Developer/personal_projs/drover/docs/任务/m17-body-scroll 任务正文限高与鼠标滚动.md` 末尾最终裁定及首版完成/主控审查记录。
- 主仓库 docs/ROADMAP.md 的 T8 两节及 docs/QUICKSTART.md 的运行说明。分支文档保留调查时点，不得据此误判最新授权。
- 审查 worktree `/Users/firegnu/Developer/personal_projs/drover-worktrees/review-m17-body-scroll`，detached `885d49f`。审查生产改动 `git diff 342b6a0..885d49f` 或 `git diff main...HEAD`；实际生产实现与 `6b914a7` 相同，885d49f仅调查。代码/测试只读，不修改、不提交、不切分支。
- 原开发worktree中的自写协议实验已完整另存stash，不属于被审代码，不读取/恢复它。不要操作任何stash、真实队列、agent或项目服务。

## 已完成验证及审查方式

主控已自行重跑看板/drover两套、10项局部视口测试、支持路径8个实际PTY/208帧、默认退化8PTY、支持路径Tab8组、32个演示/270帧。T6警告/拒绝与慢go通过；当帧命中、局部滚动外部像素不变、不采集/命令、任务切换与缩放已核对。上轮默认解码8个独立样本已证实旧ABI限制，该限制由用户接受运行范围解决。

以代码及测试充分性审查为主，可跑少量有针对性的验证；禁止重复全仓库或两套全量、32演示矩阵。需要验证滚轮支持时显式用 `/opt/anaconda3/bin/python3`；使用默认python验证明确退化只能算退化验证。只用合成VM/假corral，外部临时副本可做必要缺陷自证，不能改被审代码。

## 重点（不得扩大范围）

1. 正文限高/短正文自然展开，120×32和80×24普通标题消息条件下判据可见；长标题/消息未误入正文限高，长外层内容仍可达。
2. 鼠标矩形是当帧真实可见交集，局部/完全裁切、双栏/窄屏/多项目及resize竞态不误中其它区块。
3. wheel仅改变正文内存偏移，外层/选择/VM不变，tui直接重绘不collect/view_model/业务命令；点击/拖动/移动/横向/区域外不触发操作，普通键及PgUp/PgDn不回退。
4. `(repo, card.id)`身份、同任务刷新保持、任务/项目切换归零、正文变化和缩放夹限、极小可见区不跳过内容，正文两端可达。
5. T7 Tab显示副本、中文列宽、合法Unicode、完整命令及T6消息可见性，既有断言未削弱。
6. 原生mousemask/init/退出路径足够简单，能力退化有提示且内容可达；不要求旧curses局部滚动或新增通用框架。区分合成事件、真实PTY/curses、物理设备；后者按用户任务留给用户。
7. diff范围、RED/GREEN与缺陷植入记录可信，生产/业务边界未越界。未改tests/drover.sh的首版即被审内容，不把stash中未提交试验误算进来。

分级按“正常用会不会撞上”：必须改/建议改/可以不改。只有特意构造畸形输入才触发的通常建议改或可以不改，避免把无关防御开发重新引入。

## 唯一允许写入的文件

只追加本文件 `/Users/firegnu/Developer/personal_projs/drover/docs/任务/m17-body-scroll 交叉审查.md`（在主仓库，不在审查worktree）“## 审查意见”。先结论“可以合并/改完再合并”；逐项写级别、位置、问题、证据、最小改法；对高度预算、步长、部分裁切、原生运行时/退化、真实设备交用户等取舍明确表态。说明实际跑的定向检查；不要复述整段开发日志。

不改任何其它仓库文件、不提交、不切分支、不合并、不删worktree/分支、不启动或关闭agent、不推送、不操作真实done/go/next/loop。不改corral/corral-dispatch，不读源码或内部状态；用户指定的参考已由主控完成，本审查无需再读取corral代码。禁止碰AGENTS列出的真实项目和服务；不绕过Terminal安全拒绝，T4暂缓。

完成后只回复结论、必须改/建议改条数。命令都在前台跑完，全部做完后，回复最后一行写 DONE。

## 审查意见

结论：**可以合并。必须改 0 条，建议改 0 条。**

2026-09-22，独立只读审查 `342b6a0..885d49f`。按主仓库最终用户裁定及 ROADMAP 的 T8 最终运行范围判断：支持入口为现有 `/opt/anaconda3/bin/python3 ./bin/drover board`，旧 ABI 的 R1 随运行范围调整关闭，不重开兼容设计。以下“可以不改”是对实现与取舍的确认，不是待修缺陷。

### 逐项判断

| 级别 | 位置 | 问题 / 判断、证据与最小改法 |
| --- | --- | --- |
| 可以不改 | `bin/drover-board:878,999–1021` | 正文以独立占位插入，不靠文字识别区块；默认 `detail_sections` 仍保留完整逻辑文本。高度预算 `min(正文行数, max(1, min(h // 4, room - 7)))` 简单且符合 24 行最多 6 行、32 行最多 8 行；短正文自然展开。首屏定向检查通过。标题、消息和判据留在外层，长外层内容的原有翻页路径未被限高。接受普通标题/消息条件下留出核对信息、极小窗口及长外层内容仍需翻页的取舍；无需改。 |
| 可以不改 | `bin/drover-board:1022–1037,1150–1162,1203–1209` | 先固定正文占位，再按实际列起点与外层页计算交集；命中矩形只覆盖当帧正文和本栏横向空白，不含标题、提示行、分隔线或旁栏。上下部分裁切后用交集高度夹限并填入正文，完全不可见时矩形为空。既有坐标边界检查覆盖宽窄/多项目；本次定向重跑上/下裁切和 resize 竞态通过。接受部分裁切后第一条可见正文仍由局部偏移决定；无需改。 |
| 可以不改 | `bin/drover-board:1149–1162,1218–1230` | 只接受矩形内单独的上下滚轮位，点击、移动、拖动、横向及混合事件不会落入业务按键分支。每次 `min(3, 可见高度)` 防止一两行窗口跳过内容；局部动作只更新内存偏移并立即重绘，外层/选择/VM 不动。纯映射、采集/文件/命令哨兵及正文外字符断言有效，极小窗口逐行可达检查通过。普通键、PgUp/PgDn 和刷新分支保持原逻辑；无需改。 |
| 可以不改 | `bin/drover-board:992–997,1015–1034`；`tests/board-body-scroll.py` 身份/缩放用例 | `(repo, card.id)` 能区分等长任务和项目；同任务保留偏移，缩短仍溢出夹至非零末页，正文消失清状态，缩放重新换行及夹限。相关测试包含任务切换、项目切换/重排、30→6 与合法 3→3、末行可见，未发现漏内容或越界路径。这里保留的是显示行偏移，不增加文本锚点机制；无需改。 |
| 可以不改 | `bin/drover-board:1002–1015`；`tests/board-layout.py`、`tests/board-tab-pty.py`、`tests/drover.sh` | 继续复用既有显示副本的 Tab→四空格和 Unicode 列宽处理；长标题/消息在正文占位之外。旧测试只调整如何进入正文后部，完整命令、NBSP/U+2028、组合字符、双空格、原 VM 和各区块末尾断言仍在。`tests/drover.sh` 与基线逐字相同，T6 消息整理/命令期限未改；结合主控已完成的实际屏幕与 T6 验证，不需重复矩阵或补改。 |
| 可以不改 | `bin/drover-board:1088–1101,1121–1133,1187–1231,1348`；主仓库 `docs/QUICKSTART.md:116–126` | 原生能力检查、鼠标订阅与退出路径足够简单。缺双向能力时明确提示并恢复完整整页正文，不能称为局部滚动达标；此限制已获用户接受。补充真实 PTY 检查证实编辑返回后滚轮仍生效，q、SIGINT 和异常退出均经 `endwin` 关闭鼠标报告、恢复 ECHO/ICANON。无需自写协议或新增清理框架。物理鼠标/触控板及桌面字形仍交用户验收，本次未验、也不作为自动化已通过项。 |
| 可以不改 | 整体 diff；开发完成记录的 RED/GREEN、缺陷植入及最终裁定 | AST 比对确认生产仅改 `detail_sections/draw/key_action/tui`、新增 `init_mouse`；`bin/drover` 未改。`6b914a7..885d49f` 的生产及原回归文件无差异，诊断脚本不是默认测试入口，未计入另存的兼容实验。历史执行时序依据开发/主控记录；本次另用内存基线复现目标 RED、当前代码 GREEN，并证明滚轮落入采集的缺陷会被抓住，未把语法或 fixture 错误计作 RED。业务边界未越界；无需改。 |

### 本次实际验证

- 使用 `/opt/anaconda3/bin/python3 -B -` 内联执行以下 5 项现有定向测试，全部通过：`test_body_cap_keeps_criteria_on_first_screen`、`test_tui_wheel_only_redraws_body`、`test_partially_clipped_body_can_reach_both_ends_by_wheel`、`test_tui_ignores_nonwheel_errors_and_resize_race`、`test_tiny_body_scroll_does_not_skip_lines`。没有运行 10 项整组或两套全量。
- 同一内联检查只在内存加载 `git show 342b6a0:bin/drover-board`：首屏与真实 tui 分发两项产生 7 个预期断言失败（6 个尺寸/项目子场景及 1 个滚动断言），错误数 0。当前生产字符串的内存副本去掉 `body_scroll` 分支的 `continue` 后，原测试准确报 `3 != 1 : 滚轮不得重跑采集`。被审文件始终未改。
- 另做 3 个 80×24 原生 curses/PTY 生命周期定向场景：编辑返回再滚动并 q；getch 等待时 SIGINT；合成异常退出。均使用合成 VM、假采集及命令拒绝哨兵；编辑器调用替换为只检查 `isendwin()` 的函数，没有打开编辑器或写队列。首个场景的合成 SGR 上/下滚动确实经过生产 `tui/getmouse`，偏移 0→3→0，编辑返回后还能滚至 3；纯滚轮不增加采集次数。鼠标模式序列分别为 `hlhl`、`hl`、`hl`，最后均关闭，终端 ECHO/ICANON 恢复，stderr 为空，子进程退出 0。探针最初只匹配分开的 CSI，后改为接受 ncurses 实际输出的合并参数 `?1006;1000h/l` 后通过；该次探针断言失败不是产品缺陷。
- AST/字节范围核对、无落盘字节码的 `compile()`、`bash -n tests/drover-board.sh`、`git diff --check 342b6a0..885d49f` 均通过。没有重跑既有 8 PTY/Tab 矩阵、32 演示或全仓库回归，未重新验证旧 ABI 解码，也未把主控此前的验证写成本次执行。

所有命令前台等待完成，自起 PTY 子进程均已回收；未委派、未操作真实队列/agent/服务、未提交或切分支。本次只追加此审查文件，审查 worktree 保持干净。


## 主控逐条裁定（2026-09-22）

已核对审查agent idle、attached=0，回复 DONE，结论“可以合并，必须改0、建议改0”；审查worktree干净，仅本审查文件有预期追加。主控同意独立审查结论，按用户最终运行范围批准本地合并 `885d49f`（生产 `6b914a7`），不重复已过全量测试。

1. 正文占位与高度预算：接受。24/32行上限及短正文自然展开满足任务，长标题/消息仍属外层；无需修改。
2. 当帧命中和部分裁切：接受。交集坐标、完全不可见清矩形及尺寸竞态检查有证据；无需修改。
3. 纯输入与局部重绘：接受。可见高度约束步长避免漏行，坐标/事件过滤及直接continue守住采集和业务边界；无需修改。
4. 身份、刷新、正文变化与缩放：接受。等长身份切换与仍溢出夹限用例具有区分力；保留显示行偏移即可，不引入锚点机制。
5. T7/T6回归：接受。原Tab/Unicode/完整命令断言保留，tests/drover.sh及T6调用/期限未改，已有主控验证足够；无需重复矩阵。
6. 原生能力及生命周期：接受。独立补验编辑返回、q/SIGINT/异常的鼠标关闭和终端恢复通过；用户已接受支持运行时和旧环境退化，实际设备仍由用户验收，不补签。
7. 范围与证据：接受。AST/字节核对及有效RED/缺陷植入成立，诊断脚本与实验stash不混同。兼容实验stash按原SHA保留、不合并、不恢复、不删除。

无剩余必须改或建议改。合并时保留主仓库最新任务裁定、ROADMAP最终运行范围及QUICKSTART启动说明。所有禁止真实done/go/next/loop、禁止推送与T4暂缓约束继续有效。
