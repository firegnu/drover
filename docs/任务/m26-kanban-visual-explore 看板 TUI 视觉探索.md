# T17：探索 Kanban TUI 的视觉改进方向

2026-09-23，drover/main 拟交给 `drover/dev-kanban-visual`（Claude Code，常规档：`--model 'opus[1m]' --effort high`）。
路由：常规 / 交叉审查不要（路由：tier 拿不准；cross_review=不要；tier 按 corral-dispatch 默认选常规）。
你是被委派的探索 agent：照本文件做，不再委派别的 agent。

## 先读

1. `AGENTS.md`、`HANDOFF.md` 和 `README.md` 的看板用法。
2. `docs/ROADMAP.md` 的「看板」「字符网格视觉排版」「看板视觉层级整理」；已定设计仅作现状依据，研究建议不能直接改它。
3. `docs/任务/m20-board-layout 看板排版评估.md` 的截图补评和最终裁定、`docs/任务/m21-board-polish 看板视觉层级整理.md` 的目标和完成记录，分清哪些问题已经处理。
4. `bin/drover-board` 的 `init_colors()`、`draw()`、显示辅助函数，以及 `tests/board-demo.py`；最后读本文件。

## 工作区与边界

- 计划 worktree：`../drover-worktrees/m26-kanban-visual-explore`，分支 `m26-kanban-visual-explore`，由主控从 main 创建；无需安装依赖。
- 只在本任务文件末尾追加研究记录和完成记录，并在该分支提交。生产代码、测试、ROADMAP、配置及其他任务文件均只读；不合并、不推送、不打收尾记号。
- 本任务是视觉探索，**不实施正式改版**。不调整按键、任务判据、队列、刷新、滚动、消息语义或现有服务。若推荐方案需要改已定设计、引入第三方 TUI 库或改变 curses 架构，单列影响和成本交用户决定。
- 不操作真实 `drover next/done/go/loop`、queue.md 或 tasks.state；不读写真实项目数据，不改 corral/corral-dispatch、后台服务、PATH 或安装；不关闭其他 agent，不按项目名批量杀进程。

## 要研究的

用户的反馈是：现有 Kanban TUI 功能可用，但整体仍不够美观。请从**当前画面**寻找具体视觉原因，重点看信息主次、留白与密度、列宽与对齐、中文和长文本、颜色与状态强调、标题/判据/待办/历史的视觉权重，以及 120×32、80×24 和更窄屏幕的表现。不要只把上一轮 T11 的问题清单重写一遍。

可只读运行 `python3 tests/board-demo.py --record <临时目录>`，检查当前生产 `draw()` 在合成场景下的最终屏幕文本和 ANSI；临时示意或草图放仓库外。2026-09-22 用户提供的三张截图位置见 T11/T12 任务文件，可作**旧版历史对照**。它们早于 T12，不能冒充当前版本的真实终端截图。此前桌面 Terminal/Ghostty 的 Computer Use 访问被拒绝，不重试或换方式绕过；缺当前真实截图时如实标明视觉判断的证据限制。

## 交付

在本文件末尾追加「## Claude 视觉探索」：

1. 先用三到五条**具体观察**描述当前哪里显乱或显弱。每条标明依据是当前合成画面、旧截图还是代码推断；附画面位置或代码位置，不把推断写成已实测事实。
2. 给出两到三个有明显区别的视觉方向，用紧凑的字符布局草图或等效示意展示宽屏与窄屏的关键区域；说明字体无法由 curses 控制时还能通过什么改善。不要只换配色名称或堆装饰框。
3. 比较每个方向的可读性、信息完整性、实施范围、与 T7/T8/T12/T13 已定交互的兼容性、风险和验证方法；指出哪些只是显示层改动，哪些需用户另行决定设计变更。
4. 推荐一个可分步落地的方向，写出首个**最小实现任务**的范围、验收画面或可观察标准，以及需要用户提供当前终端截图/目视判断的部分。不要替用户批准实施。
5. 追加简短完成记录：看了什么、运行了什么、产物位置、限制和未做的事。文档检查运行 `git diff --check`；纯研究无需编造 RED/GREEN 或跑全仓库测试。

## 回复

简述最值得改的两个视觉问题、推荐方向、证据限制和提交号；命令都在前台跑完。全部完成后，回复最后一行写 DONE。
