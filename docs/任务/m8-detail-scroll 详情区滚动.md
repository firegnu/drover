# 任务：TUI 详情区能滚动

2026-09-21，drover/main 交给 drover/dev-detail-scroll（Codex，常规档：`-m gpt-6-astra -c 'model_reasoning_effort="high"'`）。
路由：常规 / 交叉审查要（路由：档=常规（0.93），交叉审查=不要；推翻：交叉审查一项主控推翻成「要」——**理由和代码无关**，这次在验证 corral-dispatch 技能新改的收尾流程，需要走到「dev + 审查两个 worktree 一起清」那条路。审查照常按真标准做，别因为理由特殊就放水）。
分工表里 TUI 属于 Claude Code（界面活），这次按用户当天的额度要求换成同强度的 Codex，不是路由结论变了。
你是被委派的 agent：照本文件做，不要再开别的 agent。

## 先读

1. `AGENTS.md`（「硬规矩」「技术约束」「测试」三节）
2. `docs/ROADMAP.md` D1 第 5 步第 3 小节（看板改 TUI 那段），特别是这两句不变量：
   - **「不存自己的状态，刷新等于重跑」**
   - **`key_action` 是纯函数、`draw()` 薄到只有排版没有判断**——这两层是特意拆开的，为的是喂键码就能验
3. `bin/drover-board`：`draw()`、`key_action()`、`tui()`、`detail_lines()`、`put()`、`width()` / `trunc()`
4. `tests/drover-board.sh` 里 `draw()` 和 `key_action()` 那两块（约 350–450 行），照它们现成的写法加测试
5. 本文件

## 你在哪里干活

- worktree：`../drover-worktrees/m8-detail-scroll`，分支 `m8-detail-scroll`（已从 main 建好）。只在这里改。
- 只动 `bin/drover-board` 和 `tests/drover-board.sh`。
- 纯 Python 标准库，没有依赖要装。
- 同时还有一个只读的交叉审查 agent 会看你这个分支的 diff，它在自己的 detached worktree 里，不会碰你的目录。除此之外没有别的 agent 在这个仓库干活。

## 要做的

详情区（`draw()` 里 `detail_lines()` 那一段）现在超出窗口高度就截断，末行写「… 还有 N 行，窗口再高些」。队列一长就不够用了。让它能滚。

### 1. 滚动本身

- 加翻页按键。建议 `PgDn` / `PgUp`（`curses.KEY_NPAGE` / `curses.KEY_PPAGE`）；要不要再加行级滚动或 Home/End 你定，但**别占用已经有含义的键**：`j` `k` `↑` `↓`（选项目）、`g` `n` `p` `a` `l` `r` `q`。
- 到顶、到底都要夹住，不许滚出空白。
- **切换选中的项目时偏移归零**——换了个项目还停在第 30 行是错的。
- 窗口变矮、或者内容变短（下一次刷新行数少了）之后，偏移要重新夹住，不能停在越界的位置。
- 末行那句提示跟着改：现在它说「窗口再高些」，有滚动之后应该告诉人**还能往下翻**（措辞你定）。顶部也被滚掉时最好有个对应的提示。

### 2. 偏移量放哪：这件活唯一需要你拿主意的地方

偏移量是**进程内的 UI 状态**，存在 `draw()` / `key_action()` 共用的那个 `state` 字典里，**绝不落盘**（硬约束：看板不存自己的状态，刷新等于重跑）。

真正的张力在这里，请正面处理，别绕开：

> `key_action` 是纯函数，可算新偏移要夹住就得知道「详情区一共几行」和「一屏放得下几行」——这两个数只有 `draw()` 在拿到窗口尺寸之后才知道。

几条路都想得到，各有代价：把夹住挪进 `draw()`（`draw` 就不再是「只有排版没有判断」了）；让 `draw()` 把上一次的行数/可视高度写回 `state`，`key_action` 读它（`draw` 有了副作用，且第一帧没有这个数）；`key_action` 只返回相对量、由 `tui()` 夹住（判断散进了那层最难测的代码）。

**你选哪条都可以，但要满足两条硬要求**：

- `key_action(k, pv, state)` 和 `draw(stdscr, vm, state)` 的**签名一个字不能改**（测试和别的调用方按位置传参）。`state` 字典里加键是可以的。
- 新加的滚动逻辑**必须能在不开 curses 的情况下断言**——和现在 `key_action` 一样喂参数就能验。如果你的方案把夹住放进了 `draw()`，那就把「夹住」本身拆成一个纯函数单独测。

把选择和被否掉的方案各一句话写进任务文件末尾的「## 实现时的取舍」，并在回复里列出来。

### 3. 不要做的

- 不要改 `detail_lines()` 产出的内容（要显示什么是 `view_model` / `detail_lines` 的事，这件活只管怎么显示）。
- 不要改那 21 个被 `bin/drover` 和另外三个套件依赖的数据函数签名。
- 不要加配置项，不要落盘任何状态，不要碰交接目录。
- 不要动 `REFRESH_MS` 的语义：没按键时自己刷一次，刷新照旧等于重跑。
- 不要按项目名或路径批量杀进程（`pkill -f drover` 这类）：主控和别的 agent 的进程命令行里都带着项目名和工作目录，一条命令能把它们全杀掉。停自己起的服务用起的时候记下的 PID，或者固定端口后 `lsof -ti:<端口>`。
- 不合并到 main，不推送。只在 `m8-detail-scroll` 上提交。

## 验收（先写测试，确认它因为功能没实现而失败，再实现）

先写测试、跑一遍看它**因为滚动还没实现**而红，再动实现。导入报错、假屏幕搭错了不算 RED。

- `key_action()`（或你拆出来的那个纯函数）：翻页键返回预期动作；到顶、到底夹住；换项目后偏移归零；已经有含义的那些键行为一个没变（现成断言别删）。
- `draw()`：沿用测试里那个「写出界就当场炸」的假屏幕，验滚到中间、滚到底、窗口变矮之后都不崩、不越界；窄屏（`rail = 0`）那条路也走一遍。
- 内容比窗口短的时候，翻页键不该把内容顶走。
- 中文宽度照旧走现成的 `width()` / `trunc()`，别自己算。
- 四个套件全要绿，**不是只跑改的那个**：
  ```sh
  for t in criteria drover install drover-board; do bash tests/$t.sh || exit 1; done
  ```
- 实测一次真界面：`python3 ./bin/drover-board`（本机已登记了 drover 自己这一个项目）。把窗口拉矮到详情区放不下，翻上翻下各一次，确认不花屏、不崩、提示行对得上。**只看不按写操作键**（`g` `n` `p` `a` `l` 会真的改队列），看完 `q` 退出。

## 记录要求

做完在本文件末尾追加「## 完成记录」（在你的分支里提交）：做了什么、测试命令和结果、遇到的问题、没做的事。上面第 2 节的取舍写成「## 实现时的取舍」。

## 回复

回复里只写：做完了哪些、测试结果、取舍各一句话、有没有要主控决定的事。命令都在前台跑完，全部做完后，回复最后一行写 DONE。

## 实现时的取舍

- 选择：`draw()` 每帧把项目身份、详情行数、可视高度和归一后的偏移写回进程内 `state`，`detail_viewport()` 纯函数统一计算夹限及翻页，`key_action()` 保持纯函数；代价是 `draw()` 有明确的 UI 状态副作用，但刷新、缩窗和内容缩短不会沿用越界偏移，首帧缺尺寸时翻页归零。
- 否掉：不把夹限判断直接埋进 `draw()`，避免只能靠画屏幕验证边界算法。
- 否掉：不让 `key_action()` 只返回相对量再由 `tui()` 算夹限，避免把算法散进 curses 事件循环。
- 项目按仓库路径识别，切项目和刷新重排后选中的项目改变都回顶；只加 PgUp/PgDn，溢出时固定留一行显示上下隐藏行数，所有偏移只在内存里。

## 完成记录

- 已完成：PgUp/PgDn 翻详情、上下夹限、切项目归零、窗口变化和内容缩短后重新夹限；保留原按键行为与超时刷新语义，翻页本身只重画，不采集数据。
- 修改范围：`bin/drover-board`、`tests/drover-board.sh`，另按本任务的记录要求追加本文件；`detail_lines()` 内容和既有函数签名不变，没有持久化 UI 状态。
- RED 1：先加按键测试，前台执行 `bash tests/drover-board.sh`，因 `AssertionError: PgDn 尚未滚动详情` 退出 1；实现纯视口计算与按键动作后同命令通过。
- RED 2：再加渲染、夹限和真实 TUI 分发的假屏幕测试，同命令因 `AssertionError: draw 尚未提供当帧详情视口` 退出 1；接通渲染和事件分发后通过。
- 回归：前台执行 `for t in criteria drover install drover-board; do bash tests/$t.sh || exit 1; done`，四套件全绿，退出 0（安装套件 9 项，使用原有隔离测试）。`git diff --check` 通过。
- 真 curses 验收：在 PTY 前台执行 `python3 ./bin/drover-board`，真实登记项目只读打开、PgDn/PgUp 后 `q` 正常退出；当前 drover 未建 `queue.md`，详情只有两行，无法触发溢出。
- 长内容验收：为不改真实交接目录，使用临时合成 git 仓库、20 条中文任务、假的 corral 和 `--projects` 清单运行同一真实 curses 程序；从 80×12（首屏 `PgUp↑0 PgDn↓17`）缩至 80×6，翻到底为 `PgUp↑23 PgDn↓0`，再缩至 20×6 验无侧栏；向上翻回为 `PgUp↑21 PgDn↓2`，输出及截断正常，`q` 退出 0，临时数据随验收脚本退出清理。
- 验收限制：桌面工具拒绝操作 Terminal（`Computer Use is not allowed to use the app 'com.apple.Terminal' for safety reasons`），因此使用工具提供的 PTY 验真实 curses 按键、缩窗及输出，没有做桌面窗口的像素目视验收；越界由假屏幕严格断言补充覆盖。
- 未做：未改设计文档、安装、启用引擎、触碰真实交接目录、合并或推送；真界面没有按写操作键。没有新增设计事项需要主控决定，以上验收替代方式请主控审查时留意。

### 主控审查返工：切项目归零测试

- 补上两个详情均为 25 行、仓库身份不同的项目：A 翻到偏移 6 后切换到 B，必须归零；另验刷新重排后选中 B 的同一行为，覆盖有侧栏与 20 列窄屏。原有断言一条未删。
- 变异自证：临时把 `draw()` 的项目身份判断替换为无条件沿用 `detail_offset`，前台执行 `bash tests/drover-board.sh`，明确在 `AssertionError: 等长项目切换后偏移必须归零` 处退出 1；随后在 `finally` 中逐字节还原实现并断言还原成功，生产代码没有最终改动。
- 还原后前台执行 `for t in criteria drover install drover-board; do bash tests/$t.sh || exit 1; done`，四个套件全绿、退出 0；`git diff --check` 通过。本次只新增测试与完成记录，原实现和取舍不变，无新增待决事项。
