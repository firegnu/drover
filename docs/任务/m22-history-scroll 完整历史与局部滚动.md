# T13：展示完整任务历史，支持历史区鼠标局部滚动

2026-09-22，drover/main 委派 drover/dev-history-scroll（Codex，常规档 gpt-6-astra / high）。
路由：常规 / 交叉审查不要（路由：tier拿不准，常规0.86、重0.14；cross_review不要，最高core_rules0.13；主控按单模块局部交互选择常规档）。
你是被委派的agent，照任务做，不再委派。已收到正式TASK T13，本轮开始执行；“入队不自动执行”描述的是此前阶段。

## 先读

AGENTS.md、HANDOFF.md、README.md；ROADMAP的T7/T8/T12显示与交互取舍；`docs/任务/m21-board-polish 看板视觉层级整理.md`主控裁定；bin/drover-board的task_view/queue_vm/detail_sections/draw/key_action/tui及相关测试；本文件。
用户附件 `/Users/firegnu/Desktop/SCR-20260922-rhnj.png` 可用view_image读取：统计完成11/放弃1但仅显示最近5条。仅为用户提供的单项目空闲截图，不复制入git，不扩大成设备验收。

## 工作区与范围

- worktree `/Users/firegnu/Developer/personal_projs/drover-worktrees/m22-history-scroll`，分支 `m22-history-scroll`，从main建立，不安装依赖。
- 生产仅改bin/drover-board直接相关的历史显示数据、布局、鼠标命中与纯重绘分发。沿用共享函数默认契约；不因新视口重构整个显示系统或引入通用框架。
- 测试优先复用现有board-demo、board-layout、board-body-scroll/pty、board-tab-pty及合成队列基础，必要时新增聚焦历史入口；不改无关测试，不删减旧行为断言。
- 文档可改ROADMAP对应设计/取舍及本文件完成记录；不改HANDOFF、主仓库、其它worktree。任务推进/判据/事件解析、bin/drover、命令调用/超时、刷新频率不改。

## 先复现与测量，再完成交互

原缺陷在task_view的finished生成处：过滤非doing且非awaiting任务后用[-5:]截断；计数却基于全部任务。先以至少12条混合历史从合成tasks.state走真实task_view/queue_vm，给出更早记录缺失的有效RED。

性能测量应在本轮前段完成：取消截取后的每条历史会调用task_commits和task_route，不要等所有UI做完才发现刷新明显变慢。用临时合成Git仓库和真实有效起止提交，至少比较5条与数百条（建议300条）的task_view耗时、git调用数；TASK_FILE_DIR含合成任务文件时也核对读路由成本。不能把a=b或空SHA、全stub git的结果当作真实查询性能。

若逐条统计造成明显刷新拖慢，按用户要求先提交调查/当前进度并报告：基线/取消限制的样本规模、运行时、次数/耗时和最小解决建议。此时停在需裁定处，不自行加通用缓存、虚拟列表、异步/后台架构，不恢复条数上限，不以“以后优化”略过。性能允许时继续下面实施。

## 已批准的数据与交互设计

1. **完整历史。** 取消最近5条限制，不改成其它隐形上限；保留现有顺序、计数以及当前/等放行任务的排除规则，不按编号或新时间规则重排。保留标题、已有记账、路由和完整放弃原因，不删记录、不归档、不改原字段。
2. **空间分配。** 沿用当前单/多项目、双栏/单栏阈值。历史放在原位置，短列表自然展开，长列表在历史区域内滚动；空闲时用待办以下剩余空间，有任务时不挤走正文/判据/agent/待办。历史起点在外层首屏之外时，允许原PgUp/PgDn先到该区域；局部视口不能造成永远无法到达历史。可见空间极小时合理降级，不越界、不少内容。
3. **局部滚轮。** 光标在当帧真正可见的历史内容行及该栏横向空白内时，仅改变历史位置，无需点击。标题、位置提示、分隔线、其它区域不能被误命中；完全离屏时不命中。正文滚轮继续只滚正文，两区互不抢事件、不动外层偏移或其它内容。缩放夹在绘制/输入之间时沿用旧坐标丢弃规则。步长不超过当前可见行数，防止小窗口跳过未显示内容。
4. **提示及完整可达。** 溢出显示“1–16 / 38 行 · 滚轮”一类提示，按实际栏宽换行后的显示行统计。最早记录、长标题、长放弃原因末尾可达；不是固定五条或只展示摘要。PgUp/PgDn仍保持原整页作用，不新增焦点、折叠、历史模式、搜索、筛选或快捷键。
5. **阅读位置。** 切换项目归零；同项目刷新保持合法位置。在顶部时跟随新历史，正在看旧记录时尽量保持原首个可见任务和该记录内位置，不能只保存全局行号。使用已有任务id与局部行位置等最小内存信息，不写状态文件。原记录不再存在时夹限；缩放/文本重排后位置合法、首尾完整可达。正文原有身份/偏移规则保持不变。
6. **纯重绘。** 历史与正文滚轮都只更新必要内存视口然后draw，不调用collect/view_model/git/corral、核对或业务命令。周期刷新和r/缩放刷新沿用原契约，不增加采集和计时器。

使用用户已确认的现有 `/opt/anaconda3/bin/python3` 与原生curses滚轮能力；不重新开启旧curses协议兼容，不改Python/PATH/启动器。缺少原生滚轮的既有退化环境须保留完整历史的PgUp/PgDn可达性，不虚称支持局部滚轮；不要因限高把历史锁死。

## 定向验收

行为改动先有效RED再GREEN；先前5条截取、无历史滚轮或锚点错误要被相应检查准确抓住，导入错误/fixture坏了不算RED。纯视觉无需伪造失败。

- 数据到画面：真实task_view/VM接合成tasks.state，至少12条完成/放弃混合数据，包含做完未放行/正在做、空历史、短历史、长标题、长放弃原因。核对顺序/统计/排除规则、原始文件与VM不变，最近和最早记录在最终画面实际出现；不能只塞finished数组绕过原缺陷。
- 局部视口：上/下界、当帧命中/裁切/离屏、区域外事件、正文与历史交替滚动、外层翻页、项目切换、同项目刷新、顶部新增和阅读旧记录时新增、缩放重排/删除锚点的夹限。身份测试别靠长度不同导致夹限假装归零；变短仍溢出的案例要区分“合法偏移保留”与“越界夹限”。
- 分发纯度：带哨兵的真实tui路径核对纯滚轮无collect/git/corral/命令调用，旧键盘映射不回退；视口只存内存。
- 保住T6全部警告/拒绝原因、T7中文/合法Unicode/Tab展示副本、T8正文限高/局部滚轮及T12标签/理由样式、历史指标降权、短消息去重。原始业务字符串不因展示被改写。
- 复用实际curses/PTY基础检查120×32、80×24和窄小窗口的单/多项目、空闲/有当前任务、最早历史/长原因末尾以及缩放；检查最终屏幕，不只检查addstr参数或中间列表。多项目宽屏必要时补160列以覆盖原三栏布局。
- 证据保存在临时目录，记录复核命令/路径与测量数据；不把私有附件或大批录制放git。合成事件/实际PTY与真实设备验收分开。Computer Use已拒绝Ghostty/Terminal，不重试、不通过脚本/AppleScript等绕过；常用终端鼠标/触控板体验由用户验收。
- 仅跑直接相关定向回归，不跑全仓库四套；避免重复跑已被其它入口包含的检查。旧断言保留，若显示期望必须调整则逐项解释，不删断言蒙混。git diff --check通过。

## 主控前段调查裁定（2026-09-22）

被审提交 `6efe51b`，agent `drover/dev-history-scroll-1` 已 idle。本轮仅 ROADMAP 和任务调查记录变动，生产与测试未改；DONE 仅表示调查轮结束。**调查证据通过，T13 功能未完成，不可合并或打收尾记号。** 开发分支、worktree 和自开 agent 保留，等用户裁定后继续。

- 有效 RED 通过：主控确认快照与被审生产字节一致，重跑真实 task_view → queue_vm 的12条混合历史检查，准确因只返回5条而退出1；临时仅去掉截取的对照退出0。原文不变及进行中/等放行排除检查通过。这是数据定位对照，不是生产 GREEN 或最终屏幕验收。
- 性能门槛确认：主控核读探针，使用301个真实合成提交、300个不同有效区间及300份路由任务文件重测，每格两次。5条基线约0.029秒；300条基线仍只展示5条，约0.036秒、5次Git；取消截取后约2.021秒、300次Git，其中Git约1.933秒、路由约0.079秒。与开发三次样本中位数2.177秒结论一致。此为单项目 task_view 耗时，不扩写为完整刷新或设备延迟实测。
- 最小建议待用户批准：仅在当次刷新历史统计中批量读取所需Git提交信息，保持原 a..b 提交数语义与共享 task_commits 默认契约。路由查询暂不改，不加跨刷新缓存、后台线程、异步、持久化或通用框架；不恢复历史条数上限。获准后先对照旧实现验证分叉/合并、非祖先及空/相同/失效端点和历史改写，重测5/300条，再继续原定滚动功能。未声称此方案已实现或性能已达标。
- 用户 T13 明确要求明显退化先报告最小方案；据此暂停新增设计实施，尚未授权继续。不合并调查分支，不做清理或收尾空提交。历史/正文命中、锚点、缩放和真实PTY尚待实现后审查；真实设备体验仍交用户。
- 证据 `/tmp/drover-m22-maAzfb/`：开发 `probe.py`、`performance.json`；主控 `main-review-red.log`、`main-review-slice-control.log`、`main-review-performance.json`。仅定向复核与 diff 检查，无全仓库测试、无交叉审查。未操作真实 done/go/next/loop、未推送；T4暂缓，T8实验stash原样保留。

## 用户批准继续：最小批量历史查询（2026-09-22）

用户对主控提出的最小方案明确回复“批准”。本节覆盖上一节“待用户批准/暂停”的状态，允许原 agent 继续同一 T13，不另开任务。

- 批准仅在当次 task_view 的历史统计范围内批量读取所需 Git 提交信息并计算提交数；保留原 a..b 语义、共享 task_commits 默认契约及原始数据。不加跨刷新缓存、持久化、后台线程、异步或通用设施；路由查询暂不改，历史不再设隐形数量上限。
- 先在开发分支 ROADMAP 记下批准范围与必要取舍，再按最小方案实施。对照原 task_commits 覆盖分叉/合并、非祖先、空/相同/失效端点及历史改写，不能用线性序号相减代替可达性差集。沿用有效RED证据，新的统计行为先定向验证；5/300条真实合成Git重测耗时及调用数，说明批量失败时保持旧结果的最小处理。
- 查询正确性和性能通过后继续完成原任务全部历史展示/局部滚动/锚点/缩放/纯重绘及实际PTY验收。只跑相关检查，保留T6/T7/T8/T12回归；不重复全仓库测试，不加独立交叉审查。若方案需突破上述范围再报告；范围内实现细节自行处理，无需重复请求同一授权。
- 仍在原 m22-history-scroll worktree 和分支工作，不改主仓库 HANDOFF。主仓库本文件的调查审查及本授权保留，最终合并由主控处理；开发只追加自身完成记录，不覆盖主控记录。完成后由主控审查再本地合并收尾。真实设备交用户，不操作真实done/go/next/loop，不推送；T4暂缓，T8实验stash原样保留。

## 主控最终审查（2026-09-22，被审 b0a586e）

**结论：通过，可以本地合并。** 仅 bin/drover-board 历史统计/显示/输入、直接相关测试和两份设计任务文档有变化；bin/drover、共享判据/队列/引擎、命令调用与超时未改。不加独立交叉审查，未跑全仓库。

1. 批量统计通过：复核正常路径仅一次 Git、完整 SHA 祖先图按先子后父传播起止标记；共享 task_commits 未改。13项 history 检查覆盖合并/非祖先、失效端点、改写、超时及错误部分输出降级。主控另造50个分叉/合并提交、65个区间逐条对照原计数，全部一致且批量仅1次Git；额外验证注释标签对象SHA降级结果一致。接受仅当次刷新计算、特殊引用/失败回退旧路径的取舍；大祖先图与失败降级可能更慢，不保证所有仓库恒定耗时。
2. 性能通过：核读真实 subprocess 边界的开发测量，主控复用301个真实合成提交/300份任务文件单次复测，5条约0.0106秒、300条约0.0958秒，均1次Git且全部记账正确。与开发三次中位数300条0.0762秒同向，消除前轮约2秒/300次Git瓶颈。只代表该合成 task_view 样本。
3. 历史/交互通过：真实 tasks.state → task_view → VM 的12条混合历史保持顺序、计数、当前及等放行排除、原文不变；所有换行行和最早记录可达。正文/历史当帧命中、边界与离屏、纯重绘无采集/命令/文件IO、新历史锚点、任务删除、等长项目切换和缩放夹限均通过。接受短历史自然展开、长历史占剩余空间、窄屏先整页到入口、缺双向滚轮时保留完整整页路径。区域被外层页面部分裁切时，底部位置提示可能需再翻外层才能看到，完整内容仍可局部滚动抵达。
4. 定向回归通过：主控重跑 board-history 13项、board-body-scroll 10项、board-layout（含8组Tab/Unicode及8组消息边界实际curses）；T6警告/长拒绝、T7合法Unicode/Tab副本、T8正文限高/输入、T12消息去重及样式无回退。旧检查三处适配逐项接受：固定坐标可合法命中历史，历史遍历新增局部滚动，PTY传输增加可选历史fixture；未删旧末尾/行为断言。核对开发有效RED及6项缺陷植入记录，非语法或fixture失败。
5. 实际PTY通过：核对开发16组/2300帧及空stderr；主控另跑120×32单项目空闲、80×24多项目进行中、40×10单项目进行中、160×32多项目进行中四组历史PTY（各含缩放/刷新/双向滚动/最早记录），以及120×32单项目、80×24多项目两组正文PTY。抽读最终屏幕首尾，T1、TITLE-END、REASON-END及判据入口可见。这里是实际curses/PTY与合成SGR输入，不是桌面/真实设备验收；用户负责常用终端体验，未绕过Computer Use限制。

主控证据：`/tmp/drover-m22-main-review-extra/checks.json`、`/tmp/drover-m22-main-review-pty/`（JSON/ANSI及review-first/tail文本）；开发证据 `/tmp/drover-m22-maAzfb/` 原样保留。主控 diff 检查通过，主仓库调查裁定、98fb26c授权及最新HANDOFF保留。不操作真实done/go/next/loop、不推送；T4暂缓，T8实验stash原样保留。

## 提交与硬边界

先将本任务批准的局部设计及取舍简洁写入ROADMAP，随后实施；不改变无关已定设计。完成/调查记录追加本文件，只提交本worktree允许文件，不用git add -A。
不合并、不推送、不打收尾记号、不清worktree/分支，交主控定向审查；按当前路由不加独立交叉审查。主控审查通过后本地合并清理补收尾更新HANDOFF，真实设备验收交用户。
不修改真实queue.md/tasks.state，不操作真实done/go/next/loop，不启动真实循环，不运行安装脚本、不改系统环境。
不改corral/corral-dispatch，不读其源码或状态目录；不碰AGENTS保护的项目/服务，不开关其它agent，不按项目名批量杀进程。只用临时合成数据和假的corral。
T4继续暂缓；T8实验stash `e039c0163f1582e89cb162f9dab45f71ba3b3bff` 原样保留，不恢复/删除。

回复简述完成范围、RED/GREEN、性能数字、证据路径和提交号；需裁定时明确未完成部分，不虚报可合并。命令都在前台跑完，本轮完成或按要求报告阻碍时最后一行写DONE，表示本轮结束。

## 前段调查记录（2026-09-22，性能门槛触发，待裁定）

**本轮完成有效 RED 与真实历史性能测量，未完成 T13 功能，不可按功能完成合并。** 原生产代码保持五条截取；取消截取只发生在临时实验副本，用于定位和测量。区域布局、滚轮、锚点及最终画面验收均未实施。本轮只提交 ROADMAP 的已批准局部设计/性能取舍和本调查记录。

遵照本任务「若逐条统计造成明显刷新拖慢……此时停在需裁定处」及用户本轮要求，在 UI 实施前停下。没有自行加入缓存、异步、虚拟列表、改变刷新频率或提出另一条隐形数量上限。

### 环境与样本

- 基线提交 `274eda74ddc545126717f885e76082e29fd4a85c`；本 worktree 开始时干净。
- 使用 `/opt/anaconda3/bin/python3`，现场 Python 3.12.2，Git 2.55.0；标准库，无依赖安装。
- 证据目录 `/tmp/drover-m22-maAzfb/`（真实路径 `/private/tmp/drover-m22-maAzfb/`），包含 `probe.py`、生产快照 `drover-board-baseline`、仅去掉 `[-5:]` 的 `drover-board-unlimited`、合成仓库 `synthetic/` 和 `commits.json`。
- 合成仓库通过 `git commit-tree` 构造 301 个真实单亲提交并让 main 指向末端，提交树为空，SHA 各不相同。每条完成记录使用相邻提交作为起止，共 300 个不同有效区间，每次真实 `git rev-list a..b` 都查得一个提交；不是 a=b、空 SHA 或 stub git。线性且每区间只有一个提交，是简单样本，不能当作复杂大仓库的耗时上界。
- 性能样本无当前任务，单独测真实 `task_view`；有/无路由目录各测 5、300 条完成历史。目录固定含 300 份合成 Markdown，经真实文件名前缀查找、读取路由并验证返回值。测试中的计时包装均调用原实现，不替换查询结果；不读真实队列或 corral。

### 有效 RED 与定位对照

从合成 `tasks.state` 进入真实 `task_view → queue_vm`，12 条历史包含 9 完成、3 放弃，另分别覆盖空闲、正在做、做完未放行。包括中文/Tab/NBSP 长标题与完整长放弃原因；待办、统计、当前卡片及状态排除、VM 输入和原始文件不变的前置断言均通过。

- `red.log`：退出 1，三种状态均准确失败于「完整历史应为12条且含最早T1，实际5条」，实际 id 都是 `T12,T11,T10,T9,T8`。不存在导入、语法或 fixture 错误；空闲统计为完成9/放弃3，等放行时完成计数10但 T999 不进入历史，符合原契约。
- `slice-control.log`：临时副本只去掉原截取，三种状态都通过，顺序为 `T12…T1`；提交数、耗时、等放行时长、长原因末尾和原文不变断言通过。这只证明数据截取根因及数据对照 GREEN，**不代表生产 GREEN、滚轮或最终屏幕已完成**。

### 性能结果

每格独立执行三次，单位为秒，表中为 `task_view` 总时间中位数；耗时不含合成仓库创建。Git 调用次数在三次测量中一致。

| 路由目录 | 历史总数 | 原截取：显示5条 / git次数 / 秒 | 临时取消截取：显示全部 / git次数 / 秒 |
|---|---:|---|---|
| 未配置 | 5 | 5 / 5 / 0.031952 | 5 / 5 / 0.031839 |
| 未配置 | 300 | 5 / 5 / 0.045139 | 300 / 300 / 2.052592 |
| 300份任务文件 | 5 | 5 / 5 / 0.029951 | 5 / 5 / 0.029923 |
| 300份任务文件 | 300 | 5 / 5 / 0.043211 | 300 / 300 / 2.177290 |

有路由目录、300 条、取消截取的三次总耗时为 2.172858、2.177290、2.192606 秒（按大小列出），相对同样 300 条的基线约 50 倍。细分中位数：

- `git`：300 次，2.086445 秒，约占总时间 96%；主因是逐条同步 Git 子进程。
- `task_route`：300 次，0.083194 秒，其中 `os.listdir` 300 次 / 0.039481 秒，`route_line` 300 次 / 0.020145 秒。目录扫描及文件读取包含在路由总时间内，不能重复相加。
- 无路由目录时仍需 2.052592 秒，其中 Git 2.042473 秒，排除路由作为主要瓶颈。

这是单项目、仅历史数据阶段的耗时，不是完整 collect、VM、draw 或设备输入延迟测量。根据现有 tui 的同步刷新路径，历史查询期间无法读取下一次输入，因此即使空闲周期仍是30秒，r/周期/缩放刷新时增加约两秒停顿也已明显；多项目还会累加。不以「以后优化」略过这项门槛。

### 最小建议与恢复条件

建议只处理本次显著瓶颈：在**当次 task_view 的历史记账范围内**批量读取提交关系，按原 `a..b` 可达性差集给每条历史计算提交数，减少逐条启动 Git。保持共享 `task_commits` 默认契约、原始状态及统计语义，不跨刷新缓存、不持久化、不引入后台线程/异步或通用框架；路由先保持原查询方式。这是待裁定的局部方案，未实施、未声称已证明优化后性能。

恢复前需主控/用户裁定是否允许该历史查询调整（批量查询具体实现及失败降级尚待验证）。获准后先对照原 `task_commits` 验分叉/合并、非祖先、空/相同/失效端点及历史改写，不使用只适合线性样本的序号相减；重测5/300条证明瓶颈改善，再继续本任务区域滚动、阅读锚点、纯重绘和实际PTY验收。若批量方案需扩大共享接口或引入通用设施，继续报告，不擅自扩张。

### 复核命令、验证范围与边界

以下命令均用前台进程跑到退出；没有 shell 后台任务、真实循环或其它 agent：

```sh
/opt/anaconda3/bin/python3 -B /tmp/drover-m22-maAzfb/probe.py red
# 预期退出1，目标缺陷RED；日志 red.log。
/opt/anaconda3/bin/python3 -B /tmp/drover-m22-maAzfb/probe.py slice-control
# 预期退出0；仅临时取消截取的对照，日志 slice-control.log。
/opt/anaconda3/bin/python3 -B /tmp/drover-m22-maAzfb/probe.py benchmark
# 预期退出0；performance.log 与 performance.json 保存原始三次样本和细分次数/耗时。
git diff --check
```

临时脚本固定读取基线快照，通过既有 `board-demo.load_board` 加载真实生产函数；基线快照与本 worktree 的 `bin/drover-board` 字节一致。证据未放入 git，也未复制用户附件。

此次因性能门槛在生产改动之前停止，`bin/`、`tests/` 全部保持不变，T6/T7/T8/T12 的既有实现及断言未修改；未跑未改代码的全仓库四套或重复视觉矩阵。只做上述合成数据定向验证及 diff 检查，没有滚轮/PTY/真实设备验收，不虚报通过。未改 HANDOFF、其它 worktree、真实 queue.md/tasks.state、corral 或其技能/内部状态；未安装、推送、合并、打收尾记号、清 worktree/分支或动 T8 实验 stash。

## 获准继续后的完成记录（2026-09-22）

**T13 已在本分支完成，交主控定向审查；真实设备体验仍交用户。** 本轮先只读主仓库任务的「主控前段调查裁定」与「用户批准继续」（`98fb26c`），在本分支 ROADMAP 记录授权后实施。没有合并主仓库或 cherry-pick 交接提交，没有覆盖主控审查、授权或 HANDOFF；上节保留为前段调查历史，不再表示当前暂停。

### 实现与必要取舍

- 完整历史：取消数据入口五条截取，保留开始顺序的逆序、当前/等放行排除规则、统计和原字段。完成记录的提交数使用当次刷新批量查询，路由不改；没有新的隐形条数上限、归档或数据写回。
- 批量计数：完整 SHA 的正常路径一次 `git rev-list --parents --topo-order` 读取端点祖先图，传播各区间起止可达标记，只累计终点可达且起点不可达的提交。保留合并、分叉、非祖先语义，不用提交序号相减；共享 `task_commits` 未改。空/相同端点为零；符号/短引用、标签对象、坏端点、非零退出（包括部分stdout）或超时退回旧逐条查询。30秒 Git 超时不变，失败降级可能慢，但保留旧结果，不缓存或丢历史。
- 历史视口：保持原位置和分栏阈值，短列表自然展开，长列表按剩余空间占位，不改变前面的正文/判据/agent/待办；首屏外入口经 PgUp/PgDn 到达。再求占位与当帧页面的交集，只有可见内容行及该栏空白可命中；标题、提示、分隔线和离屏内容不命中。步长最多三行且不超过可见行数，位置提示按当前栏宽换行后的显示行统计。
- 阅读位置：同项目刷新按任务id及记录内行位置恢复；顶部跟随新记录，读旧记录时插入新历史不跳行。删除锚点、缩放重排或内容缩短后夹到合法位置；等长项目切换/项目重排也归零。锚点附上一帧全局偏移，仅用于分清显式滚轮与刷新；没有状态文件或跨刷新业务数据缓存。
- 正文/历史滚轮在现有 key_action 和 tui 路径中分发，只改对应内存偏移并 draw。外层偏移、其它区域、采集频率、r/周期/缩放刷新及业务按键不变；绘制后发生缩放仍丢弃旧坐标。无双向滚轮能力时历史不设局部限高，完整内容沿原 PgUp/PgDn 可达。
- 只提取一条历史的原有排版为 `history_lines`，`detail_sections` 增加默认不启用的历史占位参数；旧默认及 presentation 输出均与基线相等。T12的标签/理由、历史指标与普通短消息规则未改，T7 Unicode/Tab仅处理展示副本，T6警告/拒绝原因不变。

### RED → GREEN 与缺陷自证

原12条真实 tasks.state → task_view → queue_vm 的有效 RED 已由前轮及主控核实，本轮保存为正式 `tests/board-history.py` 检查并通过。新增有效 RED 分别为：5个有效区间仍调用5次Git而非1次（`batch-red.log`）；真实历史行滚轮返回None（`scroll-red.log`）；新增两行历史后旧位置仍为8而非10（`anchor-red.log`）。各自最小实现后 GREEN。

异常路径另发现并复现了批量非零退出但含部分输出时误采纳的问题：预期 `[5,4,3,2,1]`，错误结果为全1（`partial-batch-red.log`）；显式核对退出码并退回逐条查询后通过。这些RED均是目标断言失败，不是语法/导入错误；开发中修正的测试fixture/缩进错误未计作RED。

六个独立临时缺陷副本全部准确失败于目标断言：恢复五条上限、关闭历史滚轮、禁用锚点恢复、漏掉起点可达排除、历史滚轮误走采集、采纳批量错误的部分输出。见 `mutations.log` 与 `mutations/<名称>/result.log`。副本同时带配对 drover，生产文件未被植入。

### 最终真实性能

复用前段301个真实合成提交、300个不同有效单提交区间、300份路由文件；最终将计数移到实际 `subprocess.run` 边界，包含新增批量调用和原逐条调用，均执行真实Git，不stub结果。每格三次中位数，单位秒：

| 路由 | 历史数 | 原五条截取 | 仅取消截取、仍逐条 | 最终批量 | 最终Git次数 |
|---|---:|---:|---:|---:|---:|
| 无 | 5 | 0.028367 | 0.026328 | 0.005708 | 1 |
| 无 | 300 | 0.034823 | 2.082305 | 0.024246 | 1 |
| 有 | 5 | 0.028635 | 0.029297 | 0.007537 | 1 |
| 有 | 300 | 0.043736 | 2.368776 | 0.076177 | 1 |

原截取版本始终只显示5条/5次Git；逐条取消截取版本在300条时启动300次Git。最终300条含路由的Git中位数0.020248秒，路由0.051432秒/300次；路由逻辑未改。该样本的明显秒级停顿已消除，因此继续完成UI；数据是单项目 task_view 时间，不冒充完整采集或设备延迟，祖先图很大的仓库和失败降级仍可能更慢。

对照原 `task_commits` 验证了分叉、合并、非祖先/无共同祖先、空/相同/失效端点、有效与坏端点混合、符号main改写、`git replace`改写及批量超时/错误输出降级。多次真实刷新能立即反映新提交关系；另外以乱序任务编号验证历史不按编号重排。

### 定向检查与实际PTY

证据继续放 `/tmp/drover-m22-maAzfb/`，命令均在前台跑到退出，无全仓库四套、无交叉审查：

```sh
/opt/anaconda3/bin/python3 -B tests/board-history.py
/opt/anaconda3/bin/python3 -B /tmp/drover-m22-maAzfb/batch-benchmark.py
/opt/anaconda3/bin/python3 -B tests/board-layout.py
/opt/anaconda3/bin/python3 -B tests/board-body-scroll.py
/opt/anaconda3/bin/python3 -B tests/board-body-pty.py --record /tmp/drover-m22-maAzfb/body-pty
/opt/anaconda3/bin/python3 -B tests/board-history-pty.py --record /tmp/drover-m22-maAzfb/history-pty
git diff --check
```

- `board-history.py`：13项通过（`history-final-green.log`），包括真实数据/VM、全部换行后的最终字符行可达、当帧裁切/离屏/边界/非滚轮、区域外像素不动、正文/历史交替、锚点新增/删除/缩短/重排、等长项目身份、顶部刷新、空/短历史、能力退化整页可达及缩放夹在绘制/输入之间的旧坐标丢弃。带哨兵的真实tui路径确认滚轮无collect/git/corral/子进程/文件IO，原始VM/事件文件不改。
- `board-layout.py`通过（`layout-regression.log`），保留全部旧末尾标记、T6警告/拒绝、多行长消息、T12标签/理由样式和短消息边界检查；该入口已包含8组Tab/Unicode及8组消息临界列宽实际curses检查，没有重复单跑Tab入口。
- 原 `board-body-scroll.py` 10项及 `board-body-pty.py` 8组通过（`body-scroll-final.log`、`body-pty.log`），保住T8正文限高、纯滚动、输入映射与缩放。
- 新历史PTY复用 `board-body-pty.py` 的传输与录屏，默认正文路径不变；子进程先从临时真实Git/事件构建VM，再进入真实生产tui，发送合成SGR双向滚轮。120×32、80×24、40×10、160×32各单/多项目、空闲/进行中，共16组2300帧全部通过，stderr为空。每个尺寸的全部历史显示行都在最终curses屏幕出现，含最近T12、最早T1、长标题TITLE-END、长原因REASON-END、合法NBSP及Tab展示；每组再缩放至40×10、80×24、120×32检查首尾，刷新和切项目也有检查。
- `history-pty/`保存逐帧JSON与ANSI，并抽出4组首屏/末尾 `.txt` 供复核，已实际读取120列空闲首屏与80列进行中历史末页检查。原正文PTY在 `body-pty/`，完整测量在 `batch-final-performance.json/.log`，脚本为 `batch-benchmark.py`。
- AST差异只含新增历史计数/排版函数及 task_view、detail_sections、draw、key_action、tui；9类场景×3项目的默认详情文本/presentation与原基线逐字相等，见 `contracts-and-scope.log`。其余采集、VM、判据、引擎、命令/超时逻辑及bin/drover未改；diff检查通过。

### 旧检查的必要调整与交付边界

1. 原正文命中检查在正文离屏后，固定坐标现在可能落入新增历史区：若命中历史则断言正确history_scroll及步长，否则仍断言None；全部原正文命中、内容、裁切和边界断言保留。
2. 原layout长内容遍历及board-demo录制/交互增加历史局部滚动分发；旧「历史末尾」断言原样保留。历史已限高，不能只翻外层就当作遍历完；没有删断言或放宽末尾内容要求。
3. 原body-pty只增加可选历史fixture/检查入口以复用传输，默认8组正文断言全保留并重跑通过。新增历史PTY是独立定向入口，不扩大为设备验收。

只提交本worktree的bin/drover-board、直接相关测试及ROADMAP/本任务记录。不改主仓库/HANDOFF、真实队列、corral或其源码/状态目录；不安装、推送、合并、打收尾记号或清worktree/分支。T4暂缓、T8实验stash原样保留。未访问或绕过被拒绝的Ghostty/Terminal；上述仅证明合成数据和实际PTY，真实鼠标/触控板、字体配色仍由用户验收。

## 本地合并与收尾（2026-09-22）

主控定向审查通过后，本地合并 `ed93868`，被审 `b0a586e`；合并后 bin/tests/ROADMAP 与被审提交逐字一致，主仓库原调查裁定、98fb26c授权及最终审查均保留，无冲突。未扩大测试，无独立交叉审查。

清理前确认自开 agent idle、attached=0，worktree干净且分支已合入main；随后无force删除 `m22-history-scroll` worktree、用branch -d清开发分支，工作目录已删，一并关闭 `drover/dev-history-scroll-1`。本任务未开审查worktree/agent；当前corral仅剩本主控 drover/main。收尾空提交 `d8dad55`，随后更新HANDOFF。

未操作真实done/go/next/loop、未推送；用户仍需在常用终端确认实际鼠标/触控板体验，再自行按g核对放行。T4继续暂缓，T8实验stash `e039c0163f1582e89cb162f9dab45f71ba3b3bff` 已核对原样保留，不恢复、不删除。

## 用户验收与收工（2026-09-22）

用户明确反馈“已经放行了，我觉得还可以”，记录T13实际使用认可及用户已放行；不扩写为每种设备和尺寸逐项验过。用户同时授权本次提交、推送，并要求今日到此为止。本轮只补文档，不重跑测试、不启动新任务；推前扫描待推送提交，推后核对远端。T4继续暂缓，T8实验stash原样保留；不操作真实推进命令。
