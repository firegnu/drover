# 交叉审查：TUI 详情区翻页滚动

2026-09-21，drover/main 交给 drover/review-detail-scroll（Codex，重档：`-m gpt-6-astra -c 'model_reasoning_effort="xhigh"'`）。
你是被委派的审查者：只读审查，照本文件做，不要再开别的 agent。

## 背景

`bin/drover-board` 是 drover 的 curses 看板。右边详情区原先**超出窗口高度就截断**，末行写「… 还有 N 行，窗口再高些」，队列一长就不够用。`drover/dev-detail-scroll-1` 在 `m8-detail-scroll` 分支上给它加了翻页。

这件活的设计张力写在原任务书里，是它唯一需要拿主意的地方：

> `key_action` 是纯函数、`draw()` 只排版没有判断（ROADMAP 写死的不变量）。但滚动要夹住边界，就得知道「详情一共几行」和「一屏放得下几行」——**这两个数只有 `draw()` 拿到窗口尺寸之后才知道**。

它选的方案：抽出纯函数 `detail_viewport(total, room, offset, pages)` 统一算夹限和翻页；`draw()` 每帧把项目身份 / 行数 / 可视高度 / 归一后的偏移写回进程内 `state`；`key_action()` 读这些值算新偏移，保持纯函数。被否掉的两条（夹限埋进 `draw()`、由 `tui()` 算）它写了理由。

**主控已经审过的，不必重复**：

- 范围只动了 `bin/drover-board`、`tests/drover-board.sh`、任务文件；`draw` / `key_action` 签名没变；没加配置项、没落盘、没碰 `detail_lines()` 的内容、没动那 21 个被依赖的数据函数。
- 四个套件我自己重跑全绿（`drover-board` 从 10 块到 13）。
- **我往实现里植入了三个缺陷验证测试不是空转**：翻页不动 → 红；提示行盖到正文 → 红；**切项目不归零 → 当时照样绿**。第三条是我打回返工的原因：它原先两条「切项目归零」的断言都切到 `short_pv`（`queue: None`，详情只有两行），`limit` 算出来是 0，**任何偏移都会被夹限顺带压成 0**，断言分不出是归零逻辑起作用还是夹限压平的。返工提交 `65aa0d1` 补了等长项目（`equal_pv`）的断言，我重新植入同一个缺陷，这次确实红了。

所以**测试有效性这一项我已经用植入法查过**，请把力气放在下面「重点看」。

## 先读

1. `AGENTS.md`（「硬规矩」「技术约束」「测试」三节）
2. `docs/ROADMAP.md` D1 第 5 步第 3 小节（看板改 TUI 那段），特别是两句不变量：「不存自己的状态，刷新等于重跑」、`key_action` 纯函数 / `draw()` 只排版
3. `/Users/firegnu/Developer/personal_projs/drover/docs/任务/m8-detail-scroll 详情区滚动.md`（原任务书 + 实现时的取舍 + 完成记录）
4. 本文件

## 要审查的代码

- worktree：`/Users/firegnu/Developer/personal_projs/drover-worktrees/review-m8-detail-scroll`（**detached HEAD**，指向被审提交 `65aa0d1`）。这是你自己的 worktree，dev agent 在另一个目录，别去碰它。
- 改动：在该 worktree 里跑 `git diff main...HEAD`。三个文件：`bin/drover-board`、`tests/drover-board.sh`、任务文件。
- **只读：不改、不提交、不切分支、不合并、不推送。** 要试验就在 `$(mktemp -d)` 里造合成数据，把 `bin/drover-board` 当模块 import 来跑（`tests/drover-board.sh` 里有现成写法；注意手搓 `pv` 会和真结构走散，用 `view_model` 造）。
- 可以跑：`bash tests/criteria.sh`、`tests/drover.sh`、`tests/install.sh`、`tests/drover-board.sh`（只用合成仓库和假 corral，不花钱）。
- **不能碰**：真的 `~/Developer/personal_projs/jb-finetune`、`~/.review/`、`~/wt/`、herdr、`dev.herdsman.*` 的 launchd 任务、`~/.local/bin/` 里那六个老命令、`~/.config/review/`。不要 `launchctl` 任何东西，不要往 `~/.local/bin` 拷东西。也不要碰靶场 `drover-sandbox`。
- **不要动真的交接目录** `~/.drover/`：看板默认会读 `~/.drover/projects`，要跑真界面就用 `--projects` 指向你自己造的清单。
- 不要按项目名或路径批量杀进程（`pkill -f drover` 这类）：主控和别的 agent 的命令行里都带着项目名和工作目录，一条命令能全杀掉。

## 重点看

1. **`draw()` 每帧写 `state` 这个耦合有没有失效的窗口。** `key_action` 读的是上一帧 `draw` 留下的 `detail_total` / `detail_room` / `detail_offset`。第一帧之前、窗口刚 resize、`tui()` 里 `refresh` 之后重新 `collect` 导致行数突变、连按两次翻页键之间没有重画——这些时刻读到的是不是陈旧值？会不会算出越界或者「翻了但没动」的偏移？
2. **`detail_viewport` 的边界算术**。`page = max(0, room - (1 if total > room else 0))`、`limit = max(0, total - page) if page else 0`。请自己枚举临界组合（`total == room`、`total == room + 1`、`room == 0`、`room == 1`、`total == 0`），看有没有：翻不动、跳过行、重复显示某一行、或者最后一页显示不满却还说能往下翻。
3. **提示行和正文会不会打架**。正文画在 `top + i`（`top = 2`），提示行固定画在 `bot`（`h - 2`）。`shown = lines[offset:offset + page]`。请验证任何 `h`（尤其 `h = 5`、`h = 6`）下提示行都不会盖住正文最后一行、也不会留下空行；窄屏 `rail = 0` 和有侧栏两条路都要看。
4. **切项目归零用 `repo` 做身份** 够不够。项目列表增删、重排、`sel` 越界被夹、同一个 `repo` 出现两次、`ps` 为空（`project = None`）这些情形下，归零和沿用分别对不对。注意 `ps` 为空时 `project` 是 `None`，而 `state` 初始也没有 `detail_project`——这两个 `None` 撞在一起有没有问题。
5. **中文宽度**。详情行里有中文（任务标题）。翻页之后 `put()` / `trunc()` 的宽度计算还对吗？提示行 `PgUp↑N PgDn↓N` 在窄屏（20 列）下会不会被截断成看不懂、或者越界。
6. **有没有把 UI 状态漏到进程外**。硬约束是「看板不存自己的状态，刷新等于重跑」。请确认偏移只活在 `state` 字典里，没有写文件、没有进环境变量、`REFRESH_MS` 的语义没被改。
7. **测试本身还有没有别的「夹限顺带让断言通过」的假绿点**。上面背景里那个洞是我用植入法抓到的；同一类问题可能还在别处（比如某条断言期望的 0 或边界值，其实无论实现对错都成立）。挑你觉得可疑的，用植入法自己验一遍。

## 输出

追加到**本文件**末尾「## 审查意见」。用绝对路径写：`/Users/firegnu/Developer/personal_projs/drover/docs/任务/m8-detail-scroll 交叉审查.md`——它在**主仓库工作区**里，不在你的 worktree 里；**只写这一个文件，别的什么都不要改**。

先写一句结论（**可以合并** / **改完再合并**），然后每条意见写：级别（必须改 / 建议改 / 可以不改）、位置（文件:行）、问题、改法。最后对完成记录里「实现时的取舍」那四条逐条表态（同意 / 不同意 + 理由）。

## 回复

只写结论和各级别的条数。全部做完后，回复最后一行写 DONE。


## 审查意见

**可以合并。** 必须改 0 条，建议改 2 条，可以不改 6 条。没有发现本次实现的阻塞缺陷；两条建议都是已用缺陷植入证实的测试盲点，原实现均通过了对应的独立断言。

审查对象：detached HEAD `65aa0d1bb111f938c47ae35d8b3e95831dca2179`，以下代码行号均指该提交。按 `git diff main...HEAD` 审查；只在临时目录造合成仓库、假 corral 和变异副本，没有修改被审代码。

### 逐条意见

1. **建议改｜`tests/drover-board.sh:435`：内容缩短用例仍会被归零掩盖。**

   问题：同一个项目从 25 行变成 `queue=None` 的两行，合法偏移只能是 0，分不清「夹到新的非零末页」和「行数一变就回顶」。在临时副本的 `draw()` 调用 `detail_viewport()` 前植入 `if state.get("detail_total") != len(lines): offset = 0`，现有看板套件仍然 13 块全绿、退出 0。这不是原实现的缺陷：原实现会正确保留仍有效的偏移，或夹到新末页。

   改法：补一个**缩短后仍溢出**的同仓库用例，例如 10 行高窗口（正文每页 6 行），25 行详情、旧偏移 19，刷新成 12 行后必须为 **6**；再补旧偏移仍合法时保持原值的断言。我用真实 `collect()` / `view_model()` 生成 20 条和 7 条中文任务，得到 25 / 12 行详情：原实现为 6，植入版本为 0，能够明确区分。

2. **建议改｜`tests/drover-board.sh:533`、`tests/drover-board.sh:541`：翻页纯函数断言会被相反操作抵消。**

   问题：同一个 `scroll` 先 PgDn，再 PgUp，最后才检查字典未变。若在 `key_action()` 返回 `("scroll", offset)` 前植入 `state["detail_offset"] = offset`，第一次把 0 改成 6，第二次又改回 0；后续大多传入新字典，因此最后的相等断言仍过。临时副本实跑看板套件，13 块全绿、退出 0。第 552 行的旧纯函数检查没有翻页键，也抓不到它。

   改法：每次翻页调用前单独复制输入，调用后立即断言未变；PgDn 从 0 开始、PgUp 从非零偏移开始，分别验证。我增加的独立单次断言对原实现通过，对这个植入版本失败。当前 `key_action()` 本身确实没有写输入状态。

3. **可以不改｜`bin/drover-board:848`、`bin/drover-board:935`、`bin/drover-board:955`：视口回写与读键时序成立。**

   问题核对：首次 `getch()` 前已经 `draw()`；每个 scroll / sel 的 `continue` 都回到重画，连按也没有绕过重画的分支。refresh / resize / 超时先重新采集，再从循环顶部画新数据，之后才读下一个键。直接在首帧前调用纯函数会得到 `("scroll", 0)`，不会越界。若窗口尺寸恰在一次阻塞读键期间变化且普通翻页键先返回，单次步长仍按刚显示的那一屏计算，下一帧按新尺寸重新夹限；这不是持续沿用陈旧视口的路径。

   验证：真实 `tui()` 循环配假屏幕、临时项目及假 corral，依次连续 PgDn 两次、resize 到 6×20、PgDn、刷新缩短内容、resize 回 10×80、PgUp、超时，记录 `(总行数, room, offset)` 为 `(25,7,0) → (25,7,6) → (25,7,12) → (25,3,12) → (25,3,14) → (12,3,10) → (12,7,6) → (12,7,0)`；超时后保持 `(12,7,0)`。采集共 5 次（初始、两次 resize、一次 r、一次超时），翻页不采集。改法：无需改现有分发；以后若批量处理输入，须保留每次动作前视口有效这一前提。

4. **可以不改｜`bin/drover-board:832`：边界算术正确，末页重叠有明确原因。**

   问题核对：枚举 `total=0..40`、`room=0..20`、负数/零/中间/越界偏移及正负翻页，共 38,745 组通过；逐页遍历能覆盖所有行，非边界翻页不会跳过内容，溢出且可显示正文时每屏都填满。`total==room` 不留提示、偏移归零；`total==room+1` 留一行提示，因此需隐藏两行。例如 `(8,7,0,+1)` 得 `(2,6)`，不是漏算。`room==0`、或 `room==1 且 total>1` 返回 `(0,0)`；后者不会从实际 `draw()` 进入，因为合法窗口最小高 5，`room` 最小为 2。`total==0` 不滚动。

   改法：无需改。最后一步不足整页时会有意重叠上一屏末尾，以保证末页不留空白；例如 20 行、每页 6 行的偏移是 `0 → 6 → 12 → 14`。这是夹到末页的预期效果，不是跳过行或卡在同一行。

5. **可以不改｜`bin/drover-board:848`、`bin/drover-board:858`、`bin/drover-board:873`：提示行与正文没有重叠。**

   问题核对：溢出时 `page=h-4`，正文占 `2..h-3`，提示独占 `h-2`，消息占 `h-1`。不溢出时不画提示，正文可用到 `h-2`。对 9 种高度、12 种宽度、5 种偏移共 540 帧逐格核对边界与写入区间，没有正文/提示/侧栏互相覆盖，也没有溢出页中间的空行。高度 5 时是一行正文加一行提示，高度 6 时是两行正文加一行提示；有侧栏和 `rail=0` 均通过。

   改法：无需改。保留纯函数统一决定页容量、提示固定在 `bot` 的做法。

6. **可以不改｜`bin/drover-board:598`、`bin/drover-board:845`：用 `repo` 作滚动身份足够。**

   问题核对：相同长度的项目切换、重排后实际选中仓库改变均归零；实际选中仓库不变则沿用。增删列表及 `sel` 超上界后，`draw()` 用夹限后实际展示的仓库比较身份，结果正确。`discover()` 在进入 `view_model()` 前已按 realpath 去重，重复登记及符号链接别名不会生成两个独立项目；直接给 `draw()` 传两份相同 repo 时沿用偏移也符合「同一个项目」含义。空列表仅一行提示且上界为 0，所以初始缺失的 `detail_project` 与 `project=None` 相等不会遗留非零偏移；空列表转成非空时会重置。

   改法：无需增加索引或 `name` 到身份键，也无需为两个 None 单设分支。以上各路径均用从真实模型取得的项目验证。

7. **可以不改｜`bin/drover-board:763`、`bin/drover-board:875`、`bin/drover-board:877`：中文与窄屏沿用原宽度规则。**

   问题核对：翻页只改变切片，中文标题仍经 `put()` / `trunc()` 按显示格宽截断；没有新增按字符数直接裁剪。5×20 和 6×20 的提示分别完整显示 `PgUp↑0 PgDn↓24`、`PgUp↑0 PgDn↓23`，中间及底部同样没有越界。临界出现侧栏的 28 列也走了逐格检查。特别大的行数或更窄的详情栏仍可能按既有规则省略尾部数字，这属于横向截断的显示边界，不影响翻页与上下夹限。

   改法：本次无需新增宽度算法或改终端字符宽度约定。

8. **可以不改｜`bin/drover-board:851`、`bin/drover-board:882`、`bin/drover-board:972`：UI 状态没有持久化，刷新语义保持。**

   问题核对：新增四个 `detail_*` 字段仅写入 `tui()` 的局部 `state`，没有文件、环境变量或模块级持久存储写入。`REFRESH_MS` 仍为 30000，超时仍执行 `collect()` / `view_model()`，scroll 只重画；view_model 在画前画后相等。没有把循环引擎搬回看板。

   改法：无需改；保留数据采集与临时视口状态的现有分工。

### 验证记录

- 前台执行 `for t in criteria drover install drover-board; do bash "tests/$t.sh" || exit 1; done`，四套件全绿，退出 0；安装套件 9 项，看板套件 13 块。`git diff --check` 通过。
- 补充探针全部在临时目录运行，Python 禁写字节码；合成项目经 `collect()` / `view_model()` 生成，没有手搓项目字段。算术、逐格排版、项目身份、真实 `tui()` 分发探针全部通过。
- 两个缺陷植入各用独立的临时脚本副本，通过 `DROVER_BOARD_BIN=<临时副本> bash tests/drover-board.sh` 运行；原套件均退出 0。独立补充断言则分别击中「缩短后错误回顶」和「按键函数修改输入」。没有把变异写进被审 worktree。
- 本轮没有重做真实终端的像素验收；完成记录中的 PTY 验收仍是实现者提供的证据。本轮新增证据是带严格边界/重叠检查的假屏幕和真实事件循环，不能据此声称覆盖所有终端的字形显示差异。

### 对「实现时的取舍」逐条表态

1. **同意**（原任务文件第 87 行）：`draw()` 回写视口、纯函数统一夹限、`key_action()` 只读的组合符合原任务明确允许的方案；当前事件循环保证使用前已绘制，副作用也局限于进程内 UI 状态。第一条取舍需要保留这个时序前提。
2. **同意**（第 88 行）：不把边界算法直接埋进绘制代码，使上述 38,745 组临界组合能脱离 curses 验证，职责没有散开。
3. **同意**（第 89 行）：不让 `tui()` 计算夹限，事件循环只应用返回偏移；本次没有理由再增加一套边界判断。
4. **同意**（第 90 行）：仓库路径身份配合现有 realpath 去重足够，PgUp/PgDn 不抢原按键，固定提示行保证正文与提示分离，偏移只存在内存中。末页为填满而重叠、极窄横向空间按既有规则截断，都是可接受的显示边界。


## 复核

**可以合并。** 原两条建议均已解决；本轮新增必须改 0 条、建议改 0 条。

范围仅为 `git diff 65aa0d1 2001a3f`。已确认当前 detached HEAD 为 `2001a3fa3020472b4b33f2d9b19d79b6be850c2b`；差异仅 `tests/drover-board.sh` 新增 33 行，没有删除或替换旧行，生产代码未变。

1. **已解决｜`tests/drover-board.sh:402`、`tests/drover-board.sh:447`：缩短后仍溢出。** `overflow_pv` 沿用同一 repo，仅将队列缩成 7 条，并直接断言详情为 12 行。每个用例先重画 25 行内容，再设置并重画旧偏移，确认 19 或 3 确实保留，随后才缩短；缩短后还明确检查 `detail_total==12`、`detail_room==7`。正文每页为 6 行，新上界为 6，因此 `19→6` 区分了重新夹限、错误归零和沿用越界值，`3→3` 又区分了合法偏移保留、无条件归零和无条件跳末页。两个期望都非零，不能再被夹限到零顺带满足；80 列和 20 列均执行。符合原建议，没有发现新的假绿点。

2. **已解决｜`tests/drover-board.sh:548`：每次调用立即检查输入不变。** PgDn 从 0、PgUp 从 12，各自创建独立 candidate，调用前复制，调用后立即比较，并另外断言返回 `("scroll", 6)`。两次期望偏移都与各自输入不同，所以写回状态会当场失败，没有相反操作抵消的机会，也不能靠「什么都不做」通过返回值断言。输入字典的值均为整数，`copy()` 足以保存独立快照；candidate 非空，`act()` 会把这个原字典传给 `key_action()`。符合原建议，没有发现新的假绿点。

旧断言没有削弱：旧夹限、短内容、首帧、项目切换与重排、按键行为及纯函数检查全部保留。新增渲染用例各自重建 state，后面的等长项目用例也自行重建 state；新增按键用例使用独立 candidate，不会污染后面旧断言使用的 scroll。

验证：前台执行 `bash tests/drover-board.sh`，13 块全部通过、退出 0；`bash -n tests/drover-board.sh` 和 `git diff --check 65aa0d1 2001a3f` 通过。按复核要求，没有重复两个缺陷植入，也没有重审整个分支或重跑其余三个套件。两个植入被新断言击中的结果采用主控本次提供的验证记录，不计作本轮独立实测。
