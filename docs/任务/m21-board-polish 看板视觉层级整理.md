# T12：整理看板视觉层级与排版（保留现有交互）

2026-09-22，drover/main 委派 drover/dev-board-polish（Codex，常规档 gpt-6-astra / high）。
路由：常规 / 交叉审查不要（路由：tier拿不准，常规概率0.78；cross_review不要，最高0.07；主控按单模块显示与消息呈现调整选择常规档）。
你是被委派的开发agent，照任务做，不再委派。用户已正式发出TASK T12，本轮开始实施；“入队不自动执行”描述的是此前阶段。

## 先读

AGENTS.md、HANDOFF.md、README.md；docs/ROADMAP.md的T6消息、T7字符网格和T8局部滚轮/最终运行范围；`docs/任务/m20-board-layout 看板排版评估.md`末尾主控最终裁定；bin/drover-board显示层和直接相关测试；本文件。

用户提供的三张基准截图：
- `/Users/firegnu/Desktop/SCR-20260922-qteh.png`：空闲/待办，历史单栏。
- `/Users/firegnu/Desktop/SCR-20260922-qtkf.png`：刚派发，有上下重复操作消息。
- `/Users/firegnu/Desktop/SCR-20260922-qtpm.png`：进行中，刷新后消息消失。

请实际用view_image读取用户附件，理解视觉问题；这是读取用户附件，不是桌面抓屏。不要复制截图进入git。三图只证明宽屏单项目这些静态帧，不代表窄屏、多项目或设备操作验收。

## 工作区与文件范围

- worktree：`/Users/firegnu/Developer/personal_projs/drover-worktrees/m21-board-polish`，分支`m21-board-polish`，从main建立，无需安装依赖。
- 生产仅改`bin/drover-board`的展示/布局相关函数；允许直接必要的小辅助函数，不创建通用渲染框架，不重写整个TUI。
- 直接相关测试优先沿用tests/board-layout.py、board-demo.py、board-tab-pty.py、board-body-scroll.py、board-body-pty.py；按必要性最小调整或新增一个聚焦入口。不要顺手修改无关测试。
- 文档仅更新ROADMAP中相应视觉设计/取舍及本任务文件完成记录。HANDOFF由主控维护，主仓库和其它worktree只读。
- bin/drover、collect/view_model/criteria/loop_tick、队列与事件解析、命令调用/超时、输入映射、刷新频率不改。drover-board仍是共享数据模块，保持既有对外函数契约，不为局部样式重构共享接口。

## 四项具体目标

1. **历史降权、续行整齐。** 当前任务保持最突出，历史明细适度弱化、记账指标再低一级，但不能暗到难读；不能无差别调暗全站dim（当前指标、提示等共用）。保留状态符号/文字、全部历史条目及放弃原因。标题续行与指标层级统一，不删内容、不折叠、不增加历史数量限制。
2. **判据状态突出，理由正常色。** 仅符号和短名称按现有状态着色，完整理由以普通正文色显示，明确续行缩进；保持成功/失败/不适用的现有含义，不推导新状态。保留原始理由及Unicode，不按标点拆原字段、不根据字符串内容误识别样式。窄到标签放不下时仍安全换行。
3. **普通短消息有条件去重。** 只有确认是普通操作反馈、单行、在底部实际可用显示列内完整容纳时，才省去正文里的重复消息块；底部仍显示。按put的末行保留格、实际列宽以及Tab展示约定判断，边界宽度/缩放不能丢尾字。多行、过长、含警告或拒绝原因、不能可靠判定的消息继续保留完整可滚动版本。采用最小保守规则，不建立通用自然语言分类器，不改run_drover的命令行为或T6消息优先顺序，不新增消息超时/清除/刷新机制。
4. **顶底栏低对比、对齐统一。** 减轻大面积反色，只突出按键本身，保持全部现有按键提示及末行消息。不要把强反色简单铺满底栏，不加整屏外框，不新增颜色配置、字体设置或主题系统。保留多项目选中态的清晰区分；无色/少色终端仍可辨认状态。

## 不变的交互与边界

- 保持T7原有单/多项目、双栏阈值与窄屏降级；无任务时辅助信息自然上移，不为填空白新增内容。
- 保住T8正文高度、自身滚动、当帧命中、项目/任务身份、刷新保持及缩放夹限；纯滚轮只重绘，不采集、不发命令。原PgUp/PgDn整页可达性照旧。
- 所有长标题、正文、判据、历史、待办、警告/拒绝原因仍完整可达。合法Unicode与Tab只处理展示副本，原VM和文本不改写。
- 不新增焦点、折叠、帮助、按钮或快捷键；不改g的核对/记账/放行、档位或循环。不迁移Textual，不安装依赖。
- 使用用户已确认的现有 `/opt/anaconda3/bin/python3` 验证原生双向滚轮；不修改启动器、PATH或系统Python，不恢复旧curses协议兼容目标。

## 定向验证与证据

纯样式不伪造RED；消息显示条件等行为调整先写/定位有效失败用例，再最小实现。用临时合成数据、假corral，不读写真实队列，不启动真实看板或循环。

- 消息：短普通成功反馈只显示一次；临界列宽、中英混合宽字符、Tab/多行、缩小后溢出、长警告/拒绝原因仍完整可达；不以清空msg或改变刷新节奏“去重”。
- 样式/排版：历史长标题和指标层级、判据标签与理由颜色分离、顶底栏减少反色且全部按键仍可见。断言最终绘制/样式结果，避免只断言新辅助函数。
- 相关回归：中文/合法Unicode、T7 Tab最终屏幕、长内容翻到末尾、T8局部滚轮/当帧命中/纯重绘及输入映射。只跑相关入口，不跑全仓库四套；同一入口已包含其它检查时不要无意义重复。
- 若旧纯视觉断言固定要求bar反色或旧坐标/缩进，允许按这次授权的视觉目标精确更新，并在完成记录逐条解释；保留或加强行为断言，不以删断言或放宽范围消除失败。
- 用现有演示/PTY基础检查120×32、80×24及窄小窗口；包含单/多项目、空闲/进行中、短消息/长拒绝原因、窗口缩放和末尾可达。保留基线与新版本可复核的最终屏幕文本、ANSI或既有录制产物，放临时目录并在任务记录写路径；不新增大型截图/测试基础设施，不把截图私自纳入git。
- 清楚区分合成事件、真实curses/PTY最终屏幕与真实设备。Computer Use访问Ghostty/Terminal曾被安全限制拒绝，不重试或用脚本/AppleScript/其它通道绕过；桌面颜色、字体及真实滚轮由用户最终确认，PTY证据不等于真实设备已验。
- `git diff --check`通过。记录最短必要的RED/GREEN、相关回归、范围及展示取舍；不做全仓库审查或重复无关测试。

## 提交、禁止与回复

只在本分支提交允许文件，不用git add -A。完成记录追加本文件；设计与理由简洁写入ROADMAP，先记录本任务批准的局部变更再实现，不重新打开其它设计议题。
不要合并、推送、打收尾记号或清理worktree/分支；交主控定向审查，无独立交叉审查。实际观感由用户在常用终端确认，不自行补签。
不操作真实done/go/next/loop，不改真实queue.md/tasks.state，不安装或改PATH，不改corral/corral-dispatch、不读其源码或内部状态，不碰AGENTS的受保护目录和服务，不开关其它agent，不按项目名批量杀进程。
T4继续暂缓；T8实验stash `e039c0163f1582e89cb162f9dab45f71ba3b3bff` 原样保留，不恢复、不删除。

回复简述四项改动、定向检查、证据路径、提交号和剩余实际观感确认。命令都在前台跑完，全部做完后，回复最后一行写DONE。
